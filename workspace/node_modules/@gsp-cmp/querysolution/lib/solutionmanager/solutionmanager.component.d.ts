import { OnInit, TemplateRef, EventEmitter } from '@angular/core';
import { FormBuilder } from '@angular/forms';
import { EditService } from '../service/edit.service';
import { NotifyService } from '@farris/ui-notify';
import { QuerySolutionVo } from '@farris/component-querycondition';
import { LocaleService } from '@farris/ui-locale';
import { DialogComponent } from '@farris/ui-dialog';
import { DatagridComponent } from '@farris/ui-datagrid';
export declare class SolutionManangerComponent implements OnInit {
    private formBuilder;
    private editService;
    private notify;
    private localeService;
    cancelEmitter: EventEmitter<any>;
    confirmEmitter: EventEmitter<any>;
    querySolutions: Array<QuerySolutionVo>;
    deletedQuerySolutionIds: Array<String>;
    checkedSolution: QuerySolutionVo;
    listData: QuerySolutionVo[];
    dialog: DialogComponent;
    optCell: TemplateRef<any>;
    checkCell: TemplateRef<any>;
    list: DatagridComponent;
    columns: Array<any>;
    title: string;
    authorityError: boolean;
    constructor(formBuilder: FormBuilder, editService: EditService, notify: NotifyService, localeService: LocaleService);
    ngOnInit(): void;
    show(querySolutions: QuerySolutionVo[]): void;
    getSolutionProperty(value: boolean): any;
    propertyFieldFormatter: (value: any, data: any) => any;
    findSolutionsWithoutSys(querySolutions: QuerySolutionVo[]): QuerySolutionVo[];
    delelteSolution(dataItem: QuerySolutionVo): void;
    setDefault(checked: boolean, dataItem: QuerySolutionVo): void;
    cancel(): void;
    confirm(): void;
}
