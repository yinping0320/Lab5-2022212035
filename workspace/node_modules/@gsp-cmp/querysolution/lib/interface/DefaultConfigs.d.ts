import { FieldConfig } from '@farris/component-querycondition';
export interface DefaultConfigs {
    /**
     * 对筛选条件赋默认值
     */
    fieldConfigs: FieldConfig[];
    /**
     * 赋值范围标记，为true时对所有方案赋默认值，false时只对系统默认方案复制
     */
    isCurrent: boolean;
    readonly: boolean;
}
