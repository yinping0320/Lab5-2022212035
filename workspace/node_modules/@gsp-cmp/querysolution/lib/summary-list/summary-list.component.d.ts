import { OnInit, ElementRef, NgZone, OnDestroy, ChangeDetectorRef } from '@angular/core';
import { QueryCondition } from '@farris/component-querycondition';
export declare class SummaryListComponent implements OnInit, OnDestroy {
    private ngZone;
    private cd;
    cls: boolean;
    wrapperEl: ElementRef;
    conditions: Array<QueryCondition>;
    enableTips: boolean;
    private ro;
    widthFlag: number;
    constructor(ngZone: NgZone, cd: ChangeDetectorRef);
    ngOnDestroy(): void;
    ngOnInit(): void;
    private observerElement;
    private changeTipsState;
}
