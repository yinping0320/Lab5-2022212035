import { OnChanges, EventEmitter, SimpleChanges, ComponentFactoryResolver, Injector } from '@angular/core';
import { CodeRuleSelectService } from '../../service/code-rule-select.service';
import { CodeRuleInvokeInfo } from '../../model/code-rule-invoke-info';
export declare class SelectCodeRuleComponent implements OnChanges {
    private codeRuleSvc;
    private resolver;
    private injector;
    serviceUnit: string;
    sessionId: string;
    selectorClosed: EventEmitter<any>;
    codeRuleSelected: EventEmitter<CodeRuleInvokeInfo>;
    constructor(codeRuleSvc: CodeRuleSelectService, resolver: ComponentFactoryResolver, injector: Injector);
    ngOnChanges(changes: SimpleChanges): void;
    /**
     * 点击确认，确认编号规则
     */
    confirmCodeRule(): void;
    /**
     * 点击取消，关闭选择器
     */
    closeSelector(): void;
}
