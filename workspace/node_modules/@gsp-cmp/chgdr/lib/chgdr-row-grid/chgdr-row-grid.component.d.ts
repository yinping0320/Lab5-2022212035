import { Injector, OnChanges, OnInit, SimpleChanges } from '@angular/core';
import { DatagridComponent } from '@farris/ui-datagrid';
import { ChangeColumnItem } from '../chgdr-data-viewer/chgdr-data';
import { ChgdrTreeNode } from '../chgdr-data-viewer/chgdr-tree-builder';
export declare class ChgdrRowGridComponent implements OnInit, OnChanges {
    private injector;
    chgdrTreeNode: ChgdrTreeNode;
    dataGrid: DatagridComponent;
    columns: any[];
    data: ChangeColumnItem[];
    constructor(injector: Injector);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    private readonly fieldNameColumnTitle;
    private readonly valueColumnTitle;
    private readonly oldValueColumnTitle;
    private readonly newValueColumnTitle;
}
