import { Injector, OnChanges, OnInit, SimpleChanges } from '@angular/core';
import { MessagerService } from '@farris/ui-messager';
import { NotifyService } from '@farris/ui-notify';
import { ChgdrService } from '../service/chgdr.service';
import { ChangeDataQueryResult } from '../model/change-data-query-result';
import { BeforeShowChangeDetailEvent } from '../model/chgdr-ui-config';
import { Observable } from 'rxjs';
export declare class ChgdrListComponent implements OnInit, OnChanges {
    private chgdrService;
    private messageService;
    private notifyService;
    private injector;
    baseCls: boolean;
    /** 业务实体ID */
    entityId: string;
    /** 数据ID */
    dataId: string;
    /** 变更时间-起始 */
    changeTimeStart: Date;
    /** 变更时间-结束 */
    changeTimeEnd: Date;
    /** 打开详情前的回调 */
    beforeShowChangeDetail: (event: BeforeShowChangeDetailEvent) => Observable<boolean>;
    private currentQueryParam;
    chgdrs: ChangeDataQueryResult;
    constructor(chgdrService: ChgdrService, messageService: MessagerService, notifyService: NotifyService, injector: Injector);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    query(): void;
    private buildQueryParam;
    private doQuery;
    onPageChanged($event: any): void;
    onPageSizeChanged($event: any): void;
    readonly startTimeLabel: string;
    readonly endTimeLabel: string;
    readonly queryBtn: string;
}
