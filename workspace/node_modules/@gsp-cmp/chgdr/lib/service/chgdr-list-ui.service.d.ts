import { ComponentFactoryResolver, Injector } from '@angular/core';
import { MessagerService } from '@farris/ui-messager';
import { BsModalService } from '@farris/ui-modal';
import { ChgdrListDialogConfig } from '../model/chgdr-ui-config';
export declare class ChgdrListUiService {
    private resolver;
    private injector;
    private modalService;
    private messagerService;
    constructor(resolver: ComponentFactoryResolver, injector: Injector, modalService: BsModalService, messagerService: MessagerService);
    showDialog(entityId: string, dataId: string): any;
    showDialog(config: ChgdrListDialogConfig): any;
    private create;
}
