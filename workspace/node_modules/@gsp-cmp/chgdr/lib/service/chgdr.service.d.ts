import { Injector } from '@angular/core';
import { HttpService, SessionService } from '@ecp-caf/caf-common';
import { Observable } from 'rxjs';
import { ChangeDataQueryParam } from '../model/change-data-query-param';
import { ChgLogConfigField } from '../model/chgdr-config-field';
import { ChgdrItemQueryParam } from '../model/chgdr-item-query-param';
export declare class ChgdrService {
    private http;
    private sessionService;
    private injector;
    private rootDataCodeFieldsMap;
    constructor(http: HttpService, sessionService: SessionService, injector: Injector);
    queryChangeDataHeader(currentQueryParam: ChangeDataQueryParam): Observable<any>;
    getRootEntityDataCodeFields(beId: string): Observable<ChgLogConfigField[]>;
    /** 将字符串或数字转为Date */
    private toDate;
    /**
     * 查询业务变更日志详细数据。
     * @param id headerId
     * @param changeTime 变更时间，和header.changeTime保持一致
     */
    getChangeData(headerId: string, changeTime: Date): Observable<any>;
    getChangeData(param: ChgdrItemQueryParam): Observable<any>;
    private getChangeDataById;
    private getChangeDataByParam;
    private transChangeDataFromResponse;
    private transChangeDataRowsFromResponse;
}
