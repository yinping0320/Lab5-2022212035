import { Injector } from '@angular/core';
import { HttpService, SessionService } from '@ecp-caf/caf-common';
import { GSPBusinessEntity } from '@gsp-bef/gsp-be-metadata';
import { GSPMetadataRTService } from '@gsp-lcm/metadatart-selector';
import { Observable } from 'rxjs';
export declare class ChgdrMetadataService {
    private http;
    private sessionService;
    private metadataService;
    private injector;
    private beMetadataCache;
    constructor(http: HttpService, sessionService: SessionService, metadataService: GSPMetadataRTService, injector: Injector);
    /** 获取BE元数据 */
    getGSPBusinessEntity(beId: string): Observable<GSPBusinessEntity>;
}
