export declare class I18nUtil {
    static instant(key: string, param?: any): string;
    private static getCurrentLangResource;
    static instantOrDefault(key: string, defaultText: string): string;
}
export declare class I18nService {
    constructor();
    instant(key: string, param?: any): string;
    private getCurrentLangResource;
    static staticInstant(key: string, defaultText: string): string;
}
