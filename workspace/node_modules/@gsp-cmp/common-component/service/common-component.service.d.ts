import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Observable } from 'rxjs';
import { CommonStructureDTService } from '@ecp-caf/common-structure';
export declare class CommonComponentService {
    private http;
    private structureSvc;
    private commom_component_url;
    constructor(http: HttpClient, structureSvc: CommonStructureDTService);
    getComponent(componentId: string, headers: HttpHeaders): Observable<any>;
    /**
     * 获取构件中操作参数的Schema
     * @param componentId 构件Id
     * @param operationCode 操作编号
     * @param paramCode 参数编号
     * @param headers 请求header，应包含sessionId等必需参数
     */
    getParamSchema(componentId: string, operationCode: string, paramCode: string, headers: HttpHeaders): Observable<any>;
    /**
     * 获取构件中操作返回值的Schema
     * @param componentId 构件Id
     * @param operationCode 操作编号
     * @param headers 请求header，应包含sessionId等必需参数
     */
    getReturnValueSchema(componentId: string, operationCode: string, headers: HttpHeaders): Observable<any>;
    private getCollectionType;
    private getMapType;
    private getStructureReferType;
    private getAllStructures;
}
