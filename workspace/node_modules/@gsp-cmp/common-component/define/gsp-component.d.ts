import { Operation, StructuredType } from '@ecp-caf/common-structure';
import { RefStructureInfo as RefStructureInfo } from './ref-structure-info';
export declare class GspComponent {
    id: string;
    code: string;
    name: string;
    description: string;
    application: string;
    serviceUnit: string;
    version: string;
    /**
     * 构件操作列表
     */
    operations: Array<Operation>;
    /**
     * 构件结构化类型列表（构件本身的dto列表）
     */
    structuredTypes: Array<StructuredType>;
    /**
     * 构件引用的结构化类型列表（从各类元数据中引用）
     */
    refStructures: Array<RefStructureInfo>;
}
