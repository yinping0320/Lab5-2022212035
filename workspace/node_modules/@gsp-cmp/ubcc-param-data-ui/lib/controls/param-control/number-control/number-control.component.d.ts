import { OnInit, OnChanges, SimpleChanges } from '@angular/core';
export declare class NumberControlComponent implements OnInit, OnChanges {
    param: any;
    canEdit: boolean;
    precision: number;
    max: number;
    min: number;
    constructor();
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    isReadonly(): boolean;
    getUnit(): string;
    show(): boolean;
}
