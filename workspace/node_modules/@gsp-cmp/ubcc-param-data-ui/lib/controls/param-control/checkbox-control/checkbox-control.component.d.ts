import { OnInit } from '@angular/core';
import { UbccParamDataUtils } from '../../../utils/param-data-utils';
export declare class CheckboxControlComponent implements OnInit {
    private service;
    param: any;
    canEdit: boolean;
    checkboxId: string;
    constructor(service: UbccParamDataUtils);
    ngOnInit(): void;
    getValue(): boolean;
    enable(value: any): void;
    isReadonly(): boolean;
    show(): boolean;
}
