import { TemplateRef } from '@angular/core';
import { MessagerService } from '@farris/ui-messager';
import { NotifyService } from '@farris/ui-notify';
import { IDataType } from '@ecp-caf/common-structure';
export declare class UbccParamDataUtils {
    private messagerService;
    private notify;
    constructor(messagerService: MessagerService, notify: NotifyService);
    isHelpType(dataType: IDataType): boolean;
    isHelpTypeRef(dataType: IDataType): boolean;
    getCollectionElementType(dataType: IDataType): any;
    isCollection(dataType: IDataType): boolean;
    getModalConfig(btnRef: TemplateRef<any>): {
        title: string;
        width: number;
        height: number;
        resizable: boolean;
        showButtons: boolean;
        showMaxButton: boolean;
        enableScroll: boolean;
        buttons: TemplateRef<any>;
    };
    showInfo(message: string, infoType: string, title: string, timeout: number): void;
    newGuid(): string;
    s4(): string;
}
