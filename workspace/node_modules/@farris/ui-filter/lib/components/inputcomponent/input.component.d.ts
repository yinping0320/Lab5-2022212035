import { OnInit, OnChanges, EventEmitter, AfterViewInit, ElementRef } from '@angular/core';
import { QueryCondition } from '../../entity/querycondition';
import { SolutionService } from '../../service/solution.service';
export declare class InputComponent implements OnInit, OnChanges, AfterViewInit {
    private solutionService;
    private readonly element;
    /**
     * QueryCondition类型
     */
    item: any;
    autoLabel: boolean;
    disabled: boolean;
    searchHandle: EventEmitter<{}>;
    /**判断是否是在扩展区域展示 */
    extend: boolean;
    /**判断是否显示label */
    showlabel: boolean;
    placeholder: string;
    readonly isExtend: boolean;
    readonly hidenLabel: boolean;
    constructor(solutionService: SolutionService, element: ElementRef);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    ngOnChanges(): void;
    getConditionItemTitle(field: QueryCondition): string;
    InputChanged(e: any): void;
}
