import { OnInit, EventEmitter, ElementRef } from '@angular/core';
import { QueryCondition } from '../../entity/querycondition';
import { SolutionService } from '../../service/solution.service';
export declare class SingleNumberComponent implements OnInit {
    private solutionService;
    private readonly element;
    autoLabel: boolean;
    disabled: boolean;
    searchHandle: EventEmitter<{}>;
    /**判断是否是在扩展区域展示 */
    extend: boolean;
    /**判断是否显示label */
    showlabel: boolean;
    canNull: boolean;
    number: string;
    precision: number;
    placeholder: string;
    bigNumber: boolean;
    max: number;
    min: number;
    readonly isExtend: boolean;
    readonly hidenLabel: boolean;
    _item: any;
    numberRange: string;
    item: any;
    constructor(solutionService: SolutionService, element: ElementRef);
    ngOnInit(): void;
    getConditionItemTitle(field: QueryCondition): string;
    numberChange(event: any): void;
}
