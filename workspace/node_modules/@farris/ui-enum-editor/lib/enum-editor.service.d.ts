import { LocaleService } from '@farris/ui-locale';
import { BsModalService } from '@farris/ui-modal';
import { Injector, ComponentFactoryResolver, ComponentRef } from '@angular/core';
import { Subject } from 'rxjs';
import { EnumEditorComponent } from './enum-editor.component';
export declare enum EnumOutType {
    'array' = 0,
    'object' = 1
}
export declare class EnumEditorService {
    private injector;
    private cfr;
    private modalService;
    private localeService;
    enumEditorRef: ComponentRef<EnumEditorComponent>;
    dlgRef: any;
    originalData: any[];
    textField: string;
    valueField: string;
    outType: EnumOutType;
    dataChanged: Subject<{}>;
    constructor(injector: Injector, cfr: ComponentFactoryResolver, modalService: BsModalService, localeService: LocaleService);
    showDialog(enumData?: any[], options?: {}): void;
    cancel(): void;
    save(): void;
    private _dataChanged;
    toString(enumData: any): string;
    toJSON(val: any): any[];
    private convertObject2Array;
}
