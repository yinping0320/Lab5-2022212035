import { OnInit, EventEmitter, OnChanges, SimpleChanges } from '@angular/core';
import { ElementPropertyConfig, PropertyChangeObject } from '../../entity/property-entity';
import { PropertyItemListComponent } from '../property-item-list/property-item-list.component';
export declare class PropertyPanelComponent implements OnInit, OnChanges {
    width: number;
    height: number;
    /** 是否白色主题 */
    isWhiteTheme: boolean;
    /** 是否持有面板的隐藏显示状态 */
    isPersitOpenState: boolean;
    /** isPersitOpenState=true时，控制面板是否隐藏显示 */
    isShowPanel: boolean;
    /** isPersitOpenState=true时，抛出面板的关闭事件 */
    closePropertyPanel: EventEmitter<any>;
    /** 属性类型 */
    propertyConfig: ElementPropertyConfig[];
    /**  属性值 */
    propertyData: {};
    dynamicControl: boolean;
    /** 属性变更事件 */
    propertyChanged: EventEmitter<any>;
    /** 模态框属性变更事件   */
    submitModal: EventEmitter<any>;
    /** 是否展示关闭按钮 */
    showCloseBtn: boolean;
    private eastDiv;
    itemLists: Array<PropertyItemListComponent>;
    /** 当前展示状态 */
    isOpen: boolean;
    /** 外层分类，以标签页形式展示 */
    categoryTabs: any[];
    /** 当前选中的标签页 */
    selectedTab: any;
    constructor();
    ngOnInit(): void;
    /**
     *  更改面板的隐藏显示状态，只有在isPersitOpenState=true时生效
     */
    ngOnChanges(simpleChanges: SimpleChanges): void;
    refreshPanel(): void;
    changeStatus(item: any): void;
    /**
     *  隐藏面板
     */
    collapse(): void;
    /**
     * 抛出属性变更事件
     */
    _valueChanged(changeObject: PropertyChangeObject): void;
    _submitModal($event: PropertyChangeObject): void;
    /**
     * 将属性分类按照标签页进行归类
     */
    private checkShowTabCategory;
}
