import { ComponentFactoryResolver, EventEmitter, Injector, OnInit } from '@angular/core';
import { BsModalService } from '@farris/ui-modal';
import { NotifyService } from '@farris/ui-notify';
import { PropertyEntity } from '../entity/property-entity';
export declare class ModalPropertyComponent implements OnInit {
    private notifyServ;
    private resolver;
    private modalService;
    private injector;
    valueChanged: EventEmitter<any>;
    submitModal: EventEmitter<any>;
    private _elementConfig;
    private _elementValue;
    /** 输入框中显示的值 */
    elementShowValue: string;
    showClearButton: boolean;
    elementConfig: PropertyEntity;
    elementValue: any;
    constructor(notifyServ: NotifyService, resolver: ComponentFactoryResolver, modalService: BsModalService, injector: Injector);
    ngOnInit(): void;
    changeValue($event: any): void;
    /**
     * 清除模态框内容
     */
    modalClear(): void;
    /**
     * 模态框场景下将属性值转换为输入框中显示的值
     * @param value 属性值
     */
    private convertModalShowValue;
    /**
     * 自定义编辑器使用模态框打开
     */
    openModal(): void;
    /**
     * 创建自定义编辑器
     */
    private createEditorComponent;
}
