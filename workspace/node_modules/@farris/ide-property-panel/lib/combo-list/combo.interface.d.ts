import { FarrisDataState } from '@farris/ui-common';
import { InjectionToken } from '@angular/core';
export declare type ComboPanelAlign = 'left' | 'right' | 'auto';
export declare const DEFAULT_COMBOSTATE: InjectionToken<any>;
export declare const COMBO_FACADE_SERVICE: InjectionToken<any>;
export interface ComboPanelState {
    width: number;
    height: number;
    left: number;
    top: number;
    align: ComboPanelAlign;
    open: boolean;
}
export interface ComboState extends FarrisDataState {
    readonly: boolean;
    editable: boolean;
    panel: ComboPanelState;
}
export declare const defaultComboState: ComboState;
export interface SelectItem {
    index: number;
    data: any;
}
import { EntityFilter } from '@farris/ui-common/types';
export declare type LOAD_DATA_TYPE = 'all' | 'fav' | 'list' | 'nav' | 'search' | 'children' | 'textchange' | 'sort';
export interface RemoteParams {
    pageIndex?: number;
    pageSize?: number;
    condition?: EntityFilter;
    helpId?: string;
    searchField?: string;
    searchValue?: string;
    sortName?: string;
    sortOrder?: string;
}
export interface NavigationFilter {
    searchField?: string;
    searchValue?: string;
    selected?: any;
    idValue?: string;
}
export interface SearchParam {
    searchField?: string;
    searchValue?: string;
    category?: LOAD_DATA_TYPE;
    navigation?: NavigationFilter;
}
export declare type LOOKUP_HTTP_PARAMS = RemoteParams | any;
