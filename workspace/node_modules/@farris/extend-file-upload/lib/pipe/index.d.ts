import { PipeTransform } from "@angular/core";
import { Injector } from "@angular/core";
import { ColumnFormatService } from "@farris/ui-common/column";
import { DateTimeHelperService } from "@farris/ui-common/date";
/**
 * 文件大小格式化
 */
export declare class FFileSizePipe implements PipeTransform {
    transform(value: any, args?: any): any;
}
/**
 * 日期格式化
 */
export declare class FFileDatePipe implements PipeTransform {
    private dateSer;
    constructor(dateSer: DateTimeHelperService);
    transform(value: any, returnFormat?: string): any;
}
/**
 * 格式化自定义数据
 */
export declare class FormatCellDataPipe implements PipeTransform {
    private cfs;
    private injector;
    private elRef;
    private localeId;
    constructor(cfs: ColumnFormatService, injector: Injector);
    /**
     * 格式化数据
     * @param column:列信息
     * @param rowData:当前数据
     * @param settings:配置信息
     * @param state: 状态
     * @returns
     */
    transform(column: any, rowData: any, rowIndex: any, settings: any, state?: string): any;
    /**
     * 获取真正的数据
     * @param rowData
     * @param field
     * @returns
     */
    private getRealDataValue;
}
/**
 * 附件可能之前生成的单据中并没有多语言服务
 * 调用附件的公共服务
 */
export declare class UPLocalePipe implements PipeTransform {
    constructor();
    transform(name: any, ...replaceVal: any[]): any;
}
/**
 * 解决变量有个默认的文字，变量最终可能是默认值也可能不是
 */
export declare class UPLocaleDefaultPipe implements PipeTransform {
    constructor();
    transform(realValue: any, compareTxt: string, localeName: string): any;
}
