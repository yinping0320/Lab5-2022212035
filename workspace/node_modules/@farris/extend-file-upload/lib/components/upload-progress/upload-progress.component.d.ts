import { OnInit, SimpleChanges, OnChanges } from '@angular/core';
export declare type UploadProgressStatusType = 'success' | 'error' | 'active' | 'normal';
export declare class UploadProgressComponent implements OnInit, OnChanges {
    showInfo: boolean;
    status: UploadProgressStatusType;
    successPercent: number;
    percent: number;
    maxWidth: number;
    private cachedStatus;
    private inferredStatus;
    readonly progressStatus: UploadProgressStatusType;
    constructor();
    ngOnChanges(changes: SimpleChanges): void;
    ngOnInit(): void;
    isNotNil<T>(value: T): value is NonNullable<T>;
}
