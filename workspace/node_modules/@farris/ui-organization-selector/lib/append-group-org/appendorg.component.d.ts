import { OnInit, AfterViewInit, SimpleChanges, OnChanges, ChangeDetectorRef, Injector, TemplateRef, NgZone } from '@angular/core';
import { ApiService } from '../../utils/api.service';
import { TreeNode, TreeTableComponent } from '@farris/ui-treetable';
import { MessagerService } from '@farris/ui-messager';
export declare class AppendGroupOrgComponent implements OnInit, AfterViewInit, OnChanges {
    private apiService;
    private cdr;
    private injector;
    private ngZone;
    private msgService;
    baseCls: boolean;
    orgTree: TreeTableComponent;
    orgName: TemplateRef<any>;
    primaryField: string;
    enableHierarchicalLoading: boolean;
    notGetStopOrg: boolean;
    layer: number;
    multiSelect: boolean;
    absOrgType: any;
    cascadeCheck: boolean;
    cascadeDown: boolean;
    cascadeUp: boolean;
    keepSelect: boolean;
    showCheckAll: boolean;
    selectedLimit: number;
    pathLength: number;
    virtualizedLoad: boolean;
    originalTreeData: any[];
    treeListData: TreeNode[];
    treeTableCols: any[];
    currentOrgs: any[];
    initLoad: boolean;
    private loadingService;
    private localeService;
    beforeClickNode: (e: any) => import("rxjs").Observable<boolean>;
    constructor(apiService: ApiService, cdr: ChangeDetectorRef, injector: Injector, ngZone: NgZone, msgService: MessagerService);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    changeSelections(e: any): void;
    toggleChecked(e: any): void;
    toggleSelected(e: any): void;
    hlAssembleData(): void;
    assembleData(): void;
    private inTreeList;
    private tickCheckedNodes;
    /**
    * 组织数据转换
    * @param plainSource 接口原始数据
    * @param layer 层级
    * @returns 返回树形接口的数据
    */
    private dataToTree;
    private dealDataWhenExpandTreeNode;
    private dealData;
    /**
     * 数据转换
     * @param parents 父级节点信息
     * @param treeSource 原始数据信息
     * @param layer 层级
     */
    treeData(parents: any, treeSource: any, layer: number): void;
    listenTreeExpand(obj: any): void;
    getOrganizationByParentNode(parentNode: any): void;
    private nodeExpanded;
    private expandTreeNode;
}
