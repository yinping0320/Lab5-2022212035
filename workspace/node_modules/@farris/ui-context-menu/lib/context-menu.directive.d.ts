import { Injector, ElementRef, OnInit, Renderer2 } from '@angular/core';
import { Observable } from 'rxjs';
import { FarrisContextMenuService } from './context-menu.service';
import { BeforeShowMenu, ContextMenuItem } from './menu-item.interface';
export declare class FarrisContextMenuDirective implements OnInit {
    private ctxMenuSer;
    private injector;
    private elRef;
    private render;
    menuItems: ContextMenuItem[];
    menuClass: string;
    disabled: boolean;
    context: any;
    beforeShowContextMenu: (e?: any) => Observable<BeforeShowMenu>;
    /** 触发右键菜单实际DOM标签名称 */
    activeDomName: string;
    highlight: boolean;
    id: string;
    constructor(ctxMenuSer: FarrisContextMenuService, injector: Injector, elRef: ElementRef, render: Renderer2);
    ngOnInit(): void;
    onContextMenu(event: MouseEvent): void;
    private removeSurplusMenus;
    private contextMenuId;
    private getContextData;
    private getViewMenuItems;
    private checkMenuItems;
    private checkVisibleAndDisable;
}
