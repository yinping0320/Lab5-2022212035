export interface ContextMenuItem {
    id: string;
    title: string;
    icon?: string;
    visible?: (e: any) => boolean | boolean;
    disable?: (e: any) => boolean | boolean;
    keyboard?: any;
    children?: ContextMenuItem[];
    handle?: (e?: any) => any;
}
export interface ContextMenuOptions {
    id?: string;
    /** 触发右键菜单的实际DOM */
    activeDom?: HTMLElement;
    /** 菜单项集合 */
    menuItems?: ContextMenuItem[];
    /** 鼠标事件参数 */
    event?: MouseEvent;
    /** 事件传递的上下文数据，在菜单打开前构造，每个菜单点击都会带着 */
    context?: any;
    /** 自定义菜单样式 */
    menuClass?: string;
    /** 菜单挂载的主体Dom */
    target?: HTMLElement;
    /** 触发右键菜单的实际宽度 */
    activeWidth?: number;
    /** 高亮触发右键DOM */
    highlight?: boolean;
}
export declare type BeforeShowMenu = {
    show: boolean;
    data: any;
} | boolean;
