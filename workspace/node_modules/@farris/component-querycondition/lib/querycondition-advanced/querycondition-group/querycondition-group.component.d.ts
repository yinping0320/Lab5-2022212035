import { OnInit, ChangeDetectorRef, OnDestroy, Injector } from '@angular/core';
import { QueryconditionAdvancedData } from '../type/type';
import { StateService } from '../service/state.service';
import { QueryCondition } from '../../entity/QueryCondition';
import { MessagerService } from '@farris/ui-messager';
import { LocaleService } from '@farris/ui-locale';
import { Subscription } from 'rxjs/internal/Subscription';
export declare class QueryconditionGroupComponent implements OnInit, OnDestroy {
    private stateSer;
    private cd;
    private msgSer;
    private localeService;
    private injector;
    COMPARE_TYPE_NAME: string[];
    COMPARE_TYPE_NAME_MULTI: string[];
    COMPARE_TYPE_IN_CONTROL: number[][];
    data: QueryconditionAdvancedData;
    fieldConfig: Array<QueryCondition>;
    checklist: Array<string>;
    deleteGroupText: any;
    fieldConfigSubscribe: Subscription;
    checkedListSubscribe: Subscription;
    forceState: any[];
    _self: any;
    activeConditionType: any;
    private overLayService;
    constructor(stateSer: StateService, cd: ChangeDetectorRef, msgSer: MessagerService, localeService: LocaleService, injector: Injector);
    ngOnInit(): void;
    ngOnDestroy(): void;
    /**
     * 下拉面板展开关闭事件
     * @param event 下拉展开tue， 关闭false
     * @param dropdownMenu 点击的dropdown序号
     */
    dpStateHandler(event: any, dropdownMenu?: any): void;
    /**
     * 修改筛选条件控件类型
     * @param item 筛选条件对象
     * @param fieldItem 选中的控件类型
     */
    changeConditionType(item: any, fieldItem: any): void;
    /**
     * 修改筛选条件比较符
     * @param item 筛选条件对象
     * @param compareTypeIndex 筛选条件比较符
     */
    changeCompareType(item: any, compareTypeIndex: any): void;
    /**
     * 添加筛选条件
     * @param addPositionItem 添加的位置
     */
    addItem(addPositionItem: any): void;
    /**
     * 删除筛选条件
     * @param item 删除项
     */
    deleteItem(item: any): void;
    /**
     * 修改筛选条件组关联关系
     */
    changeRelation(): void;
    /**
     * 选中筛选条件
     * @param item 选中项
     */
    handleCheckItem(item: any): void;
    /**
     * 取消筛选组
     */
    deleteGroup(): void;
}
