import { OnInit, OnChanges, EventEmitter, SimpleChanges } from '@angular/core';
import { QueryCondition } from '../../entity/querycondition';
import { SolutionService } from '../../service/solution.service';
export declare class DropDownListComponent implements OnInit, OnChanges {
    private solutionService;
    hideLabel: boolean;
    /**
     * QueryCondition类型
     */
    item: any;
    /**
     * 某个字段发生值变化事件时，需要同步清空的字段，可能不是当前字段
     */
    changeFields: Array<string>;
    conditionChange: EventEmitter<{}>;
    dropDownListDataSource: Array<any>;
    uri: string;
    beforeShow: any;
    beforeHide: any;
    idField: string;
    textField: string;
    selectedItemId: any;
    testId: string;
    placeholder: string;
    editable: boolean;
    constructor(solutionService: SolutionService);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    getDataSource(): void;
    selectChange(selected: any): void;
    onClear(event: any): void;
    getConditionItemTitle(field: QueryCondition): string;
}
