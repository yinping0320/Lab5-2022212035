import { OnInit, Injector, OnChanges, SimpleChanges, EventEmitter } from '@angular/core';
import { QueryCondition } from '../../entity/querycondition';
import { SolutionService } from '../../service/solution.service';
import { CheckBoxControl } from '../../entity/controltype/checkbox/checkbox-control';
export declare class CheckBoxComponent implements OnInit, OnChanges {
    private injector;
    hideLabel: boolean;
    _item: QueryCondition;
    checkValue: any[];
    controlData: CheckBoxControl;
    item: QueryCondition;
    testId: string;
    /**
     * 某个字段发生值变化事件时，需要同步清空的字段，可能不是当前字段
     */
    changeFields: Array<string>;
    conditionChange: EventEmitter<{}>;
    solutionService: SolutionService;
    constructor(injector: Injector);
    ngOnChanges(changes: SimpleChanges): void;
    ngOnInit(): void;
    valueChanged(event: any[]): void;
    getConditionItemTitle(field: QueryCondition): string;
}
