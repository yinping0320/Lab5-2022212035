import { OnInit, OnChanges, Injector, EventEmitter, SimpleChanges } from '@angular/core';
import { QueryCondition } from '../../entity/querycondition';
import { SolutionService } from '../../service/solution.service';
import { RadioControl } from '../../entity/controltype/radio/radio-control';
export declare class RadioComponent implements OnInit, OnChanges {
    private injector;
    hideLabel: boolean;
    _item: QueryCondition;
    solutionService: SolutionService;
    radioControl: RadioControl;
    radioValue: any;
    testId: string;
    showLabel: boolean;
    item: QueryCondition;
    /**
     * 某个字段发生值变化事件时，需要同步清空的字段，可能不是当前字段
     */
    changeFields: Array<string>;
    conditionChange: EventEmitter<{}>;
    cls: boolean;
    constructor(injector: Injector);
    ngOnChanges(changes: SimpleChanges): void;
    ngOnInit(): void;
    valueChanged(event: any): void;
    getConditionItemTitle(field: QueryCondition): string;
}
