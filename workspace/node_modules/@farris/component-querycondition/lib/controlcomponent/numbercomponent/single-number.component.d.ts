import { OnInit, EventEmitter, OnChanges, SimpleChanges } from '@angular/core';
import { QueryCondition } from '../../entity/querycondition';
import { SolutionService } from '../../service/solution.service';
export declare class SingleNumberComponent implements OnInit, OnChanges {
    private solutionService;
    hideLabel: boolean;
    number: number;
    placeholder: string;
    precision: number;
    textAlign: string;
    item: any;
    testId: string;
    canNull: boolean;
    bigNumber: boolean;
    min: number;
    max: number;
    /**
     * 某个字段发生值变化事件时，需要同步清空的字段，可能不是当前字段
     */
    changeFields: Array<string>;
    conditionChange: EventEmitter<{}>;
    constructor(solutionService: SolutionService);
    ngOnChanges(changes: SimpleChanges): void;
    ngOnInit(): void;
    valueChange(data: any): void;
    getConditionItemTitle(field: QueryCondition): string;
}
