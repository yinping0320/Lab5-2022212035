import { OnInit, EventEmitter, OnChanges, SimpleChanges, AfterViewInit } from '@angular/core';
import { SolutionService } from '../service/solution.service';
import { LocaleService } from '@farris/ui-locale';
import { DialogComponent } from '@farris/ui-dialog';
import { QueryCondition } from '../entity/querycondition';
import { MultiSelectComponent } from '@farris/ui-multi-select';
import { NotifyService } from '@farris/ui-notify';
export declare class ConfigDialogComponent implements OnInit, OnChanges, AfterViewInit {
    private solutionService;
    private localeService;
    private notify;
    inputSelectedFileds: Array<QueryCondition>;
    filedsDataSource: Array<QueryCondition>;
    cancelEmitter: EventEmitter<any>;
    confirmEmitter: EventEmitter<any>;
    dialog: DialogComponent;
    multiSelectCmp: MultiSelectComponent;
    selectedIds: Array<string>;
    title: string;
    visibleFields: Array<QueryCondition>;
    visibleSelectedFields: Array<QueryCondition>;
    constructor(solutionService: SolutionService, localeService: LocaleService, notify: NotifyService);
    ngOnChanges(changes: SimpleChanges): void;
    ngOnInit(): void;
    ngAfterViewInit(): void;
    /**
     * 取消
     */
    cancel(): void;
    /**
     * 确认
     */
    confirm(): void;
    /**
     * 重置
     */
    reset(): void;
}
