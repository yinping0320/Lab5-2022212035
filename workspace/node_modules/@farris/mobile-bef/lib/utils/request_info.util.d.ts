import { Entity } from '@farris/mobile-devkit';
import { BefRepository } from '../bef_repository';
import { RequestInfo } from '../types';
declare class RequestInfoUtil {
    /**
     * 构造RequestInfo
     */
    static buildRequestInfo(repository: BefRepository<Entity>): RequestInfo;
    /**
     * 向body中添加RequestInfo
     */
    static appendRequestInfoToBody(body: any, repository: BefRepository<Entity>): any;
}
export { RequestInfoUtil };
