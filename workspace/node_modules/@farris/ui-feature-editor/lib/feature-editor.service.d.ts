import { ComponentFactoryResolver, EventEmitter, Injector } from '@angular/core';
import { BsModalService, BsModalRef } from '@farris/ui-modal';
import { LocaleService } from '@farris/ui-locale';
import { IFeature, IFeatureEditorOptions } from './type';
import { I18nService } from './i18n-service';
export declare class FeatureEditorService {
    injector: Injector;
    private modalService;
    private componentFactoryResolver;
    private localeService;
    private languageService;
    dialog: BsModalRef;
    private componentRef;
    private keyDownHandler;
    onUserConfirmed: EventEmitter<any>;
    private eventManager;
    constructor(injector: Injector, modalService: BsModalService, componentFactoryResolver: ComponentFactoryResolver, localeService: LocaleService, languageService: I18nService);
    /**
     * 弹出特性编辑器
     * @param features 特征数组
     * @param options 弹窗配置
     * @returns
     */
    show(features: Array<IFeature>, options?: IFeatureEditorOptions): void;
    private registerShortcutKey;
    private setFirstInputFocus;
    private onTabKeydownHandler;
    /**
     * 用户点击确认按钮
     */
    private onConfirmClick;
    /**
     * 确认
     * @param options options
     */
    private onConfirm;
    /**
     * 是否可以关闭
     * @param options options
     * @returns
     */
    private canClose;
    /**
     * 是否可以弹出特性编辑器
     * @param options options
     * @returns
     */
    private canOpen;
}
