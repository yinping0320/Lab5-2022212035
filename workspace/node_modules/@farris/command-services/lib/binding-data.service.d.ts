import { AppContext, BindingData } from '@farris/devkit';
/**
 * 实体服务
 * @scope FrameComponent
 */
declare class BindingDataService {
    private bindingData;
    private appContext;
    /**
     * 构造函数
     */
    constructor(bindingData: BindingData, appContext: AppContext);
    /**
     * 设置当前框架的当前行
     */
    setCurrentId(id: string, frameId: string): void;
    setCurrentRow(id: string, frameId: string): void;
}
export { BindingDataService };
