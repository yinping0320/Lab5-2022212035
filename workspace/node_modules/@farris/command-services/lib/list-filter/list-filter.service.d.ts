import { FrameContext } from '@farris/devkit';
import { ListFilterHandler, QueryCondition } from '@farris/ui-list-filter';
export declare class ListFilterConditionsService implements ListFilterHandler {
    private frameContext;
    constructor(frameContext: FrameContext);
    setCurrentFilterConditions(conditions: QueryCondition[]): void;
}
