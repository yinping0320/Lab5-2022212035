import { MessagePipe } from './message-pipe';
import { Subject } from 'rxjs';
export declare class MessagePipeService {
    private static rtfService;
    private static querystringService;
    /**
     * 通过token获取消息管道
     * @param token 消息管道标识
     */
    static get(token: string): MessagePipe;
    /**
     * 创建消息管道
     * @param pipeName 管道名称
     * @param useFormContext 是否使用表单上下文创建管道标识
     * @param pipeType 管道类型(可选)，默认Subject<any>
     */
    static create(pipeName: string, useFormContext: boolean, pipeType?: Subject<any>): MessagePipe;
}
