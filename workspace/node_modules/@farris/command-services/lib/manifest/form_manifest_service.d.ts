import { HttpClient } from "@angular/common/http";
import { Injector } from "@angular/core";
import { Observable } from "rxjs";
import { FormManifest, IFormManifestService } from "@farris/devkit";
export declare class FormManifestService implements IFormManifestService {
    private injector;
    private modulePath;
    private httpClient;
    private static mainfests;
    constructor(injector: Injector, modulePath: any, httpClient: HttpClient);
    load(): Observable<FormManifest>;
}
