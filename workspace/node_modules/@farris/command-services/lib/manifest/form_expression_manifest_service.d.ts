import { HttpClient } from "@angular/common/http";
import { Injector } from "@angular/core";
import { Observable } from "rxjs";
import { FrameContext, IFormExpressionManifestService, IFormManifestService } from "@farris/devkit";
export declare class FormExpressionManifestService implements IFormExpressionManifestService {
    private injector;
    private frameContext;
    private httpClient;
    private modulePath;
    private formManifestService;
    private static mainfests;
    constructor(injector: Injector, frameContext: FrameContext, httpClient: HttpClient, modulePath: any, formManifestService: IFormManifestService);
    load(): Observable<any>;
}
