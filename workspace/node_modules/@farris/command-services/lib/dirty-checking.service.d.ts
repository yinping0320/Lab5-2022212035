import { ApplicationRef, ChangeDetectorRef, Injector } from "@angular/core";
/**
 * angular 脏值检查服务
 */
export declare class DirtyCheckingService {
    injector: Injector;
    private cd;
    private appRef;
    constructor(injector: Injector, cd: ChangeDetectorRef, appRef: ApplicationRef);
    /**
     * Checks this view and its children.
     * @param global 是否全局检查
     */
    detectChanges(global?: boolean | string): void;
    /**
     * 在指定时间（ms）后触发Angular脏值检查
     * @param duration 超时时间(ms)
     * @param global 是否全局检查
     */
    detectChangesAfter(duration: number, global?: boolean | string): void;
}
