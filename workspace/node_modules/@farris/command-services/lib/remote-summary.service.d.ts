import { ViewModel } from '@farris/devkit';
import { Observable } from 'rxjs';
export declare class RemoteSummaryService {
    private viewModel;
    constructor(viewModel: ViewModel);
    /**
     * 服务器端合计
     * @param methodName 服务器端合计自定义动作名称
     * @param filters 过滤条件
     */
    summary(methodName: string, filters: any): Observable<any>;
}
