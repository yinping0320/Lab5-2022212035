import { ViewModel } from '@farris/devkit';
import { FilterConditionService } from '../filter-condition.service';
import { CommandService } from '../command-service';
declare class FilterConditionDataService {
    private viewModel;
    private filterConditionService;
    private commandService;
    constructor(viewModel: ViewModel, filterConditionService: FilterConditionService, commandService: CommandService);
    /**
     * 执行过滤
     * @param commandName 命令名称
     * @param frameId 命令所在的frameId
     */
    filter(commandName: string, frameId: string): import("rxjs").Observable<any>;
}
export { FilterConditionDataService };
