import { Injector } from "@angular/core";
import { WorkFlowMessageService } from "./work-flow-message.service";
/**
 * 工作流消息
 * @warning 禁止使用表单上下文，所有上下文必须从外部传入
 */
export declare class WorkFlowMessage {
    private injector;
    workFlowMessageService: WorkFlowMessageService;
    private listeners;
    constructor(injector: Injector, workFlowMessageService: WorkFlowMessageService);
    setup(): void;
    addEventListener(callback: (event: MessageEvent) => void): string;
    removeEventListener(eventId: string): void;
}
