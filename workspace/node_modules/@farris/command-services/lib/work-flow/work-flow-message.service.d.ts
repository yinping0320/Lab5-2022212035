import { WorkFlow } from "./types";
export declare class WorkFlowMessageService {
    private messageHandler;
    private handler;
    constructor();
    /**
     * 监听工作流消息
     * @param callback 回调
     */
    listen(callback: (e: WorkFlow.IMessage) => void): void;
    destroy(): void;
    /**
     * 给工作流表单发送消息
     * @param message
     */
    send(message: WorkFlow.IMessage): void;
}
