import { FrameContext, NgFormControl } from "@farris/devkit";
export declare class FormControlService {
    constructor();
    getFormControlsByFrameContext(frameContext: FrameContext): {
        [propertyName: string]: NgFormControl;
    } | null;
    /**
     * 通过binding元数据获取NgFormControl
     * @param frameContext
     * @param binding binding
     * @returns
     */
    getFormControlByBinding(frameContext: FrameContext, binding: string): NgFormControl;
    /**
     *
     * @param frameContext frameContext
     * @param fullPath
     * @returns
     */
    getFormControlIndexByFullPath(frameContext: FrameContext, fullPath: string): number;
    /**
     * 通过binding元数据获取NgFormControl位置
     * @param frameContext
     * @param binding binding
     * @returns 从0开始
     */
    getFormControlIndexByBinding(frameContext: FrameContext, binding: string): number;
}
