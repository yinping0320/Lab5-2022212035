import { Injector } from '@angular/core';
import { NumberFormat, UserSettings } from '@farris/devkit';
import { FrmI18nSettingService } from '@gsp-sys/rtf-common';
export declare class UserSettingsService implements UserSettings {
    private injector;
    private frmI18nSettingService;
    constructor(injector: Injector, frmI18nSettingService: FrmI18nSettingService);
    /**
     * 用户时区字符串
     */
    readonly timeZone: string;
    /**
     * 时区偏移
     */
    readonly timeZoneOffset: number;
    /**
     * 默认语言
     */
    readonly defaultLanguage: string;
    /**
     * 日期格式化字符串
     * like: yyyy-MM-dd
     */
    readonly dateFormat: string;
    /**
     * 时间格式
     * like: HH:MM:ss
     */
    readonly timeFormat: string;
    readonly dateTimeFormat: string;
    /**
     * 地址格式
     */
    readonly addressFormat: any;
    /**
     * 数字格式
     */
    readonly numberFormat: NumberFormat;
    /**
     * 区域代码
     */
    readonly regionCode: string;
    /**
     * 用户配置
     */
    private readonly userSettings;
    private readonly timezone;
    private readonly i18nSetting;
}
