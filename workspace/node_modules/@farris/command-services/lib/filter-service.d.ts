/**
 * 过滤条件服务
 * @scope FormModule
 */
declare class FilterService {
    /**
     * 移除包含特殊值的过滤条件
     * @todo：假定filters为字符串，待设计时支持数组参数后一起调整
     */
    stripFiltersWithSpecialValue(filters: string, specialValues: string): string;
    /**
     * 获取过滤条件的值
     * @todo：没有Filter接口定义，目前产品部大小写不一致。
     */
    private getFilterValue;
}
export { FilterService };
