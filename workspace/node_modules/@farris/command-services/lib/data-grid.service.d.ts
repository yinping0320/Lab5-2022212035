import { Service } from "./service";
export declare class DataGridService extends Service {
    /**
     * 清空所有勾选行
     * @description 取消勾选当前表单所有勾选行
     */
    clearChecks(): void;
    /**
     * 取消勾选删除的行
     * @param ids ids
     * @returns
     * @description 取消勾选当前绑定路径下指定数据，清空下级表格中所有勾选行，仅供删除场景使用
     */
    uncheckDeletedRows(ids: any): void;
    /**
     * 取消勾选行
     * @param ids ids
     * @returns
     */
    uncheckRows(ids: any): void;
    /**
     * 根据命令上下文获取当前命令所在组件的表格实例
     * @param commandContext 命令上下文
     * @returns
     */
    private getFormGridComponents;
    private getGridComponentByFrameContexts;
}
