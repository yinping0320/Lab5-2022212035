import { Injector } from "@angular/core";
import { AppContext, Entity, NgFormControl, Repository } from "@farris/devkit";
export declare class FormService {
    private injector;
    private appContext;
    private repository;
    constructor(injector: Injector, appContext: AppContext, repository: Repository<Entity>);
    /**
     * 根据字段路径获取该字段在卡片或表格中的索引
     * @param source ngFormControls或any[][]
     * @param propertyPath 字段除bindingPath之外的完整路径
     * @returns
     */
    getIndex(source: {
        [propertyName: string]: NgFormControl;
    } | any[], propertyPath: string): number;
}
