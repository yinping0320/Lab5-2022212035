import { Injector } from "@angular/core";
import { FrameContext } from "@farris/devkit";
import { AttentionService } from '@farris/ui-tooltip';
export declare class FormAttentionService {
    private injector;
    private frameContext;
    private attentionService;
    constructor(injector: Injector, frameContext: FrameContext, attentionService: AttentionService);
    /**
     * 闪烁按钮
     * @param ids id字符串或数组
     * @param duration 时间间隔
     * @returns
     */
    blink(ids: string | string[], duration?: any): import("rxjs").Observable<never>;
}
