import { FrameContext } from '@farris/devkit';
/**
 * 实体遍历服务
 * @todo
 * 实体层没有封装相关方法，临时通过BindingData来获取数据
 */
declare class EntityTraversingService {
    private frameContext;
    private readonly bindingData;
    constructor(frameContext: FrameContext);
    /**
     * 获取值
     */
    getPropValue(path: string[]): any;
    /**
     * 获取实体数据
     */
    getEntityData(path: string[]): any;
    /**
     * 获取实体列表数据
     */
    getEntityListData(path: string[]): any[];
}
export { EntityTraversingService };
