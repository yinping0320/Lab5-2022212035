import { HttpErrorResponse } from '@angular/common/http';
import { FormMessageService } from '../form-message.service';
import { LanguageService } from '../languag.service';
/**
 * 表单错误提示服务
 * @description 统一处理前后端异常
 */
declare class FormErrorService {
    private msgService;
    private languageService;
    constructor(msgService: FormMessageService, languageService: LanguageService);
    /**
     * 异常路由方法
     */
    exception(message: string, error: any): void;
    /**
     * http请求异常处理器
     */
    httpErrorHandler(httpError: HttpErrorResponse): void;
    private commonErrorHandler;
    /**
     * httpErrorResponse like
     * @param error 异常
     */
    private isHttpErrorResponse;
}
export { FormErrorService };
