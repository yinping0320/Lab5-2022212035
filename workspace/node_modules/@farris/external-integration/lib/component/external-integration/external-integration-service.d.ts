import { HttpClient } from "@angular/common/http";
import { EventEmitter } from "@angular/core";
import { VariableParseService, ViewModel } from "@farris/devkit";
export declare class ExternalIntegrationService {
    private http;
    private variableParseService;
    private vm;
    constructor(http: HttpClient, variableParseService: VariableParseService, vm: ViewModel);
    sapData: EventEmitter<{}>;
    sapUrl: string;
    /**
     * 调用外部服务
     */
    queryExtIntegrationService(externalParams: any, serviceCode: any): void;
    /**
     * 组装sap api入参
     */
    private assembleSapRequest;
}
