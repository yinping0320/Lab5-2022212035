import { EventEmitter, AfterViewInit, ElementRef, Injector, NgZone, OnChanges, OnDestroy, OnInit, Renderer2, SimpleChanges } from '@angular/core';
import { DialogComponent } from '@farris/ui-dialog';
export declare class LookupAdvancedLayoutComponent implements OnInit, OnDestroy, OnChanges, AfterViewInit {
    elRef: ElementRef;
    private injector;
    private zone;
    private render2;
    title: string;
    activeTab: string;
    enableFav: boolean;
    dataTab: string;
    favTab: string;
    tabChanged: EventEmitter<{}>;
    close: EventEmitter<{}>;
    headerRel: ElementRef;
    activetabRef: ElementRef;
    dialogRef: DialogComponent;
    constructor(elRef: ElementRef, injector: Injector, zone: NgZone, render2: Renderer2);
    ngOnInit(): void;
    ngOnDestroy(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngAfterViewInit(): void;
    private setCurrentTab;
    changeTabs($event: any, tabstr: any): void;
    closeDialog($event: any): void;
    changeDialogSize($event: MouseEvent): void;
}
