import { GspAppModel } from './../models/gsp-app.model';
import { OnInit, Injector, OnChanges, SimpleChanges, NgZone } from '@angular/core';
import { HttpService } from '@farris/ide-devkit';
import { MessagerService } from '@farris/ui-messager';
import { LocaleService } from '@farris/ui-locale';
export declare class GspAppManagerComponent implements OnInit, OnChanges {
    private http;
    private injector;
    private ngZone;
    private messagerSer;
    serverIp: string;
    path: string;
    /** 所有app数据 */
    allAppTreeData: any[];
    appIdField: string;
    showMask: boolean;
    appModel: GspAppModel;
    localeSer: LocaleService;
    private readonly APP_URI;
    constructor(http: HttpService, injector: Injector, ngZone: NgZone, messagerSer: MessagerService);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    validate(): boolean;
    treeNodeClicked(e: any): void;
    uuid(): string;
    addInvoke(): void;
    removeInvoke(id: any): void;
    private initAllAppTree;
    private convert2TreeNodes;
    private createNewAppInvok;
}
