import { TreeTableComponent } from './../treetable.component';
import { OnInit, Injector, ChangeDetectorRef, EventEmitter, OnChanges, SimpleChanges, ElementRef } from '@angular/core';
import { BehaviorSubject } from 'rxjs';
export declare class TreetableNodesPagerComponent implements OnInit, OnChanges {
    private tt;
    private cd;
    private injector;
    disabled: boolean;
    context: any;
    pagerListEl: ElementRef<any>;
    pageChange: EventEmitter<{}>;
    showTotal: boolean;
    showPager: BehaviorSubject<boolean>;
    pagerInfo: any;
    readonly pagers: any[];
    constructor(tt: TreeTableComponent, cd: ChangeDetectorRef, injector: Injector);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    private setPagerInfo;
    getPagerCount(): number;
    isFirstPager(): boolean;
    isLastPager(): boolean;
    /**
     * Go to the previous page
     */
    previous($event: any): void;
    /**
     * Go to the next page
     */
    next($event: any): void;
    setCurrent(page: number, pageSize?: number, $event?: any): void;
    pagerChanged(newPageIndex: number): void;
}
