import { PipeTransform, ElementRef } from '@angular/core';
import { DomSanitizer, SafeHtml } from '@angular/platform-browser';
export declare class HighlightPipe implements PipeTransform {
    sanitizer: DomSanitizer;
    private el;
    constructor(sanitizer: DomSanitizer, el: ElementRef);
    transform(text: string, keyword: any, field?: string, findField?: string): SafeHtml;
    parseHTML(html: any, context: any): any;
    private replaceContent;
}
