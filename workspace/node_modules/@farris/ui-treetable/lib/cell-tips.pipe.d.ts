import { CommonUtils } from '@farris/ui-common';
import { PipeTransform } from '@angular/core';
export declare class CellTipsPipe implements PipeTransform {
    private commonUtils;
    constructor(commonUtils: CommonUtils);
    transform(col: any, ...args: any[]): any;
    private getCellTooltipContent;
}
