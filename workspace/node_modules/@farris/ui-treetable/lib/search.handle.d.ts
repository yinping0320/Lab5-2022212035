import { TreeTableComponent } from './treetable.component';
import { RowNode, TreeNode } from './types/treenode';
export declare class SearchHandle {
    private ttInstance;
    allNodes: any[];
    constructor(ttInstance: TreeTableComponent);
    research(from?: 'client' | 'server'): void;
    search(field: string, value: string, from?: 'client' | 'server'): any;
    private _refreshTree;
    private _updateSerializedValues;
    findParent(item: TreeNode, allNodes: any[]): any[];
    private searchExpression;
    private getValue;
    getFindTextTotal(field: string, value: string, nodes: RowNode[]): number;
    searchOnClient(field: string, value: string, nodes: RowNode[]): RowNode[];
    searchWithConditions(conditons: {
        [key: string]: any;
    }): void;
    findParents(rowNodes: any, allNodes: any): any[];
    private resetTreeData;
    private searchOnServer;
}
