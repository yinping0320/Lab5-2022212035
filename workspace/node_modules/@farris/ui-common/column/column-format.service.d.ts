import { NumberHelperService } from '@farris/ui-common/number';
import { DateTimeHelperService } from '@farris/ui-common/date';
import { DataFormatter } from './column-format.options';
import { FilterCondition, SortCondition } from '@farris/ui-common/types';
export declare class ColumnFormatService {
    datehelper: DateTimeHelperService;
    numberhelper: NumberHelperService;
    constructor(datehelper: DateTimeHelperService, numberhelper: NumberHelperService);
    format(value: any, data?: any, formatter?: DataFormatter, context?: any): any;
    private dateTimeFormat;
    private numberFormat;
    private enumFormat;
    private imageFormat;
    private booleanFormat;
    private convertCompare;
    buildSortString(sorts: SortCondition[]): string;
    buildSqlWhere(conditions: FilterCondition[]): string;
}
