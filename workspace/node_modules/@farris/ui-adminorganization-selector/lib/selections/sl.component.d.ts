import { EventEmitter, OnInit, AfterViewInit, SimpleChanges, OnChanges, ChangeDetectorRef, Injector, NgZone, OnDestroy } from '@angular/core';
import { ApiService } from '../../utils/api.service';
import { FavoritesTool } from '../../utils/favorites.tool';
import { StateMgrTool } from '../../utils/state.mgr.tool';
import { Subscription } from 'rxjs';
import { LocaleService } from '@farris/ui-locale';
export declare class SelectionComponent implements OnInit, AfterViewInit, OnChanges, OnDestroy {
    private apiService;
    private cdr;
    private favoritesTool;
    private injector;
    private ngZone;
    private stateMgr;
    primaryField: string;
    enableGroup: boolean;
    openedChange: EventEmitter<boolean>;
    datas: any[];
    items: any[];
    pageIndex: number;
    pageSize: number;
    moreText: string;
    selectionsSub: Subscription;
    localeService: LocaleService;
    moreDataText: string;
    noneDataText: string;
    constructor(apiService: ApiService, cdr: ChangeDetectorRef, favoritesTool: FavoritesTool, injector: Injector, ngZone: NgZone, stateMgr: StateMgrTool);
    ngOnDestroy(): void;
    ngOnInit(): void;
    ngAfterViewInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    openGroupDialog(): void;
    trackByFn(index: any, item: any): any;
    loadMore(): void;
    removeItem(e: any, data: any, i: any): void;
}
