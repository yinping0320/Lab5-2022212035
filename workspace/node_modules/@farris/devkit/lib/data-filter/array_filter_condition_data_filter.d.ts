import { DataFilter } from './data_filter';
import { FilterCondition } from './types';
import { List } from 'immutable';
import { BindingObject } from '../binding-data';
export declare class ArrayFilterConditionDataFilter extends DataFilter {
    constructor();
    filter(data: List<BindingObject>, conditions: FilterCondition[]): import("immutable").Iterable<number, BindingObject>;
    validateRowData(bindingObject: BindingObject, conditions: any): boolean;
    private checkRowDataWithCondition;
}
