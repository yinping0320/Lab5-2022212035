import { DataFilter } from './data_filter';
import { ObjectFilterCondition } from './types';
import { BindingObject } from '../binding-data';
import { List } from 'immutable';
export declare class ObjectFilterConditionDataFilter extends DataFilter {
    constructor();
    filter(data: List<BindingObject>, conditions: ObjectFilterCondition): import("immutable").Iterable<number, BindingObject>;
    validateRowData(bindingObject: BindingObject, conditions: ObjectFilterCondition): boolean;
    private checkAllFieldInRowData;
    private getRelationResult;
    private checkAllFieldResult;
}
