import { Modification } from '../changeset/types';
import { Dynamic } from './types';
import { Entity } from './entity';
/**
 * 支持动态字段集合的动态实体
 */
export declare class DynamicEntity extends Entity implements Dynamic {
    /**
     * 是否是嵌套的动态实体
     */
    readonly IsNested: boolean;
    /**
     * @param data JSON数据
     */
    constructor(data: any);
    loadDynamicData(dynamicData: any): void;
    /**
     * 初始化动态数据
     * @param dynamicData 动态数据
     */
    private initializeDynamicField;
    private createDynamicEntityFromJsonData;
    /**
     * 将变更记录保存至变更集中
     * @param value 变更记录
     * @todo
     * 1、preValue的处理有问题，下级传递上来的变更这样可以，根DyanmicaEntity上的，data已经发生变化，prevalue和value是一样了；
     * 2、当value是下级冒泡上来的，需要根据value去更新当前层级的data，该逻辑不应该放在setChagnes，待修改。
     */
    setChanges(value: Modification): void;
    /**
     * toJSON
     */
    toJSON(): any;
}
