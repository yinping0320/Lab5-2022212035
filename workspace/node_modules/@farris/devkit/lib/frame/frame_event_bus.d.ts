import { Subject } from 'rxjs';
import { FrameEvent } from './frame_event';
/**
 * 框架事件总线
 */
declare class FrameEventBus {
    /**
     * @todo 暂不实现对订阅的注销
     */
    private subscriptions;
    /**
     * 事件流
     */
    events: Subject<FrameEvent>;
    /**
     * 注册事件处理
     */
    on(eventType: string, eventHandler: any, frameId: string): void;
    /**
     * 取消事件处理
     */
    off(eventType: string, frameId: string): void;
    /**
     * 触发事件
     */
    trigger(eventType: string, eventData?: any, frameIds?: string[]): void;
}
export { FrameEventBus };
