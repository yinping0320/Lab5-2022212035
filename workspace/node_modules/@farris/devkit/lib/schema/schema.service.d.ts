import { IFormViewModel } from './form-metadata';
import { Schema, SchemaEntityField } from './schema';
export declare class SchemaService {
    constructor();
    /**
     * 根据字段标识数组获取指定字段集合
     * @param fieldIds 字段标识数组
     * @param schema 实体结构
     * @param viewModel 视图模型
     * @returns 已Map形式存储的指定字段字典
     */
    getFieldsByIds(fieldIds: string[], schema: Schema, viewModel: IFormViewModel): Map<string, SchemaEntityField>;
    /**
     * 将字段列表打平为字典结构
     * @param fields 字段属性
     * @param fieldsMap 缺省参数，用来存储字段的Map对象
     * @returns 以Map形式存储的字段字段
     */
    private flattenFields;
    /**
     * 获取表字段列表
     * @param entities 实体对象集合
     * @param entityPath 指定实体路径，根实体路径为'/'
     */
    private getEntityFields;
}
