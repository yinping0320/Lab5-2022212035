import { Injector } from "@angular/core";
import { Observable } from "rxjs";
import { Expression } from './types';
import { IFormExpressionManifestService } from "../manifest/index";
import { Translate } from "../i18n/index";
export declare class ExpressionRegistry {
    private injector;
    private formExpressionManifestService;
    private translate;
    private _expressions;
    constructor(injector: Injector, formExpressionManifestService: IFormExpressionManifestService, translate: Translate);
    /**
     * 加载表达式文件
     */
    load(): Observable<Expression.ExpressionObject[]>;
    /**
     * 获取所有表达式
     */
    readonly expressions: Observable<Expression.ExpressionObject[]>;
    /**
     * 根据表达式id获取对应的表达式对象
     * @param id 表达式id
     * @returns
     */
    getExpressionById(id: string): Expression.ExpressionObject;
    private getExpressionMessage;
    private transform;
    private cleanSpecialCharacters;
    private replaceAll;
}
