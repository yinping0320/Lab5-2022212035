import { Expression } from '../expression/index';
export declare class ExpressionExecutor implements Expression.IExecutor {
    /**
     * 编译执行
     * @param expression
     * @param context
     * @returns
     */
    compile(expressionObject: Expression.ExpressionObject, context?: {
        [prop: string]: any;
    }): any;
    /**
     * 解析
     * @param expression
     * @param context
     * @returns
     */
    eval(expression: string, context?: {
        [prop: string]: any;
    }): any;
    /**
     * 构造表达式上下文
     * @param context
     * @returns
     */
    private buildContext;
}
