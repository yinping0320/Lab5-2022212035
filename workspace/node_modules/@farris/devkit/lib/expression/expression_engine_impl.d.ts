import { Injector } from '@angular/core';
import { ExpressionRegistry } from './expression_registry';
import { ExpressionEventEmitter } from './expression_event_emitter';
import { ResolverRegistry, ResolveService } from '../resolver/index';
import { EventHandlerRegistry } from '../event-handler/index';
export declare class ExpressionEngineImpl {
    private injector;
    private expressionRegistry;
    private expressionEventEmitter;
    private resolverRegistry;
    private eventHandlerRegistry;
    private resolveService;
    private expressionObjects;
    constructor(injector: Injector, expressionRegistry: ExpressionRegistry, expressionEventEmitter: ExpressionEventEmitter, resolverRegistry: ResolverRegistry, eventHandlerRegistry: EventHandlerRegistry, resolveService: ResolveService);
    private attachEvent;
    /**
     * 解析表达式依赖
     * @returns
     */
    private resolveDependency;
    /**
     * 获取表达式事件处理器
     * @param event event
     * @returns
     */
    private getEventHandler;
}
