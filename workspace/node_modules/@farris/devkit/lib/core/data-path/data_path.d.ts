import { DataPathNode, DataPathNodeType } from './data_path_node';
/**
 * 变更路径（简单双向列表）
 */
declare class DataPath {
    /**
     * 头节点
     */
    head: DataPathNode;
    /**
     * 长度
     */
    length: number;
    /**
     * 构造函数
     */
    constructor();
    /**
     * 添加一个节点到头部
     */
    unshift(type: DataPathNodeType, data: any): void;
    /**
     * 在链表最后追加一个节点
     */
    push(type: DataPathNodeType, data: any): void;
    /**
     * 获取链表尾部节点
     */
    getTail(): DataPathNode;
    /**
     * 转换为数组格式
     */
    toArray(): any[];
    /**
     * 转换为字符串格式
     */
    toString(): string;
    /**
     * 拷贝
     */
    clone(): DataPath;
}
export { DataPath };
