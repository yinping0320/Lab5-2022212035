import { Injector } from '@angular/core';
import { BindingData } from '../binding-data/index';
import { Entity } from '../entity/index';
import { Expression } from '../expression/index';
import { Repository } from '../repository/index';
/**
 * 依赖副作用器
 * @description 当表达式计算结果为true时，清空字段值
 * 当value==true且属性当前值不是null时，设置属性值为null
 */
export declare class DependencyEffector implements Expression.Effector {
    private injector;
    private repository;
    private namespace;
    private bindingData;
    ns: string;
    constructor(injector: Injector, repository: Repository<Entity>, namespace: any, bindingData: BindingData);
    effect(path: string, value: boolean, options: Expression.EffectOptions): void;
}
