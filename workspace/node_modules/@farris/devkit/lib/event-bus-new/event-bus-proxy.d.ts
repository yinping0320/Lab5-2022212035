import { IEmitable } from './types';
import { Type } from '@angular/core';
export declare class EventBusProxy {
    private eventBus;
    private hostType;
    private eventTokenValueProvider;
    constructor(eventBus: IEmitable, hostType: Type<any>, eventTokenValueProvider: () => any);
    post(eventName: string, data: any): void;
}
