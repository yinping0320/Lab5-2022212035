import { ICacheKeyCompare, ICacheObject, ICacheProvider, Key } from "../types";
export declare class MemoryCacheProvider<T> implements ICacheProvider<T> {
    protected store: Array<ICacheObject<T>>;
    protected cacheKeyCompare: ICacheKeyCompare;
    constructor(cacheKeyCompare?: ICacheKeyCompare);
    has(key: Key): boolean;
    length(): number;
    set(value: ICacheObject<T>): void;
    get(key: Key): ICacheObject<T>;
    delete(key: Key): void;
    clear(): void;
    keys(): IterableIterator<Key>;
    values(): IterableIterator<ICacheObject<T>>;
}
