import { ICacheObject, ICacheProvider, Key } from "../types";
/**
 * 默认缓存
 * @description
 * 默认缓存使用Map作为缓存容器，存储速度快。但不支持自定义的key对比，对于简单类型的key使用值对比，对于引用类型的key，对比的是引用的地址。
 */
export declare class DefaultCacheProvider<T> implements ICacheProvider<T> {
    private buffer;
    constructor();
    has(key: Key): boolean;
    length(): number;
    set(value: ICacheObject<T>): void;
    get(key: Key): ICacheObject<T>;
    delete(key: Key): void;
    clear(): void;
    keys(): IterableIterator<Key>;
    values(): IterableIterator<ICacheObject<T>>;
}
