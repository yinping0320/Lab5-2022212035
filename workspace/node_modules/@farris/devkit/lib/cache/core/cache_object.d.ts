import { ICacheObject, Key, TTL } from "../types";
export declare class CacheObject<T> implements ICacheObject<T> {
    key: Key;
    content: T;
    ttl: TTL;
    createAt: Date;
    constructor(key: Key, content: T, ttl: TTL);
    isExpired(): boolean;
}
