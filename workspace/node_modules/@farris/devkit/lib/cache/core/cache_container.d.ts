import { ICacheProvider, Key, TTL } from "../types";
export declare class CacheContainer<T> {
    protected provider: ICacheProvider<T>;
    constructor(provider: ICacheProvider<T>);
    get(key: Key): T;
    set(key: Key, value: T, ttl?: TTL): void;
    private isCacheObjectExpired;
}
