import { AppContext } from './app_context';
export declare class ComponentManager {
    private appContext;
    constructor(appContext: AppContext);
    get(paths: string[]): any;
    /**
     * 获取指定frame下所有的组件
     * @param frameId
     * @returns
     */
    getComponentsByFrameId(frameId: string): Map<string, any> | undefined;
}
