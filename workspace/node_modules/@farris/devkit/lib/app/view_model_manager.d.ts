import { IDisposable } from '../core';
import { ViewModel } from '../view-model';
/**
 * ViewModel管理类
 */
declare class ViewModelManager implements IDisposable {
    /**
     * ViewModelMap
     */
    private viewModelMap;
    /**
     * 构造函数
     */
    constructor();
    /**
     * 获取ViewModelMap
     */
    getViewModelMap(): Map<string, ViewModel>;
    /**
     * 根据name获取ViewModel
     * @return 找不到时返回undefined
     */
    getViewModelByName(name: string): ViewModel;
    /**
     * 初始化全局的viewModel
     */
    register(name: string, viewModel: ViewModel): void;
    /**
     * 是否Repository已经存在
     */
    exsit(name: string): boolean;
    dispose(): void;
}
export { ViewModelManager };
