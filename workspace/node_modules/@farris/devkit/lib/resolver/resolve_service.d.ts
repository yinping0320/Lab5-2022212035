import { Injector } from "@angular/core";
import { ResolverRegistry } from "./resolver_registry";
export declare class ResolveService {
    private injector;
    private resolverRegistry;
    constructor(injector: Injector, resolverRegistry: ResolverRegistry);
    resolve(expression: string): string[];
}
