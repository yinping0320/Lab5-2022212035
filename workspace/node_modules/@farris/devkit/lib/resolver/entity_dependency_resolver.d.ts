import { Repository } from '../repository/index';
import { IResolver } from './types';
export declare class EntityDependencyResolver implements IResolver {
    private repository;
    private entityTypeInfo;
    constructor(repository: Repository<any>);
    /**
     * 解析用户表达式中的实体依赖
     * @param expr 用户配置的完整表达式
     * @returns
     */
    resolve(expr: string): string[];
    /**
     * 获取合法的实体属性表达式
     * @param entityPropertyExpression 实体属性表达式
     * @returns
     */
    private getValidEntityPropertyExpression;
    /**
     * 获取所有实体依赖
     * @param expr 表达式字符串
     * @returns
     */
    private getEntityDependency;
}
