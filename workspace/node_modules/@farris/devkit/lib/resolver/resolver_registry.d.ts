import { Injector } from '@angular/core';
import { IResolver } from './types';
export declare class ResolverRegistry {
    private injector;
    resolvers: Array<IResolver>;
    constructor(injector: Injector, resolvers: Array<IResolver>);
}
