import { Injector } from '@angular/core';
import { Modification } from '../changeset/index';
import { Entity } from '../entity';
import { Repository } from '../repository/index';
import { ChangeListener } from './change_listener';
declare class RepositoryChangeListener extends ChangeListener {
    private injector;
    private repository;
    private namespace;
    private bindingData;
    constructor(injector: Injector, repository: Repository<Entity>, namespace: any);
    private registerEvent;
    /**
     * 构建事件路径参数
     * @param event event
     * @description 构建完之后的路径类似[id,prop] or [id,从表名s,从表当前行id,从表属性] or [id,udt,udt_prop]
     * @returns
     */
    buildEventPath(event: Modification): string[];
    private convertEventType;
}
export { RepositoryChangeListener };
