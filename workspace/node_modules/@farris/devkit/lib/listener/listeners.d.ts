import { Observable } from 'rxjs';
import { ListenerRegistry } from './listener_registry';
import { Expression } from '../expression/index';
declare type EventArgs = Expression.EventArgs;
/**
 * 监听器
 * @description 监听bindingList、UIState变更
 */
declare class Listeners {
    private registry;
    private subject;
    readonly onEvent: Observable<EventArgs>;
    constructor(registry: ListenerRegistry);
    private regist;
}
export { Listeners };
