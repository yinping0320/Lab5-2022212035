import { Injector } from '@angular/core';
import { BindingData, Change } from '../binding-data/index';
import { ChangeListener } from './change_listener';
/**
 * 监听bindingList变更
 * @description 主要用于监听行切换等事件
 */
declare class BindingDataChangeListener extends ChangeListener {
    private injector;
    private bindingData;
    private namespace;
    private repository;
    constructor(injector: Injector, bindingData: BindingData, namespace: any);
    /**
     * 注册值变化事件
     */
    private registerEvent;
    buildEventPath(change: Change): string[];
}
export { BindingDataChangeListener };
