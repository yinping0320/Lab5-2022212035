import { Injector } from '@angular/core';
import { UIState, UIStateObservableParam } from '../ui-state/index';
import { ChangeListener } from './change_listener';
import { BindingData } from '../binding-data/index';
/**
 * 监听UIState变更
 */
declare class UIStateChangeListener extends ChangeListener {
    private injector;
    private uiState;
    private namespace;
    private frameId;
    private bindingData;
    constructor(injector: Injector, uiState: UIState, namespace: any, frameId: string, bindingData: BindingData);
    buildEventPath(change: UIStateObservableParam): string[];
    private registerEvent;
}
export { UIStateChangeListener };
