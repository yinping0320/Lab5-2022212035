/**
 * 使用方法：
 * [mapFields]="{ id: 'user.userId', name: 'user.userName' }"
 * key 为帮助上的字段， value 为 表单中的字段名
 * 帮助上的同一个字段可以映射到表单中的多个字段中，{ ... id: 'user.userid, user.addusid'}
 * 多字段以逗号隔开
 *
 */
import { OnInit, OnDestroy, AfterViewInit, Injector } from '@angular/core';
import { ViewModel } from '@farris/devkit';
import { OrganizationSelectorComponent } from '@farris/ui-organization-selector';
export declare class OrganizationSelectorDataMappingDirective implements OnInit, AfterViewInit, OnDestroy {
    private vm;
    private targetComponent;
    private injector;
    mapFields: any;
    constructor(vm: ViewModel, targetComponent: OrganizationSelectorComponent, injector: Injector);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    ngOnDestroy(): void;
    private getFieldBinding;
    tagRemovedMappingData(index: any, mapFields: any): void;
    /**
     *
     * @param helpData 清空时，值为null
     * @param mapFields 格式形如：{id: "assoField.assoField", code: "assoField.assoField_Code", name: "assoField.assoField_Name"}
     */
    mappingData(helpData: any, mapFields: any): void;
    private getValue;
    private getBindingPathArray;
}
