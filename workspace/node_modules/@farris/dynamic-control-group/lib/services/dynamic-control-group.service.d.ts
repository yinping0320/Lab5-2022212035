import { Injector } from '@angular/core';
import { IDynamicBaseControl } from '../interfaces/dynamic-control.interface';
import { DynamiControlGroupHttpInterface } from '../interfaces/dynamic-control-group-http.interface';
import { Observable } from 'rxjs';
export declare class DynamicControlGroupService {
    private injector;
    httpService: DynamiControlGroupHttpInterface;
    constructor(injector: Injector);
    getFormControlsByUri(uri: string): Observable<IDynamicBaseControl[]>;
    toFormValue(formControls: Array<IDynamicBaseControl>, initialData: any): any;
}
