import { OnInit } from "@angular/core";
import { FormGroup } from "@angular/forms";
import { CommonUtils } from '@farris/ui-common';
import { IDyanmicHelpControl } from "../../interfaces/dynamic-control.interface";
export declare class HelpComponent implements OnInit {
    private utils;
    className: string;
    control: IDyanmicHelpControl;
    form: FormGroup;
    options: any;
    constructor(utils: CommonUtils);
    ngOnInit(): void;
    /**
     * @param helpData 选中的数据，如果是多选，是数组；如果是单选，是普通对象
     * @param mapFields
     * @param bindingData 帮助所在form的value对象
    */
    mapping: (helpData: any, mapFields: any) => void;
}
