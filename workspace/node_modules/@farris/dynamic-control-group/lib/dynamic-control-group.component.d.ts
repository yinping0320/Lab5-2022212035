import { OnInit, AfterViewChecked, ChangeDetectorRef, OnChanges, SimpleChanges } from '@angular/core';
import { IDynamicBaseControl } from './interfaces/dynamic-control.interface';
import { FormGroup } from '@angular/forms';
import { DynamicControlGroupService } from './services/dynamic-control-group.service';
export declare class DynamicControlGroupComponent implements OnInit, OnChanges, AfterViewChecked {
    private controlGroupService;
    private cdRef;
    className: string;
    contents: Array<IDynamicBaseControl>;
    form: FormGroup;
    extraClassNames: string;
    isControlInline: boolean;
    isEmbedded: boolean;
    formData: any;
    uri: string;
    isGrouped: boolean;
    controlEntries: Array<Array<any>>;
    constructor(controlGroupService: DynamicControlGroupService, cdRef: ChangeDetectorRef);
    ngOnChanges(changes: SimpleChanges): void;
    ngOnInit(): void;
    initFormAndControlEntries(): void;
    groupControls(contents: Array<IDynamicBaseControl>): Map<string, Array<IDynamicBaseControl>>;
    initStyles(): string;
    ngAfterViewChecked(): void;
}
