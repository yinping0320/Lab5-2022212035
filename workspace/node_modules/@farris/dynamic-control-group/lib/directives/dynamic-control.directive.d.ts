import { ComponentFactoryResolver, ComponentRef, Injector, OnInit, ViewContainerRef } from "@angular/core";
import { IDynamicBaseControl } from "../interfaces/dynamic-control.interface";
import { FormGroup } from '@angular/forms';
export declare class DynamicFormControlDirective implements OnInit {
    private resolver;
    private container;
    private injector;
    data: IDynamicBaseControl;
    formGroup: FormGroup;
    componentRef: ComponentRef<any>;
    constructor(resolver: ComponentFactoryResolver, container: ViewContainerRef, injector: Injector);
    ngOnInit(): void;
    addToForm(control: IDynamicBaseControl, originalForm: FormGroup): FormGroup;
}
