import { AfterViewInit, ChangeDetectorRef, ElementRef, EventEmitter, Injector, OnChanges, OnDestroy, OnInit, Renderer2, SimpleChanges } from '@angular/core';
import { OverLayHiddenService } from '@farris/ui-common';
import { BehaviorSubject } from 'rxjs';
import { UtilService } from '../services/farris-datepicker.util.service';
import { MultiSelectedOrderBy } from '../interfaces/my-options.interface';
export declare class MultiSelectDateInputComponent implements OnInit, OnChanges, AfterViewInit, OnDestroy {
    private injector;
    private render;
    private el;
    overLayService: OverLayHiddenService;
    protected cd: ChangeDetectorRef;
    private utilService;
    dates: string;
    separator: string;
    placeholder: string;
    editable: boolean;
    orderBy: MultiSelectedOrderBy;
    clickHandle: EventEmitter<{}>;
    remove: EventEmitter<{}>;
    shadowBox: ElementRef;
    container: ElementRef;
    _dateItems: any[];
    hiddenItemsCount$: BehaviorSubject<number>;
    commonUtils: any;
    constructor(injector: Injector, render: Renderer2, el: ElementRef, overLayService: OverLayHiddenService, cd: ChangeDetectorRef, utilService: UtilService);
    ngOnInit(): void;
    ngOnDestroy(): void;
    ngOnChanges(changes: SimpleChanges): void;
    private initDateItems;
    ngAfterViewInit(): void;
    onClick($event: MouseEvent): void;
    onRemove($event: MouseEvent, txt: any, index: any): void;
    onResized($event: any): void;
    private setHiddenCount;
    updateShadowBoxPosition({ top, left, width }?: {
        top: any;
        left: any;
        width: any;
    }): void;
    private moveShadowToBody;
    toggleShadowBox($event: any, show?: boolean): void;
}
