import { ElementRef, EventEmitter, Injector, OnInit } from '@angular/core';
export declare class DynamicDateInputComponent implements OnInit {
    private inject;
    readonly: boolean;
    disabled: boolean;
    value: string;
    text: string;
    isFocus: boolean;
    placeholder: string;
    showClear: boolean;
    inputClick: EventEmitter<{}>;
    tabKeydown: EventEmitter<{}>;
    clear: EventEmitter<{}>;
    clearIcon: ElementRef;
    constructor(inject: Injector);
    ngOnInit(): void;
    onMouseEnter(event: any): void;
    onMouseLeave(event: any): void;
}
