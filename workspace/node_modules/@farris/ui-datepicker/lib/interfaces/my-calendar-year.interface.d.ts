import { IMyDate } from './my-date.interface';
export interface IMyCalendarYear {
    year: number;
    currYear: boolean;
    selected?: boolean;
    disabled: boolean;
    range?: boolean;
    yearObj?: IMyDate;
}
