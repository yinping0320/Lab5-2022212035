import { IMySelectorPosition, IMyOptions } from '../interfaces/public-api';
import { ComponentRef } from '@angular/core';
export declare class DatePickerService {
    opts: any;
    utilService: any;
    private _mouseWheelHandle;
    private _mouseScrollHandle;
    constructor(opts: any);
    static parseOptions(opts: IMyOptions, defaultOpts: any): IMyOptions;
    validate(value: string): boolean;
    registerScrollEvent(fn: any): void;
    removeMouseEvent(): void;
    appendSelector(elem: any): void;
    getSelectorPosition(elem: any, calendarRef?: ComponentRef<any>): IMySelectorPosition;
    private getSelectorDimension;
}
