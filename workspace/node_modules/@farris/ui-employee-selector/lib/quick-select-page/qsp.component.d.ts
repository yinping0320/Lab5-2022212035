import { EventEmitter, OnInit, AfterViewInit, ChangeDetectorRef } from '@angular/core';
import { NotifyService } from '@farris/ui-notify';
import { FavoritesTool } from '../../utils/favorites.tool';
import { PanelMode } from '../../utils/user.type';
import { ApiLocalService } from '../../utils/api-service';
export declare class QuickSelectComponent implements OnInit, AfterViewInit {
    private apiService;
    private notifyService;
    private favoritesTool;
    private cdRef;
    idField: string;
    multiSelect: boolean;
    enableChildHierarchy: boolean;
    includeStopUser: boolean;
    personnelOrderField: string;
    includeCurrentUser: boolean;
    userTypeMode: string;
    filterId: string;
    absUserOrgUrl: string;
    quickInput: any;
    enableFavorites: boolean;
    absOrgType: string;
    busNum: string;
    originalSelections: any[];
    validElementsInfo: string;
    withPermission: boolean;
    selectionsChange: EventEmitter<any>;
    togglePanelModelChange: EventEmitter<PanelMode>;
    allChecked: boolean;
    allIndexId: string;
    noDataImg: string;
    items: any[];
    recentContactsList: any[];
    favoritesList: any[];
    searchText: string;
    private searchTextStream;
    selections: any[];
    firstLoad: boolean;
    checkeds: any[];
    showUserCode: boolean;
    constructor(apiService: ApiLocalService, notifyService: NotifyService, favoritesTool: FavoritesTool, cdRef: ChangeDetectorRef);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    private getItemsBySearch;
    private getRecentContactsList;
    togglePanelMode(showQuickMode: boolean, showAdvancedMode: boolean): void;
    toggleAll(): void;
    toggleOne(data: any): void;
    assembleItemsChecked(): void;
    assembleItemsFavorites(): void;
    /**
     * 全选后处理
     * @param dataArr 全选的值
     */
    checkAll(): void;
    /**
     * 全部取消
     * @param dataArr 取消的数据
     * @returns void
     */
    unCheckAll(): void;
    /**
     * 当前行选中
     * @param data 当前行的数据（）
     */
    selectLine(data: any): void;
    /**
     * 单选取消选中
     * @param data 取消选中行
     */
    unSelectLine(data: any): void;
    confirmSelections(): void;
    toggleFavorites(e: any, item: any): void;
    /**
     * 选中并插入当前行数据到右侧
     * @param data 当前行的数据
     * @param collection 暂存当前数据的集合
     */
    _insertOneIntoSelections(data: any): void;
    /**
     * 判断是否在内
     * @param value value
     * @param field field
     * @param array array
     * @returns 位置索引
     */
    _isInArray(value: string, field: string, array: any[]): number;
    /**
     * 翻页检查：人员展示区的勾选状态与已选人员区是否对应
     */
    toggleAllChecked(): void;
    trackByFn(index: any, item: any): any;
}
