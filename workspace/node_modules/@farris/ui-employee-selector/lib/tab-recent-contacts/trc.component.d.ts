import { EventEmitter, OnInit, AfterViewInit, Injector } from '@angular/core';
import { NotifyService } from '@farris/ui-notify';
import { FavoritesTool } from '../../utils/favorites.tool';
export declare class RecentContactsComponent implements OnInit, AfterViewInit {
    private notifyService;
    private favoritesTool;
    private injector;
    idField: string;
    /**
     * 数据源类型，默认为'System_organization'，即设置为系统数据源，可获取系统组织、系统人员
     * 设置为'Administrative_organization'就是扩展数据源 ，可获取行政组织、行政人员
     */
    absOrgType: string;
    absUserOrgUrl: string;
    busNum: string;
    searchText: any;
    searchPage: boolean;
    includeStopUser: boolean;
    includeCurrentUser: boolean;
    enableFavorites: boolean;
    validElementsInfo: string;
    multiSelect: any;
    orgId: string;
    userIds: string;
    withPermission: boolean;
    selectionsChange: EventEmitter<any>;
    recentContactsChange: EventEmitter<any>;
    private cdRef;
    allChecked: boolean;
    columns: any;
    selections: any[];
    showUserCode: boolean;
    cloneUserIds: string;
    cloneSelections: any[];
    allIndexId: string;
    noDataImg: string;
    items: any[];
    recentContactsList: any[];
    favoritesList: any[];
    constructor(notifyService: NotifyService, favoritesTool: FavoritesTool, injector: Injector);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    assembleData(): void;
    searchByCodeOrName(): void;
    restoreSelections(): void;
    toggleAll(): void;
    toggleOne(data: any): void;
    /**
     * 用于数据展示
     */
    loadRecentContactsList(): void;
    /**
     * 全选后处理
     * @param dataArr 全选的值
     */
    checkAll(): void;
    /**
     * 全部取消
     * @param dataArr 取消的数据
     * @returns void
     */
    unCheckAll(): void;
    /**
     * 当前行选中
     * @param data 当前行的数据（）
     */
    selectLine(data: any): void;
    /**
     * 单选取消选中
     * @param data 取消选中行
     */
    unSelectLine(data: any): void;
    private emitSelections;
    /**
     * 获取最近联系人列表
     */
    getRecentContactsList(): void;
    toggleFavorites(e: any, item: any): void;
    /**
     * 清空最近联系人
     */
    clearRecentContactsList(): void;
    /**
     *  取消选中
     * @param $event 取消勾选的数据
     */
    cancelSelections($event: any): void;
    /**
     * 全部取消勾选
     */
    clearCheckeds(): void;
    clearUserIds(): void;
    /**
     * 选中并插入当前行数据到右侧
     * @param data 当前行的数据
     * @param collection 暂存当前数据的集合
     */
    _insertOneIntoSelections(data: any): void;
    /**
     * 判断是否在内
     * @param value value
     * @param field field
     * @param array array
     * @returns 位置索引
     */
    _isInArray(value: string, field: string, array: any[]): number;
    /**
     * 翻页检查：人员展示区的勾选状态与已选人员区是否对应
     */
    toggleChecked(): void;
    private isAllChecked;
    trackByFn(index: any, item: any): any;
}
