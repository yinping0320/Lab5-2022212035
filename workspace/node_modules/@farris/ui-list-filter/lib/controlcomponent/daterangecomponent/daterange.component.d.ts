import { OnInit, OnChanges, EventEmitter, Injector, ChangeDetectorRef, SimpleChanges } from '@angular/core';
import { QueryCondition } from '../../entity/querycondition';
import { SolutionService } from '../../service/solution.service';
export declare class DateRangeComponent implements OnInit, OnChanges {
    private solutionService;
    private injector;
    private cdr;
    /**
     * QueryCondition类型
     */
    item: any;
    autoLabel: boolean;
    disabled: boolean;
    showTime: boolean;
    showType: string;
    searchHandle: EventEmitter<{}>;
    startTime: Date;
    endTime: Date;
    /**判断是否是在扩展区域展示 */
    extend: boolean;
    /**判断是否显示label */
    showlabel: boolean;
    rangeDate: string;
    placeholder: string;
    beginPlaceHolder: string;
    endPlaceHolder: string;
    dateFormat: string;
    returnFormat: string;
    readonly isExtend: boolean;
    readonly hidenLabel: boolean;
    /**
    * 某个字段发生值变化事件时，需要同步清空的字段，可能不是当前字段
    */
    changeFields: Array<string>;
    constructor(solutionService: SolutionService, injector: Injector, cdr: ChangeDetectorRef);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    initDate(): void;
    getDate(dateString: Date): Date;
    onDateChange(event: any): void;
    getConditionItemTitle(field: QueryCondition): string;
}
