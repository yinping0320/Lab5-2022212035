import { OnInit, Injector } from '@angular/core';
import { NgControl } from '@angular/forms';
import { ListFilterComponent } from '../list-filter.component';
export declare class ClearFieldsDirective implements OnInit {
    private ngCtrl;
    private listFilterCmp;
    private inject;
    clearFields: string;
    constructor(ngCtrl: NgControl, listFilterCmp: ListFilterComponent, inject: Injector);
    ngOnInit(): void;
}
