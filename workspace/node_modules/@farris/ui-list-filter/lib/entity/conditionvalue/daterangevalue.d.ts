import { EventEmitter } from '@angular/core';
import { IValue } from "./ivalue";
import { ControlType } from "../controltype";
export declare class DateRangeValue implements IValue {
    clear$: EventEmitter<{}>;
    startTime: string;
    endTime: string;
    constructor(value?: {
        startTime: string;
        endTime: string;
    });
    isEmpty(): boolean;
    setOriginalValue(value: {
        formatted: string;
        delimiter: string;
    }): void;
    getOriginalValue(): string;
    getControlType(): ControlType;
    clearValue(): void;
}
