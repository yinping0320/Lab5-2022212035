import { IValue } from "./ivalue";
import { ControlType } from "../controltype";
import { EventEmitter } from "@angular/core";
export declare class MonthRangeValue implements IValue {
    clear$: EventEmitter<{}>;
    startTime: string;
    endTime: string;
    constructor(value?: {
        startTime: string;
        endTime: string;
    });
    isEmpty(): boolean;
    setOriginalValue(value: {
        formatted: string;
        delimiter: string;
    }): void;
    getOriginalValue(): string;
    getControlType(): ControlType;
    clearValue(): void;
}
