import { AfterViewInit, ElementRef, OnDestroy } from '@angular/core';
import { Subscription } from 'rxjs';
import { AngularMonacoEditorConfig } from './config';
import { CodeEditorEventService } from '../service/code-editor.event.service';
export declare abstract class BaseMonacoEditor implements AfterViewInit, OnDestroy {
    private codeEditorEventService;
    private config;
    protected _editor: any;
    private _options;
    protected _windowResizeSubscription: Subscription;
    _editorComponent: ElementRef;
    onInit: any;
    options: any;
    constructor(codeEditorEventService: CodeEditorEventService, config: AngularMonacoEditorConfig);
    ngAfterViewInit(): void;
    protected abstract initMonaco(options: any): void;
    ngOnDestroy(): void;
}
