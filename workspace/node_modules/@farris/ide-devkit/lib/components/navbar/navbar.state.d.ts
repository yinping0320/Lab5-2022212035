import { EventEmitter, OnDestroy } from '@angular/core';
import { BsDropdownDirective } from '../dropdown/index';
import { DropdownItemDirective } from '../dropdown/dropdown-item.directive';
export declare class NavbarState implements OnDestroy {
    private dropdownInstances;
    private menuItems;
    private subscriptions;
    private level1Dropdown;
    private level2Dropdown;
    onItemClick: EventEmitter<string>;
    autoClose: boolean;
    constructor();
    addDropdown(dropdown: BsDropdownDirective): void;
    addMenuItem(menuItem: DropdownItemDirective): void;
    private subscribeDropdown;
    private subscribeMenuItem;
    ngOnDestroy(): void;
    hideCurrentDropdown(): void;
    hideAllDropdown(): void;
    show(dropdown: BsDropdownDirective): void;
    hide(dropdown: BsDropdownDirective): void;
    private innerShowL1Dropdown;
    private innerHideL1Dropdown;
    private innerShowL2Dropdown;
    private innerHideL2Dropdown;
}
