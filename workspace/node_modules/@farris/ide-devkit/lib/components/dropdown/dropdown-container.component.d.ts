import { OnDestroy, ChangeDetectorRef, Renderer2, ElementRef } from '@angular/core';
import { BsDropdownState } from './dropdown.state';
export declare class BsDropdownContainerComponent implements OnDestroy {
    private _state;
    private cd;
    private _renderer;
    isOpen: boolean;
    display: string;
    position: string;
    readonly direction: 'down' | 'up';
    private _subscription;
    constructor(_state: BsDropdownState, cd: ChangeDetectorRef, _renderer: Renderer2, _element: ElementRef);
    ngOnDestroy(): void;
}
