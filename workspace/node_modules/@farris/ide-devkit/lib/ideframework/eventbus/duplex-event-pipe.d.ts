import { Observable } from 'rxjs';
import { IThenable } from './types';
export declare class DuplexEventPipe {
    private listenerResults;
    private subject;
    constructor(eventName: string);
    notify(value: any): IThenable;
    listen(callback: (value: any) => Observable<boolean> | boolean | void, caller?: any): void;
}
