import { Panel } from './panel';
import { Dock } from './dock';
import { PanelLocation } from './types';
import { ViewRegistry } from './view-registry';
import { Disposable } from './event-kit';
export declare class PanelContainer {
    private viewRegistry;
    private location;
    private dock;
    private emitter;
    private subscriptions;
    panels: Panel[];
    constructor(viewRegistry: ViewRegistry, location: PanelLocation, dock?: Dock);
    destroy(): void;
    getElement(): void;
    onDidAddPanel(callback: any): Disposable;
    onDidRemovePanel(callback: any): Disposable;
    onDidDestroy(callback: any): Disposable;
    getLocation(): PanelLocation;
    isModal(): boolean;
    getPanels(): Panel[];
    addPanel(panel: Panel): Panel;
    panelForItem(item: any): Panel;
    panelDestroyed(panel: any): void;
    getPanelIndex(panel: any): number;
}
