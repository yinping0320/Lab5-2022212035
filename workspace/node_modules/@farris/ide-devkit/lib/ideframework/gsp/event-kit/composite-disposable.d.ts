export declare class CompositeDisposable {
    private disposed;
    private disposables;
    constructor(...disposables: any[]);
    dispose(): void;
    add(...disposables: any[]): void;
    remove(disposable: any): void;
    delete(disposable: any): void;
    clear(): void;
    assertDisposable(disposable: any): boolean;
}
