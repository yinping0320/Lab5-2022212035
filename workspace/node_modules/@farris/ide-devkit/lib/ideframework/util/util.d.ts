export declare class Util {
    newGuid(): string;
    /**
     * 向url添加QueryParam
     * @param url 原始url
     * @param paramName 参数名
     * @param paramValue 参数值
     * @param override 是否覆盖。为True时，如果参数已存在，将覆盖已有参数，否则放弃处理。默认为False。
     */
    setUrlParam(url: string, paramName: string, paramValue: string, override?: boolean): string;
}
