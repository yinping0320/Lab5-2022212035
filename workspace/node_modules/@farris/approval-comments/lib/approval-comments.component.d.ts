import { Injector, OnChanges, OnDestroy, OnInit, SimpleChanges } from '@angular/core';
import { ApprovalComment } from '../entity/approval-comments';
import { WorkItemLog } from '../entity/workItemLog';
import { ApiService } from '../utils/api.service';
declare enum DisplayTypes {
    Single = "single",
    Default = "default",
    Theme1 = "theme1"
}
export declare class ApprovalCommentComponent implements OnInit, OnChanges, OnDestroy {
    private apiService;
    private injector;
    title: string;
    domains: any[];
    procInstId: string;
    sortOrder: string;
    field: string;
    bindingfield: string;
    displayType: DisplayTypes;
    attachmentVisible: boolean;
    dataList: ApprovalComment[];
    filterAction: string[];
    AttachUtil: any;
    DisplayTypes: typeof DisplayTypes;
    workItemLogs: Array<Partial<WorkItemLog>>;
    constructor(apiService: ApiService, injector: Injector);
    ngOnDestroy(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnInit(): void;
    private sortPath;
    private getFieldData;
    private getApprovalComment;
    private getUrl;
    translateTask(code: any): any;
    private getFileType;
    private getDownLoadUrl;
    attachmentDownload(downloadUrl: string): void;
    /**
     * 预览附件
     */
    attachmentPreview(metadataIds: any): void;
}
export {};
