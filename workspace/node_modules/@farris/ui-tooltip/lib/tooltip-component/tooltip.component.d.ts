import { TemplateRef, ElementRef, OnInit, ViewContainerRef, Renderer2 } from '@angular/core';
import { AfterViewInit } from '@angular/core';
export declare class TooltipComponent implements OnInit, AfterViewInit {
    el: ElementRef;
    private render;
    contentViewf: ViewContainerRef;
    tooltipNode: ElementRef;
    arrowNode: ElementRef;
    innerNode: ElementRef;
    zIndex: string;
    contentIsString: boolean;
    content: string | TemplateRef<any>;
    private _tipWidth;
    /**因为表单生成没有排除factory,不能使用模板 */
    tipWidth: any;
    private _tipCls;
    tipCls: any;
    placement: string;
    private rectifyPlacement;
    hostBoundingClientRect: any;
    referenceBoundingRect: any;
    private rectifyGutter;
    nfContent: string | TemplateRef<any>;
    positionStyle: {};
    constructor(el: ElementRef, render: Renderer2);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    /**
     * 规则判断默认样式下出现滚动条，尺寸从默认变成大号
     */
    judgeWidth(): void;
    private autoRectifyDirection;
    /**
     * 判断是否超出边界
     */
    private isOverBounding;
    private containsEl;
    calculatePosition(hostPosition: any): void;
    offsetDirection(tplacement?: string): string;
}
