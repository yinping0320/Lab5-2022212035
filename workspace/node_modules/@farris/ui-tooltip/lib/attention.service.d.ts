import { OnInit, RendererFactory2, NgZone } from '@angular/core';
export interface AttentionElement {
    idOrEl: any;
    type?: string;
    class?: string;
}
export declare class AttentionService implements OnInit {
    ngZone: NgZone;
    private renderer;
    constructor(renderFac: RendererFactory2, ngZone: NgZone);
    catchAttention(els: AttentionElement[], timer?: number): void;
    ngOnInit(): void;
}
