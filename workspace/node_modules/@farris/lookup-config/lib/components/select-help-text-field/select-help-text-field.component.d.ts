import { OnInit, EventEmitter, TemplateRef, NgZone } from '@angular/core';
import { LocaleService } from '@farris/ui-locale';
import { NotifyService } from '@farris/ui-notify';
import { TreeNode, TreeTableComponent } from '@farris/ui-treetable';
export declare class SelectHelpTextFieldComponent implements OnInit {
    private notifyService;
    private ngZone;
    private localeService;
    closeModal: EventEmitter<any>;
    submitModal: EventEmitter<any>;
    value: any;
    helpId: string;
    schemaData: any;
    modalFooter: TemplateRef<any>;
    class: string;
    modalConfig: {
        title: string;
        width: number;
        height: number;
        showButtons: boolean;
        showMaxButton: boolean;
    };
    helpTreeData: TreeNode[];
    treeCols: {
        field: string;
        title: string;
    }[];
    fieldName: string;
    fieldLabel: string;
    fieldTitle: string;
    fieldError: string;
    treeTable: TreeTableComponent;
    constructor(notifyService: NotifyService, ngZone: NgZone, localeService: LocaleService);
    ngOnInit(): void;
    /**
     * 获取帮助schema字段
     * @param schema schema
     */
    getHelpFields(schema: any): void;
    /**
     * schema字段集合组装成树
     * @param fields schema字段集合
     */
    assembleFields2Tree(fields: any): any[];
    /**
     * 取消
     */
    clickCancel(): void;
    /**
     * 确定
     */
    clickConfirm(): void;
}
