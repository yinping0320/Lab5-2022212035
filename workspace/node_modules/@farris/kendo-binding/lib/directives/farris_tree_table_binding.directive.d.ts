import { OnInit, OnChanges, SimpleChanges, EventEmitter } from '@angular/core';
import { FrameContext } from '@farris/devkit';
import { TreeTableComponent } from '@farris/ui-treetable';
import { FarrisTreetableFormatService } from '../format/index';
/**
 * 树表格绑定指令
 */
declare class FarrisTreeTableBindingDirective implements OnInit, OnChanges {
    private treeTable;
    private treetableFormatSer;
    private frameContext;
    /**
     * 树存储空间
     */
    private storage;
    private renderSubject;
    private builder;
    private treeNodesBuilder;
    /**
     * 分级码信息的key
     */
    hierarchyInfoKey: string;
    rowClassName: (context: any) => any;
    cellClassName: (data: any, field: any) => any;
    /**
     * 启用节点分页
     */
    /**
     * 节点分页大小
     */
    nodePageSize: number;
    /**
     * 绑定数据
     */
    private readonly bindingData;
    /**
     * 绑定数据列表
     */
    private readonly bindingList;
    rowClass: EventEmitter<any>;
    cellClass: EventEmitter<any>;
    private _PROP;
    private listeners;
    private prop;
    /**
     * 获取当前vm
     */
    private readonly viewModel;
    /**
     * 构造函数
     */
    constructor(treeTable: TreeTableComponent, treetableFormatSer: FarrisTreetableFormatService, frameContext: FrameContext);
    /**
     * 指令初始化
     */
    ngOnInit(): void;
    /**
     * 指令输入变更
     */
    ngOnChanges(changes: SimpleChanges): void;
    private registerEvent;
    /**
     * 收集树组件实例
     */
    private setComponentRef;
    private setNodePaginationInfo;
    /**
     * 绑定数据
     */
    private bindData;
    private onTreeLoadComplete;
    private addTreeLoadCompleteListener;
    private shouldComponentUpdate;
    /**
     * 从树组件上获取选中行的id
     */
    private getSelectedIdFromTree;
    /**
     * 设置TreeTable的当前行
     */
    private setSelectedIdToTree;
    /**
     * 设置BindingList的CurrentId
     */
    private setCurrentIdToBindingList;
    private updateCheckedRows;
    /**
     * 更新勾选行数据
     */
    protected setCheckedRows(ids?: string[]): void;
    /**
     * 设置ids变量
     * @param idOrIds id or ids
     */
    protected setChecks(idOrIds: string | string[]): void;
    /**
     * 获取勾选行id数组
     * @returns
     */
    protected getChecks(): string[];
    /**
     * 获取分页信息
     */
    private getPagingInfo;
    private updatePageInfo;
    /**
     * 行切换
     */
    selectionChangeHandler(event: any): void;
    /**
     * 勾选节点发生变化
     * @param event event
     */
    checkValuesChange(event: any): void;
    /**
     * 将被展开的节点设置为当前行
     */
    expandHandler(event: any): void;
    onPageChanged(event: any): void;
    onPageSizeChanged(event: any): void;
    /**
     * 节点页码切换事件
     * @param event event
     */
    onNodePageChanged(event: any): void;
}
export { FarrisTreeTableBindingDirective };
