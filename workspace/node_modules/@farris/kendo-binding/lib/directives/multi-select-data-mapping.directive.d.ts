import { OnInit, EventEmitter } from '@angular/core';
import { MultiSelectComponent } from '@farris/ui-multi-select';
import { ViewModel } from '@farris/devkit';
export declare class MultiSelectDataMappingDirective implements OnInit {
    private vm;
    private multiSelectComponent;
    dataSource: any;
    idField: string;
    textField: string;
    valueField: string;
    fjmField: string;
    fjdField: string;
    uiStateField: any;
    selectedId: string;
    selectedIdChanged: EventEmitter<string>;
    private originalDataSource;
    constructor(vm: ViewModel, multiSelectComponent: MultiSelectComponent);
    ngOnInit(): void;
    private selectIdBindingToUIStateField;
    /**
     *
     * @param data 需要格式化的数据
     */
    private formatDataSource;
    /**
     * 把平行结构的数据转换成树形结构
     * @param plainSource
     * @param field
     * @param layer
     */
    private plainToTree;
    /**
     * 递归遍历树形结构
     * @param parents
     * @param treeSource
     * @param field
     * @param layer
     */
    private recursive;
    /**
       *
       * @param ids 选中数据的id
       * @param dataSource 原始数据
       */
    private getListSelectionsById;
    /**
     *
     * @param ids 选中数据的id
     * @param dataSource 原始数据
     */
    private getTreeSelectionsById;
    /**
     * 根据父节点初始化树结构
     * @param bindingObjects
     */
    private buildTreeNodesByFjd;
}
