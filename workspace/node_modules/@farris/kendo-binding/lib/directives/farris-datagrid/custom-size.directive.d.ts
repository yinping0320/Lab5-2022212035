import { AfterViewInit, ChangeDetectorRef, ElementRef, Injector, OnInit, OnChanges, SimpleChanges } from '@angular/core';
import { DatagridComponent } from '@farris/ui-datagrid';
import { FrameContext } from '@farris/devkit';
export declare class CustomDatagridSizeDirective implements OnInit, AfterViewInit, OnChanges {
    private dg;
    private injector;
    private cd;
    private el;
    private frameContext;
    useCustomSize: boolean;
    sizeApiUri: string;
    private restService;
    constructor(dg: DatagridComponent, injector: Injector, cd: ChangeDetectorRef, el: ElementRef, frameContext: FrameContext);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngAfterViewInit(): void;
    private applyNewSize;
    private getSizeConfig;
}
