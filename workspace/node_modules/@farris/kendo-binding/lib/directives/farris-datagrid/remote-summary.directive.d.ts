import { ViewModel } from '@farris/devkit';
import { DatagridComponent } from '@farris/ui-datagrid';
export declare class FarrisDataGridRemoteSummaryDirective {
    viewModel: ViewModel;
    grid: DatagridComponent;
    /**
     * 服务器端合计命令名称
     */
    remoteSummaryCommand: string;
    constructor(viewModel: ViewModel, grid: DatagridComponent);
    /**
     * 执行命令
     * @param commandName 命令名称
     * @param frameId frameId
     */
    private execute;
}
