import { Subscription } from 'rxjs';
import { OnInit, EventEmitter, OnDestroy, OnChanges } from '@angular/core';
import { BindingData, BindingList, ViewModel, Change } from '@farris/devkit';
import { ListViewComponent } from '@farris/ui-list-view';
export declare class FarrisListViewBindingDirective implements OnInit, OnDestroy, OnChanges {
    private bindingData;
    private viewModel;
    private listview;
    bindingDataChangeEvent: Subscription;
    /**
     * 主键
     */
    protected readonly primaryKey: string;
    supportPaged: boolean;
    /**
     * 选中行切换事件
     */
    selectedRowChange: EventEmitter<any>;
    constructor(bindingData: BindingData, viewModel: ViewModel, listview: ListViewComponent);
    ngOnInit(): void;
    ngOnChanges(): void;
    ngOnDestroy(): void;
    /**
     * 获取分页信息
     */
    protected getPagingInfo(): any;
    /**
     * 设置listview属性
     */
    private setListViewPageProps;
    protected readonly bindingList: BindingList;
    private bindData;
    protected fireSelectedRowChange(selectedRowContext: any): void;
    /**
     * 设置BindingList的当前行
     * @param id 当前行内码
     */
    protected setSelectionIdToBindingData(id: string): void;
    /**
     * 数据源发生变更
     * @param change 变更
     */
    protected onBindingDataChange(change: Change): void;
    /**
     * 设置当前行
     * @param change 变更
     */
    private updateSelectedRow;
    protected selectRow(id: any): void;
    /**
     * 注册bindingdata变化事件
     */
    private registerBindingDataChangeEvent;
    /**
     * 取消bindingdata变化订阅
     */
    private unRegisterBindingDataChangeEvent;
    private setChecks;
    changeRow(event: any): void;
    /**
     * 切换页码触发事件
     * @param event 切换页码参数
     */
    pageChangedHandler(event: any): void;
    /**
     * 设置每页数据条数触发事件
     * @param event 切换页码参数
     */
    pageSizeChangedHandler(event: any): void;
    checkValuesChange(event: any): void;
}
