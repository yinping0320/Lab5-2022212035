import { OnInit, OnChanges, OnDestroy, SimpleChanges, Injector } from '@angular/core';
import { BindingList, FrameContext } from '@farris/devkit';
import { AppointmentCalendarComponent } from '@farris/appointment-calendar';
export declare class AppointmentCalendarBindingDirective implements OnInit, OnChanges, OnDestroy {
    private injector;
    private frameContext;
    private calendarComponent;
    private __DATA__;
    private bindingDataChangeEvent;
    private readonly bindingData;
    private readonly viewModel;
    private restService;
    /**
     * 获取绑定数据
     */
    protected readonly bindingList: BindingList;
    /**
     * 房间列表api url
     */
    url: string;
    /**
     * http method, default PUT
     */
    method: string;
    startDateVariable: string;
    endDateVariable: string;
    viewTypeVariable: string;
    constructor(injector: Injector, frameContext: FrameContext, calendarComponent: AppointmentCalendarComponent);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    private bindData;
    private onBindingDataChange;
    private registerBindingDataChangeEvent;
    /**
     * 取消bindingdata变化订阅
     */
    private unRegisterBindingDataChangeEvent;
    private loadPlacements;
    private bindPlacements;
    private updateState;
    private updateSelectedRow;
    private selectCalendarRow;
    filterChanged(event: any): void;
    protected setSelectionIdToBindingData(id: string): void;
    selectChange(event: any): void;
}
