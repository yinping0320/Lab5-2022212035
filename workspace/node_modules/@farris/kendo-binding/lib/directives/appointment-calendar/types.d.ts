import { Repository } from "@farris/devkit";
export declare abstract class BefRepository extends Repository<any> {
    restService: BefRestService;
}
export interface BefRestService {
    request(url: string, method: string, params?: any, options?: any, selfHandError?: boolean, ignoreError?: boolean, ignoreChanges?: boolean): any;
    buildRequestInfo(): any;
}
export declare enum ViewType {
    day = "day",
    week = "week"
}
