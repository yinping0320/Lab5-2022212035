import { Injector, OnChanges, OnDestroy, SimpleChanges } from "@angular/core";
import { FrameContext } from "@farris/devkit";
export declare class FarrisDynamicRequiredDirective implements OnChanges, OnDestroy {
    protected frameContext: FrameContext;
    private injector;
    /**
     * like {field:'',type:'require'}
     */
    field: string;
    /**
     * 启用校验的时机
     */
    requiredOn: boolean;
    constructor(frameContext: FrameContext, injector: Injector);
    ngOnDestroy(): void;
    ngOnChanges(changes: SimpleChanges): void;
    /**
     *
     * @param isRequired
     */
    private updateValidateRule;
}
