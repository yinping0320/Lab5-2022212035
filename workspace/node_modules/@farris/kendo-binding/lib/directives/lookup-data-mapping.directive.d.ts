/**
 * 使用方法：
 * [data-mapping]="{ id: 'user.userId', name: 'user.userName' }"
 * key 为帮助上的字段， value 为 表单中的字段名
 * 帮助上的同一个字段可以映射到表单中的多个字段中，{ ... id: 'user.userid, user.addusid'}
 * 多字段以逗号隔开
 *
 */
import { OnInit } from '@angular/core';
import { BindingObject, ViewModel } from '@farris/devkit';
import { LookupGridComponent } from '@farris/ui-lookup';
import { DataMapping } from './data-mapping';
export declare class LookupDataMappingDirective extends DataMapping implements OnInit {
    vm: ViewModel;
    private lookup;
    mapfields: any;
    target: BindingObject;
    constructor(vm: ViewModel, lookup: LookupGridComponent);
    ngOnInit(): void;
}
