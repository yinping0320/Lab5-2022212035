import { BindingObject, ViewModel } from '@farris/devkit';
/**
 * 帮助映射基类
 */
export declare class DataMapping {
    vm: ViewModel;
    target: BindingObject;
    /**
     * 多选帮助默认分隔符
     */
    protected readonly defaultSpliter = ",";
    /**
     * 映射数据
     * @param helpData 清空时，值为null
     * @param mapFields 格式形如：{id: "assoField.assoField", code: "assoField.assoField_Code", name: "assoField.assoField_Name"}
     * @param asClear 类似清空
     * @param [spliter=','] 多选分隔符
     */
    mappingData(helpData: any, mapFields: any, asClear?: boolean, spliter?: string): void;
    private mapping;
    private updateTarget;
    private updateTargetValue;
    /**
     * 获取帮助字段对应的值
     * @param field 帮助字段
     * @param helpData 帮助数据
     * @param [spliter=','] 多选帮助分隔符
     * @returns
     */
    private getHelpValue;
    protected getValue(f: string, data: any): string;
    protected setValue(target: object, paths: string[], value: any): void;
    private getBindingPathArray;
    private isNumberValue;
    /**
     *
     * @param mapFields  格式形如：{id: "assoField.assoField", code: "assoField.assoField_Code", name: "assoField.assoField_Name"} 或者 {id:'vid',code:'code',name:'name'}
     */
    private getMapFieldsPrimaryKey;
    private sortMapFieldKeys;
}
