import { Injector, OnInit } from "@angular/core";
import { UserSettings } from "@farris/devkit";
import { TimePickerComponent } from '@farris/ui-time-picker';
export declare class FarrisTimeLocalizationDirective implements OnInit {
    private injector;
    private userSettings;
    private componentRef;
    constructor(injector: Injector, userSettings: UserSettings, componentRef: TimePickerComponent);
    ngOnInit(): void;
    private localize;
}
