import { Injector, PipeTransform } from '@angular/core';
declare class ImageIdToUrlPipe implements PipeTransform {
    private injector;
    private defaultPic;
    private downloadService;
    constructor(injector: Injector);
    transform(value: string, rootId?: string): string;
}
export { ImageIdToUrlPipe };
