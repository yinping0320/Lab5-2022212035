import { BefRepository } from '@farris/mobile-bef';
import { Observable } from 'rxjs';
export declare class UploadAttachmentService {
    private befRepository;
    constructor(befRepository: BefRepository<any>);
    /**
     * 缓存附件
     * @param originFiles
     * @param uploadInfo
     */
    upload(originFiles: any[], uploadInfo: any): Observable<any[]>;
    /**
     * 预览附件pdf
     * @param fileType
     * @param rootId
     * @param metadataIdList
     */
    previewPdf(rootId: string, metadataIdList: string[]): Observable<string>;
    /**
     * 预览office txt文档
     * @param rootId
     * @param metadataIdList
     */
    previewDoc(rootId: string, metadataIdList: string[]): Observable<any>;
    /**
     * 获取图片url
     * @param attachmentId  附件id
     * @param rootId 根目录id
     * @returns
     */
    getAttachmentPath(attachmentId: string, rootId: string): string;
}
