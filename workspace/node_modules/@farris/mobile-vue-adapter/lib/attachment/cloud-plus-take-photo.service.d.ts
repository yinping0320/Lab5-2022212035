import { Subject } from 'rxjs';
import { TakePhoto } from './take-photo.interface';
export declare class CloudPlusTakePhoto implements TakePhoto {
    private collectFile;
    private jsBrigdeService;
    constructor(collectFile: Subject<any>);
    takeShortVideo(param: any): void;
    takePhotoAttachment(param: any): void;
    selectAttachments(param: any): void;
    selectFile(param: any): void;
    downloadAttachment(options: any): any;
    getSysInfo(): any;
    /**
     * 调起当前设备 播放短视频
     * @param path 视频路径
     */
    playShortVideo(path: string): void;
    /**
     * 预处理图片
     */
    processPic(result: string[]): {
        file: File;
        content: string;
    }[] | {
        file: File;
        content: string;
    };
    processFile(result: any[]): {
        file: File;
        content: string;
    }[];
    /**
     * 预处理视频
     * @param result
     */
    processVideo(result: any): {
        file: File;
        content: string;
        type: string;
        url: any;
        name: string;
    };
}
