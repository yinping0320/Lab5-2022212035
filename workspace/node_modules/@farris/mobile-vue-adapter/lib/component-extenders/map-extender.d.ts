import { ComponentExtender } from './component-extender';
/**
 * 列表组件扩展
 */
declare class MapExtender implements ComponentExtender {
    private mapInstance;
    private viewModelContext;
    constructor(mapInstance: any, viewModelContext: any);
    /**
     * 扩展组件
     */
    extend(): void;
    /**
     * 清理状态
     */
    dispose(): void;
    private getBindingPathArray;
}
export { MapExtender };
