import { ComponentExtender } from './component-extender';
/**
 * 列表组件扩展
 */
declare class TabExtender implements ComponentExtender {
    private tabInstance;
    private viewModelContext;
    constructor(tabInstance: any, viewModelContext: any);
    /**
     * 扩展组件
     */
    extend(): void;
    /**
     * 清理状态
     */
    dispose(): void;
}
export { TabExtender };
