import { Subject } from 'rxjs';
import { MapOperation } from './map-operation.interface';
export declare abstract class MapService {
    locationInstance: MapOperation;
    location$: Subject<unknown>;
    /**
     * 获取当前位置
     */
    getCurrentLocation(): void;
    /**
     * 获取坐标集合
     */
    getMapMarkers(): Subject<unknown>;
    setMapMarkers(): void;
}
