import { Subject } from 'rxjs';
import { MapOperation } from './map-operation.interface';
export declare class CloudPlusMapOperation implements MapOperation {
    private location$;
    constructor(location$: Subject<any>);
    /**
     * 云上协同  调用获取当前位置接口
     */
    getCurrentLoc(): void;
    private locationCallback;
    /**
     * 组装坐标结果
     * @param res 结果
     */
    private assembleMarkers;
}
