import { ViewModelContext } from '@farris/mobile-devkit';
/**
 * 帮助映射服务
 */
export declare class LookupMappingService {
    private viewModelContext;
    constructor(viewModelContext: ViewModelContext);
    /**
     * 映射字段
     * @summary
     * mapFields格式形如：
     * {
     *    id: "assoField.assoField",
     *    code: "assoField.assoField_Code",
     *    name: "assoField.assoField_Name"
     * }
     */
    mapFields(helpData: any, mappingFields: any): void;
    /**
     * 获取字段值
     */
    getFieldValue(fieldPath: string): any;
    private getValue;
    private getBindingPathArray;
}
