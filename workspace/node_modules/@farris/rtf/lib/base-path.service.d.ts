export declare class BasePathService {
    constructor();
    static getService(): any;
    /**
     * 获取基路径.
     * @param defaultValue - 当基路径为空或不存在时使用的默认值，默认为空字符.
     * @returns
     */
    static getBasePath(defaultValue?: string): any;
    /**
     * 在绝对路径的基础上增加基路径.
     * @param path - 原始地址.
     * @param defaultValue - 默认值.
     * @returns
     */
    static convertPath(path: string, defaultValue?: string): string;
    static parseParam(queryString: string): {
        [propName: string]: any;
    };
}
