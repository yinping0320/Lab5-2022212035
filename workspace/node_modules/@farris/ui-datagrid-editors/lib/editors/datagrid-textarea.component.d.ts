import { OnInit, ElementRef, Renderer2, NgZone, OnDestroy, Injector } from '@angular/core';
import { TextareaWordcountDirective } from '@farris/ui-forms';
import { DatagridBaseEditorDirective } from '../datagrid-base-editor.directive';
export declare class DatagridTextareaComponent extends DatagridBaseEditorDirective implements OnInit, OnDestroy {
    private ngzone;
    injector: Injector;
    instance: ElementRef;
    wordcountref: TextareaWordcountDirective;
    private inputClickHandler;
    private modalService;
    private messagerService;
    private eventManager;
    private altEnterHandler;
    constructor(render: Renderer2, el: ElementRef, ngzone: NgZone, injector: Injector);
    ngOnInit(): void;
    ngOnDestroy(): void;
    private bindEventHandler;
    showDialog(): void;
}
