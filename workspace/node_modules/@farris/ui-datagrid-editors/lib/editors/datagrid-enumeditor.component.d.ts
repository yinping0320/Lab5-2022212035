import { OnInit, Renderer2, ElementRef, NgZone, Injector, AfterViewInit } from '@angular/core';
import { EnumEditorInputComponent } from '@farris/ui-enum-editor';
import { DatagridBaseEditorDirective } from '../datagrid-base-editor.directive';
export declare class DatagridEnumEditorComponent extends DatagridBaseEditorDirective implements OnInit, AfterViewInit {
    private ngzone;
    injector: Injector;
    instance: EnumEditorInputComponent;
    constructor(render: Renderer2, el: ElementRef, ngzone: NgZone, injector: Injector);
    ngOnInit(): void;
    ngAfterViewInit(): void;
}
