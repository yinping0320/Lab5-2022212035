import { OnInit, Renderer2, ElementRef, NgZone, Injector } from '@angular/core';
import { DatagridBaseEditorDirective } from '../datagrid-base-editor.directive';
export declare class DatagridCheckboxComponent extends DatagridBaseEditorDirective implements OnInit {
    private ngzone;
    injector: Injector;
    checked: boolean;
    instance: ElementRef;
    chkboxEl: ElementRef;
    constructor(render: Renderer2, el: ElementRef, ngzone: NgZone, injector: Injector);
    ngOnInit(): void;
    onSpaceHandle($event: KeyboardEvent): void;
    handleClick(event: MouseEvent): void;
    private checkedChanges;
}
