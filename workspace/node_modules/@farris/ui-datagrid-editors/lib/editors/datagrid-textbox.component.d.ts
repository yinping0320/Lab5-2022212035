import { OnInit, Renderer2, ElementRef, OnDestroy, Injector } from '@angular/core';
import { DatagridBaseEditorDirective } from '../datagrid-base-editor.directive';
export declare class DatagridTextboxComponent extends DatagridBaseEditorDirective implements OnInit, OnDestroy {
    instance: ElementRef;
    inputType: string;
    constructor(render: Renderer2, el: ElementRef, injector: Injector);
    ngOnInit(): void;
    ngOnDestroy(): void;
}
