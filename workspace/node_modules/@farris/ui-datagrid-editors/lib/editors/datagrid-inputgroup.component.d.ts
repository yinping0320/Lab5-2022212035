import { EventEmitter, Injector } from '@angular/core';
import { OnInit, Renderer2, ElementRef } from '@angular/core';
import { DatagridBaseEditorDirective } from '../datagrid-base-editor.directive';
import { InputGroupComponent } from '@farris/ui-input-group';
export declare class DatagridInputGroupComponent extends DatagridBaseEditorDirective implements OnInit {
    injector: Injector;
    clear: EventEmitter<{}>;
    clickHandle: EventEmitter<{}>;
    instance: InputGroupComponent;
    constructor(render: Renderer2, el: ElementRef, injector: Injector);
    ngOnInit(): void;
    onClear($event: any): boolean;
    onClick($event: any): boolean;
    onValueChange($event: any): void;
    private eParams;
}
