import { OnInit, Renderer2, ElementRef, NgZone, Injector, AfterViewInit } from '@angular/core';
import { SorterComponent } from '@farris/ui-sort-editor';
import { DatagridBaseEditorDirective } from '../datagrid-base-editor.directive';
export declare class DatagridSortEditorComponent extends DatagridBaseEditorDirective implements OnInit, AfterViewInit {
    private ngzone;
    injector: Injector;
    instance: SorterComponent;
    constructor(render: Renderer2, el: ElementRef, ngzone: NgZone, injector: Injector);
    ngOnInit(): void;
    ngAfterViewInit(): void;
}
