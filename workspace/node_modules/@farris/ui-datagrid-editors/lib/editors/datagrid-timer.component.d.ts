import { OnInit, Renderer2, ElementRef, Injector } from '@angular/core';
import { DatagridBaseEditorDirective } from '../datagrid-base-editor.directive';
import { TimePickerComponent } from '@farris/ui-time-picker';
export declare class DatagridTimerComponent extends DatagridBaseEditorDirective implements OnInit {
    instance: TimePickerComponent;
    constructor(render: Renderer2, el: ElementRef, injector: Injector);
    ngOnInit(): void;
    onValueChange($event: any): void;
}
