import { Injector, PipeTransform } from '@angular/core';
import { SearchBoxComponent } from '../search-box.component';
export declare class SearchWordPipe implements PipeTransform {
    private injector;
    private searchBoxRef;
    constructor(injector: Injector, searchBoxRef: SearchBoxComponent);
    transform(value: any, ...args: any[]): any;
}
