import { AfterViewInit, ComponentFactoryResolver, EventEmitter, Injector, NgZone, OnDestroy, OnInit, Renderer2 } from '@angular/core';
import { InputGroupComponent } from '@farris/ui-input-group';
import { SearchBoxComponent } from '../search-box.component';
import { SearchField } from '../search-box.types';
export declare class SearchPanelDirective implements OnInit, AfterViewInit, OnDestroy {
    private injector;
    private ngzone;
    private render;
    private inputRef;
    private searchBoxRef;
    private cfr;
    fields: Array<SearchField>;
    maxFields: number;
    /** 启用 任意字段 */
    useAnyField: boolean;
    escHandler: EventEmitter<{}>;
    private panelElement;
    private searchPanelRef;
    private maxPanelItems;
    private minPanelWidth;
    constructor(injector: Injector, ngzone: NgZone, render: Renderer2, inputRef: InputGroupComponent, searchBoxRef: SearchBoxComponent, cfr: ComponentFactoryResolver);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    ngOnDestroy(): void;
    private removePanelElement;
    hide(e?: any): void;
    private setPanelPosition;
    private createDataPanel;
    private selectItem;
    private calculationPanelHeight;
    private getInputSizeInfo;
    private getPanelSize;
    registerKeyboardEvent($event: KeyboardEvent): void;
}
