import { OnInit, ElementRef, Renderer2, AfterViewInit, EventEmitter, OnDestroy } from '@angular/core';
import { Subscription } from 'rxjs';
export declare class LoadingComponent implements OnInit, AfterViewInit, OnDestroy {
    private render;
    private el;
    showMessage: boolean;
    loadingStyleDom: string[];
    id: number;
    private _isActive;
    private activeSubject;
    message: string;
    width: number;
    /** loading 样式 */
    type: number;
    delay: number;
    isActive: boolean;
    loadingContainerEl: ElementRef;
    loadingBackdrop: ElementRef;
    closed: EventEmitter<any>;
    activedSubscrition: Subscription;
    parentContainer: any;
    private destroy$;
    private debugSer;
    private commonUtils;
    constructor(render: Renderer2, el: ElementRef);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    ngOnDestroy(): void;
    close(): void;
    private setPosition;
    private animate;
    private addAnimationEndEvent;
    closeWhenClick5($event: MouseEvent): void;
}
