import { OnInit, TemplateRef, Renderer2, ElementRef, NgZone, Injector, OnDestroy } from '@angular/core';
import { FilterPanelService, FilterPanelComponent } from '@farris/ui-filter-panel';
import { DatagridComponent } from '../../datagrid.component';
import { DatagridSmartFilterService } from '../../services/datagrid-smart-filter.service';
export declare class DatagridSmartFilterComponent implements OnInit, OnDestroy {
    private render;
    private el;
    private zone;
    private inject;
    private filterPanelService;
    private smartFilterSer;
    private dg;
    column: any;
    filterData: any;
    disabled: boolean;
    sortTmp: TemplateRef<any>;
    filterPanelRef: FilterPanelComponent;
    private idService;
    private smartFilterDataChanged$;
    private removeFilter$;
    private clearAllFilter$;
    private smartFilterEvents;
    constructor(render: Renderer2, el: ElementRef, zone: NgZone, inject: Injector, filterPanelService: FilterPanelService, smartFilterSer: DatagridSmartFilterService, dg: DatagridComponent);
    ngOnInit(): void;
    ngOnDestroy(): void;
    private getFilterData;
    private getPanelPosition;
    private hideFilterPanel;
    private clearColumnFilter;
    showFilterPanel($event: any): void;
    onClick($event: any): boolean;
    onSort($event: any, order: any): void;
}
