import { PipeTransform, Injector } from '@angular/core';
export declare class CellTootipMessagePipe implements PipeTransform {
    private injector;
    private colFormatSer;
    constructor(injector: Injector);
    transform(col: any, ...args: any[]): any;
    private getCellTooltipContent;
}
