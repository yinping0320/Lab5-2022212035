import { OnDestroy, ElementRef, Injector, OnInit } from '@angular/core';
export declare class DatagridCellTooltipDirective implements OnInit, OnDestroy {
    private el;
    private injector;
    enable: boolean;
    position: 'top' | 'bottom' | 'left' | 'right' | 'top-left' | 'top-center' | 'top-right';
    positionStyle: string;
    tooltipStyleClass: string;
    message: string;
    delay: number;
    cls: string;
    type: 'danger' | 'success' | 'info' | 'warning';
    tipMode: 'allways' | 'auto';
    container: any;
    private commonUtils;
    private showTipTimer;
    private _mouseenter;
    private _mouseleave;
    private _mouseclick;
    private ngZone;
    private render;
    private dg;
    constructor(el: ElementRef, injector: Injector);
    ngOnInit(): void;
    ngOnDestroy(): void;
    onMouseEnter($event: any): void;
    onMouseLeave(): void;
    onClick(e: MouseEvent): void;
    private getTextContainer;
    private donotShowTips;
    hide(): void;
    show($event: MouseEvent): void;
    private updateArrowPosition;
    create(elSize: any): void;
    private tooltipCls;
    private getThemeCls;
    /** 获取宿主尺寸 */
    private getBoxSizeObj;
    /** 获取tooltip 容器尺寸 */
    private getTooltipSize;
    /** 获取窗口尺寸 */
    private getWindowSize;
    /** 根据内容的多少，自动更改tooltip 的位置 */
    private changeTooltipPosition;
}
