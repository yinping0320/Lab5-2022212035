import { OnInit, ElementRef, Injector, OnDestroy } from '@angular/core';
export declare class DatagridHeaderCheckboxComponent implements OnInit, OnDestroy {
    private injector;
    checked: boolean;
    disabled: boolean;
    indeterminate: boolean;
    chk: ElementRef;
    private dfs;
    private dg;
    private destroy$;
    constructor(injector: Injector);
    ngOnInit(): void;
    ngOnDestroy(): void;
    updateStatus(checked: boolean): void;
    handleClick(event: MouseEvent): void;
}
