import { Subscription } from 'rxjs';
import { DatagridComponent } from './../../datagrid.component';
import { OnInit, ElementRef, Injector, OnDestroy, OnChanges, SimpleChanges } from '@angular/core';
export declare class DatagridCheckboxComponent implements OnInit, OnDestroy, OnChanges {
    private injector;
    dg: DatagridComponent;
    rowData: any;
    rowIndex: any;
    checked: boolean;
    disabled: boolean;
    indeterminate: boolean;
    chk: ElementRef;
    private dfs;
    private cd;
    checked$: any;
    private destroy$;
    subscriptions: Subscription[];
    constructor(injector: Injector, dg: DatagridComponent);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    private listenSubjects;
    handleClick(event: MouseEvent): void;
    private changeCheckedStatus;
}
