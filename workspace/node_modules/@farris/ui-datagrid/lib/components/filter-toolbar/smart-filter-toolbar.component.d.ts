import { Subject } from 'rxjs';
import { EventEmitter, OnInit, ElementRef, Renderer2, Injector, OnChanges, SimpleChanges, OnDestroy } from '@angular/core';
import { DatagridComponent } from '../../datagrid.component';
export declare class SmartFilterToolbarComponent implements OnInit, OnChanges, OnDestroy {
    private el;
    private render;
    private inject;
    padding: string;
    height: number;
    filterData: any[];
    clearFilters: EventEmitter<{}>;
    removeFilter: EventEmitter<{}>;
    filterChange: EventEmitter<{}>;
    resize: EventEmitter<{}>;
    filtebar: any;
    sk: string;
    dg: DatagridComponent;
    destory$: Subject<{}>;
    constructor(el: ElementRef, render: Renderer2, inject: Injector);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    /** 条件变化事件 */
    onQuery($event: any): void;
    /** 清除所有条件 */
    onResetFilter($event: any): void;
    /** 移除单个条件 */
    onRemoveFilter($event: any): void;
    /** 展开折叠事件 */
    onResize($event: any): void;
}
