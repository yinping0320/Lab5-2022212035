import { LocaleService } from '@farris/ui-locale';
import { Subscription } from 'rxjs';
import { OnInit, Injector, ChangeDetectorRef, ElementRef, Renderer2, NgZone, OnDestroy } from '@angular/core';
import { DatagridService } from './../../services/datagrid.service';
import ResizeObserver from 'resize-observer-polyfill';
export declare class DatagridFilterToolbarComponent implements OnInit, OnDestroy {
    private inject;
    private el;
    private cd;
    private dgs;
    private render;
    private ngZone;
    private localeSer;
    empty: string;
    filterText: string;
    openFilterPanel: boolean;
    showExpand: boolean;
    enableFilterSetting: boolean;
    filterPanelRef: ElementRef;
    filterPanelTextRef: ElementRef;
    filterTextRef: ElementRef;
    filterToolbarRef: ElementRef;
    readonly toolbarWidth: any;
    readonly filterTextContainerWidth: number;
    readonly filterTextRealWidth: any;
    filterChangeSubscription: Subscription;
    windowResizeHandler: any;
    ro: ResizeObserver;
    private destory$;
    private dfrs;
    private dg;
    constructor(inject: Injector, el: ElementRef, cd: ChangeDetectorRef, dgs: DatagridService, render: Renderer2, ngZone: NgZone, localeSer: LocaleService);
    ngOnInit(): void;
    ngOnDestroy(): void;
    private bindEvent;
    private bindFilterFieldHandler;
    private removeFilterField;
    private updateExpandState;
    isShowExpand(): boolean;
    clearFilter($event: any): boolean;
    refreshFilter($event: any): boolean;
    showFilterTextPanel(): void;
}
