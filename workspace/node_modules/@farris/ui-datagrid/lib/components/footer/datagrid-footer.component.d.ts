import { DatagridComponent } from './../../datagrid.component';
import { OnInit, ElementRef, Renderer2, OnDestroy, Injector, OnChanges, SimpleChanges } from '@angular/core';
import { Subscription } from 'rxjs';
export declare class DatagridFooterComponent implements OnInit, OnChanges, OnDestroy {
    private render;
    private injector;
    dg: DatagridComponent;
    private _footerData;
    data: any[];
    height: number;
    width: number;
    columns: any[];
    columnsGroup: any;
    /** 显示位置，默认为 底部 */
    position: 'top' | 'bottom';
    /** 是否有左侧固定列 */
    hasFixedColumns: boolean;
    fast: boolean;
    footerContainer: ElementRef;
    footerBody: ElementRef;
    footerBody2: ElementRef;
    fixedLeft: ElementRef;
    private fixedRight;
    fr: any;
    scrollXSubscription: Subscription;
    private dgs;
    private scrollX;
    private showShadowCls;
    rightFixedColumnWidth: number;
    private cdRef;
    private destroy$;
    constructor(render: Renderer2, injector: Injector, dg: DatagridComponent);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    private scrollEvents;
    private getRightFixedColWidth;
    private setFooterPosition;
    private renderFooterStyle;
    setFixedColumnPosition(isShow: any, scrollLeft: any): void;
}
