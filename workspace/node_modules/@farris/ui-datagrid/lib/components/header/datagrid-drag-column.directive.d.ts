import { NgZone, Injector, Renderer2, ElementRef, AfterViewInit, OnChanges, SimpleChanges, OnDestroy } from '@angular/core';
import { ContainerOptions } from '@farris/smooth-dnd';
import { DatagridComponent } from '../../datagrid.component';
import { DatagridHeaderComponent } from './datagrid-header.component';
import { DragDropColumnService } from './drag-drop-column.service';
export interface DragStartEndInfo {
    isSource: boolean;
    payload: any;
    willAcceptDrop: boolean;
}
export declare class DragColumnDirective implements AfterViewInit, OnChanges, OnDestroy {
    private ngzone;
    private injector;
    private render;
    private el;
    private header;
    private dg;
    private dndSer;
    private container;
    enableDrag: boolean;
    groupName: string;
    options: ContainerOptions;
    constructor(ngzone: NgZone, injector: Injector, render: Renderer2, el: ElementRef, header: DatagridHeaderComponent, dg: DatagridComponent, dndSer: DragDropColumnService);
    ngOnChanges(changes: SimpleChanges): void;
    ngAfterViewInit(): void;
    ngOnDestroy(): void;
    private disposeDnd;
    private initDnD;
    private getChildPayload;
    onDropReady(dropResult: any): void;
    private onDrop;
    private onDragStart;
    private onDragEnd;
    private onDragEnter;
    private columnsGroup;
}
