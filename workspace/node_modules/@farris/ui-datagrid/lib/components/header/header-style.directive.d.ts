import { ElementRef, Renderer2, Injector, OnInit, AfterViewInit } from '@angular/core';
import { DataColumn } from './../../types/data-column';
export declare class HeaderStyleDirective implements OnInit, AfterViewInit {
    private el;
    private render;
    private inject;
    column: DataColumn;
    groupHeader: boolean;
    private dg;
    constructor(el: ElementRef, render: Renderer2, inject: Injector);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    private renderCss2Element;
    private renderStyle;
}
