import { ElementRef, Injector, OnInit, NgZone, Renderer2, OnDestroy } from '@angular/core';
import { DatagridComponent } from '../../datagrid.component';
export declare class DatagridRowDblClickDirective implements OnInit, OnDestroy {
    private el;
    private injector;
    private ngZone;
    private render;
    dg: DatagridComponent;
    constructor(el: ElementRef, injector: Injector, ngZone: NgZone, render: Renderer2, dg: DatagridComponent);
    ngOnInit(): void;
    ngOnDestroy(): void;
    dblclickRowEvent(event: any): boolean;
}
