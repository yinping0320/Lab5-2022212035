import { OnInit, ChangeDetectorRef, ElementRef, Injector, NgZone, EventEmitter, OnDestroy, Renderer2 } from '@angular/core';
import { Subscription } from 'rxjs';
import { DatagridService } from './../../services/datagrid.service';
import { DatagridComponent } from '../../datagrid.component';
import { DatagridBodyComponent } from './datagrid-body.component';
import { DatagridFacadeService } from '../../services/datagrid-facade.service';
export declare class FixedLeftRowsComponent implements OnInit, OnDestroy {
    private cd;
    dg: DatagridComponent;
    dgb: DatagridBodyComponent;
    dfs: DatagridFacadeService;
    el: ElementRef;
    private injector;
    private ngZone;
    columns: any[];
    toggle: EventEmitter<{}>;
    checkedRows: any[];
    selectedRows: any[];
    data: any[];
    /** 启用分组时，数据源中自动设置行索引字段 */
    groupRowIndex: string;
    isGroupRow: string;
    groupRow: string;
    isGroupFooter: string;
    visible: string;
    groupLevelField: string;
    filterConditions: string;
    subscriptions: Subscription[];
    dgs: DatagridService;
    render2: Renderer2;
    private destroy$;
    constructor(cd: ChangeDetectorRef, dg: DatagridComponent, dgb: DatagridBodyComponent, dfs: DatagridFacadeService, el: ElementRef, injector: Injector, ngZone: NgZone);
    private listenSubject;
    ngOnInit(): void;
    ngOnDestroy(): void;
    private detectChanges;
    toggleGroupRow(row: any, index: any, open: any, $event?: MouseEvent): void;
    getGroupSummaryData(groupId: string): any;
    getGroupColumn(field: any): import("../../types").DataColumn;
}
