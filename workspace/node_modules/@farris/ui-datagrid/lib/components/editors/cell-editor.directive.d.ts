import { DatagridComponent } from './../../datagrid.component';
import { ComponentFactoryResolver, ComponentRef, OnInit, AfterViewInit, ViewContainerRef, OnDestroy, ApplicationRef } from '@angular/core';
import { FormGroup, FormBuilder } from '@angular/forms';
import { DataColumn } from '../../types';
export declare class GridCellEditorDirective implements OnInit, AfterViewInit, OnDestroy {
    private resolver;
    private container;
    private app;
    private fb;
    dg: DatagridComponent;
    column: DataColumn;
    group: FormGroup;
    rowData: any;
    value: any;
    height: any;
    componentRef: ComponentRef<any>;
    private cfs;
    private timer;
    constructor(resolver: ComponentFactoryResolver, container: ViewContainerRef, app: ApplicationRef, fb: FormBuilder, dg: DatagridComponent);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    ngOnDestroy(): void;
    private createCellEditor;
    private updateControlValue;
    hideCover(): void;
}
