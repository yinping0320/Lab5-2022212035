import { DatagridComponent } from '../../datagrid.component';
import { OnInit, Injector, ElementRef, AfterViewInit, OnChanges, SimpleChanges, DoCheck } from '@angular/core';
export declare class AutoHeightDirective implements OnInit, AfterViewInit, OnChanges, DoCheck {
    dg: DatagridComponent;
    private inject;
    private el;
    autoHeight: boolean;
    /** 数据列表总高度 */
    wheelHeight: number;
    private _timer;
    private _dgb;
    constructor(dg: DatagridComponent, inject: Injector, el: ElementRef);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngDoCheck(): void;
    private updateDatagridHeight;
    private _updateDatagridHeight;
}
