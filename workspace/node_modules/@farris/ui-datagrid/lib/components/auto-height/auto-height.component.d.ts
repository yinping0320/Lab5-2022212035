import { DatagridComponent } from './../../datagrid.component';
import { OnInit, Injector } from '@angular/core';
/**
 * 自动高度
 * 禁用分页，禁用固定表头，禁用虚拟加载，禁用固定列
 * 支持单元格编辑, 支持排序, 支持拖动列宽
 */
export declare class AutoHeightComponent implements OnInit {
    dg: DatagridComponent;
    private inject;
    columns: any[];
    columnsGroup: any;
    data: any;
    constructor(dg: DatagridComponent, inject: Injector);
    ngOnInit(): void;
}
