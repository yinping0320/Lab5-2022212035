import { HttpClient } from '@angular/common/http';
import { DataResult } from './state';
import { Observable } from 'rxjs';
import { InjectionToken } from '@angular/core';
export declare const DATAGRID_REST_SERVICEE: InjectionToken<RestService>;
export declare abstract class RestService {
    abstract getData(url: string, param?: any): Observable<DataResult>;
}
export declare class DefaultRestService implements RestService {
    private http;
    constructor(http: HttpClient);
    getData(url: string, param?: any): Observable<DataResult>;
}
