import { PipeTransform } from '@angular/core';
import { DatagridFacadeService } from '../services/datagrid-facade.service';
export declare class RowDataIdPipe implements PipeTransform {
    private dfs;
    constructor(dfs: DatagridFacadeService);
    transform(rowData: any, fixedRight?: boolean, fixedLeft?: boolean, isGroupRow?: boolean): any;
}
