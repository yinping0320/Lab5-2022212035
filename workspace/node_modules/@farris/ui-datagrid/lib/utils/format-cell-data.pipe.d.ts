import { PipeTransform, Injector } from '@angular/core';
import { ColumnFormatService } from '@farris/ui-common/column';
import { DataColumn } from '../types/data-column';
export declare class FormatCellDataPipe implements PipeTransform {
    private cfs;
    private injector?;
    private localeService;
    private dataGrid;
    private elRef;
    private localeId;
    constructor(cfs: ColumnFormatService, injector?: Injector);
    transform(col: any, rowData: any, groupFooter?: boolean, footer?: boolean): any;
    private setFooterCellTitle;
    private getMultilingualValue;
    private setPlaceHolderWhenEnableEditCellStyle;
    private getDatagridInstance;
    cellIsReadOnly(col: DataColumn, rowData: any): boolean;
}
