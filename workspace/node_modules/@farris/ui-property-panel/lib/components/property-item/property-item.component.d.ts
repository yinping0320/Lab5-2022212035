import { OnInit, OnChanges, SimpleChanges, EventEmitter, ComponentFactoryResolver, Injector, ElementRef } from '@angular/core';
import { BsModalService } from '@farris/ui-modal';
import { NotifyService } from '@farris/ui-notify';
import { PropertyEntity } from '../../entity/property-entity';
export declare class PropertyItemComponent implements OnInit, OnChanges {
    private resolver;
    private modalService;
    private injector;
    private notifyServ;
    el: ElementRef;
    elementConfig: PropertyEntity;
    elementValue: any;
    valueChanged: EventEmitter<any>;
    submitModal: EventEmitter<any>;
    triggerRefreshPanel: EventEmitter<any>;
    elementShowValue: any;
    selectOptions: any[];
    itemType: string;
    show: boolean;
    numberRestriction: {
        format: string;
        min: string;
        max: string;
    };
    showClearButton: boolean;
    showDefaultValue: boolean;
    constructor(resolver: ComponentFactoryResolver, modalService: BsModalService, injector: Injector, notifyServ: NotifyService, el: ElementRef);
    ngOnChanges(changes: SimpleChanges): void;
    ngOnInit(): void;
    refresh(): void;
    /**
     * 鼠标移入
     */
    onmouseover(): void;
    /**
     * 鼠标离开
     */
    onmouseleave(): void;
    /**
     * 清除模态框内容
     */
    modalClear(): void;
    /**
     * 变更属性值
     */
    changeValue(): void;
    /**
     * 自定义编辑器使用模态框打开
     */
    openModal(): void;
    /**
     * 创建自定义编辑器
     */
    private createEditorComponent;
    /**
     * 模态框场景下将属性值转换为输入框中显示的值
     * @param value
     */
    private _convertModalShowValue;
}
