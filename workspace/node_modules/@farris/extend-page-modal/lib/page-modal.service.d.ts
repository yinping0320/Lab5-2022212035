import { ComponentRef, TemplateRef, EventEmitter, RendererFactory2, NgZone, Injector } from '@angular/core';
import { FEModalBackdropComponent } from './page-modal-backdrop.component';
import { FEPageModalComponent } from './page-modal.component';
import { FEPageModalSidebarComponent } from './page-sidebar.component';
import { FEPageModalOptions } from './page-modal-options.class';
import { FEPageModalRef } from './page-modal-ref.service';
import { ComponentLoaderFactory, ComponentLoader } from '@farris/ui-modal/component-loader';
import { Observable } from 'rxjs';
export declare class FEPageModalService {
    private clf;
    private _injector;
    ngZone: NgZone;
    version: string;
    config: FEPageModalOptions;
    onShow: EventEmitter<any>;
    onShown: EventEmitter<any>;
    onHide: EventEmitter<any>;
    onHidden: EventEmitter<any>;
    protected isBodyOverflowing: boolean;
    protected originalBodyPadding: number;
    protected scrollbarWidth: number;
    protected backdropRef: ComponentRef<FEModalBackdropComponent>;
    private _backdropLoader;
    private modalsCount;
    private lastDismissReason;
    private loaders;
    private _renderer;
    private commonUtils;
    constructor(rendererFactory: RendererFactory2, clf: ComponentLoaderFactory, _injector: Injector, ngZone: NgZone);
    showByComponent(): void;
    showByUrl(content: string, config?: FEPageModalOptions): Observable<FEPageModalRef | null>;
    /** Shows a modal */
    show(content: string | TemplateRef<any> | any, config?: FEPageModalOptions): FEPageModalRef;
    hide(level: number): void;
    /**
     * 确保当前窗口在最上层
     * @param _modalRef
     */
    private updateZIndex;
    _showBackdrop(): void;
    _hideBackdrop(): void;
    _showModal(content: any): FEPageModalRef;
    _onMouseDown(e: any): boolean;
    _hideModal(level: number): void;
    getModalsCount(): number;
    getCurrentModalContainer(): ComponentLoader<FEPageModalSidebarComponent | FEPageModalComponent>;
    setDismissReason(reason: string): void;
    removeBackdrop(): void;
    /** AFTER PR MERGE MODAL.COMPONENT WILL BE USING THIS CODE */
    /** Scroll bar tricks */
    /** @internal */
    checkScrollbar(): void;
    setScrollbar(): void;
    private resetScrollbar;
    private getScrollbarWidth;
    private _createLoaders;
    private removeLoaders;
    private copyEvent;
    private checkDialogSize;
}
