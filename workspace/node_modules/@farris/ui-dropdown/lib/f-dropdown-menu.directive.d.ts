import { AfterViewChecked, ElementRef, OnDestroy } from "@angular/core";
import { FDropdownDirective } from "./f-dropdown.directive";
export declare class FDropdownMenuDirective implements AfterViewChecked, OnDestroy {
    private elementRef;
    private dropdown;
    private _docRect;
    showDropdownMenu: boolean;
    dpIsOpen: boolean;
    private overLayService;
    private commonUtils;
    constructor(elementRef: ElementRef, dropdown: FDropdownDirective);
    ngAfterViewChecked(): void;
    private getRealPlacement;
    ngOnDestroy(): void;
    private openStateChange;
    private changeDirection;
    private issubMenu;
    private setMenuPanelPosition;
}
