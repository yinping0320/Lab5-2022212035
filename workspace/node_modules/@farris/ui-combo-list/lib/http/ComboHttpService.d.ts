import { InjectionToken } from '@angular/core';
import { Observable } from 'rxjs';
export interface ComboHttpService {
    /**
     * 根据元数据ID及相关参数获取数据
     * @param uri 数据ID or Rest API
     * @param data 服务器端接收参数 key/value
     */
    context?: any;
    getData(uri: string, params?: any, method?: string): Observable<any>;
}
export declare const ComboServerSideToken: InjectionToken<ComboHttpService>;
