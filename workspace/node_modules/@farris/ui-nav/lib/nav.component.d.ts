import { OnInit, EventEmitter, OnDestroy } from '@angular/core';
import { Observable, Subscription } from 'rxjs';
interface navItem {
    id: string;
    text: string;
    num?: number;
    disable?: boolean;
}
export declare class NavComponent implements OnInit, OnDestroy {
    _horizontal: boolean;
    horizontal: any;
    _navData: navItem[];
    navData: Array<navItem>;
    _activeNavId: string;
    activeNavId: string;
    /** nav切换前事件 */
    navPicking: (emptyObj?: {}) => Observable<any>;
    navPickingSubscription: Subscription;
    maxNum: number;
    navChange: EventEmitter<{}>;
    constructor();
    ngOnInit(): void;
    ngOnDestroy(): void;
    initNavData(): void;
    navClick(nav: any): void;
    initEvents(): void;
}
export {};
