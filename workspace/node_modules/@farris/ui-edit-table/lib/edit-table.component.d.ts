import { ChangeDetectorRef, EventEmitter, Injector, OnInit } from "@angular/core";
import { IdService } from "@farris/ui-common";
import { MessagerService } from "@farris/ui-messager";
import { NotifyService } from "@farris/ui-notify";
import { EditTableColumn } from "./edit-table.types";
export declare class EditTableComponent implements OnInit {
    private injector;
    private idSer;
    private cdr;
    private msgSer;
    private notifySer;
    columns: Array<EditTableColumn>;
    data: any[];
    selectId: string;
    /** 行数据标识字段 */
    idField: string;
    dataChange: EventEmitter<{}>;
    private selectedItem;
    constructor(injector: Injector, idSer: IdService, cdr: ChangeDetectorRef, msgSer: MessagerService, notifySer: NotifyService);
    ngOnInit(): void;
    private dataChanged;
    private createNewData;
    /** 插入新行 */
    insertRow(index: number, $event?: MouseEvent): void;
    /** 新增行 */
    appendNew($event: MouseEvent): void;
    /** 选中行 */
    selectRow($event: MouseEvent, rowData: any, index: any): void;
    deleteRow($event: MouseEvent, index: any): void;
    clear($event: any): void;
    moveRow($event: any, action: 'top' | 'bottom' | 'up' | 'down'): void;
    onComboLookupValueChange($event: any, ctx: any): void;
}
