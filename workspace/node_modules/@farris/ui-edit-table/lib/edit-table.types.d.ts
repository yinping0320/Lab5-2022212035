import { TemplateRef } from "@angular/core";
export interface EditTableEditor {
    type: 'combo-list' | 'input-group' | 'number' | 'datetime' | 'checkbox' | 'text' | 'color' | 'lookup' | 'switch';
    options?: {
        [key: string]: any;
    };
}
export declare type EditorFn = (rowdata: any) => EditTableEditor;
export interface EditTableColumn {
    field: string;
    title: string;
    editTemplate?: TemplateRef<any>;
    defaultValue?: any;
    halign?: '' | 'left' | 'center' | 'right';
    align?: '' | 'left' | 'center' | 'right';
    editor?: EditorFn | EditTableEditor;
}
