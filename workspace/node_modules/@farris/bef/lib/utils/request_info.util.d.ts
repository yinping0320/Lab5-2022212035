import { Entity } from '@farris/devkit';
import { BefRepository } from '../bef_repository';
import { RequestInfo } from '../types';
declare class RequestInfoUtil {
    /**
     * 检查是否有requestInfo
     */
    static hasRequestInfo(options: any): boolean;
    /**
     * 检查是否有RquestInfo
     * @summary
     * 产品部依赖了请求时传递RequestInfo，返回时直接取到returnValue的特性。
     */
    static hasRequestInfoWithR(options: any): boolean;
    /**
     * 构造RequestInfo
     */
    static buildRequestInfo(repository: BefRepository<Entity>): RequestInfo;
    /**
     * 构造带requestInfo的body（仅适用于body中有多个参数的情况）
     * @summary
     * 一个参数时body=requestInfo，不适用该方法。
     */
    static buildBodyWithRequestInfo(body: any, repository: BefRepository<Entity>): any;
    static getRequestInfo(options: any): any;
}
export { RequestInfoUtil };
