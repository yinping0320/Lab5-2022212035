import { Injector } from "@angular/core";
import { BackEndMessage } from "@farris/devkit";
export declare class BackEndMessageUtil {
    static handleMessage(messages: object[], injector: Injector, options?: any): void;
    static isBackEndMessageHandlerExist(injector: Injector): boolean;
    static getFormlessMessages(messages: BackEndMessage.IBizMessage[]): BackEndMessage.IBizMessage[];
    static buildBackEndMessages(messages: object[]): BackEndMessage.IBizMessage[];
}
