import { AppContext } from '@farris/devkit';
import { ChangeDetail } from './types';
import { Injector } from '@angular/core';
/**
 * Be变量管理器
 */
declare class BefVariableManager {
    private appContext;
    private ngVariables;
    private injector;
    /**
     * 变量元数据
     */
    private ngVariableMap;
    /**
     * 设置值
     */
    private innerValueMap;
    /**
     * 构造函数
     */
    constructor(appContext: AppContext, ngVariables: any, injector: Injector);
    /**
     * 获取变更集
     */
    handleChangeDetail(changeDetail: ChangeDetail): void;
    /**
     * Build ChangeDetail instance for all variables.
     */
    buildChangeDetail(): ChangeDetail;
    /**
     * Clear variable values cached in the innerValueMap property.
     */
    reset(): void;
    /**
     * 清空所有vo变量变更集
     */
    clearChanges(): void;
    /**
     * 清空只读vo变更
     * @param changeDetail
     * @returns
     */
    clearChangeDetail(changeDetail: ChangeDetail): void;
    /**
     * Append changed variable to ChangeDetail instance.
     */
    private appendToChangeInfo;
    /**
     * 从UIState上获取值
     */
    private getValueFromUIState;
    /**
     * 获取根组件上的UIState
     */
    private getRootUIState;
    /**
     * 值比较
     * @todo 临时采用这种方式
     */
    private isValueEqual;
    /**
     * Check if the object is a plain object
     */
    private isUdtVariable;
    /**
     * 设置值到UIState
     * @todo：
     * 1、服务器端不支持；
     * 2、日期类型处理方案待定。
     */
    private setValueToUIState;
}
export { BefVariableManager };
