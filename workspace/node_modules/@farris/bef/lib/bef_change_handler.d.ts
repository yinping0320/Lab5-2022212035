import { Type } from '@angular/core';
import { Entity, EntityList, EntityCollection } from '@farris/devkit';
import { ChangeDetail } from './types';
/**
 * 处理服务器端变更
 */
declare class BefChangeHandler {
    /**
     * 构造函数
     */
    constructor();
    /**
     * 处理Bef变更集
     */
    handle(entityType: Type<Entity>, entityCollection: EntityCollection<Entity>, changeDetails: ChangeDetail[]): void;
    /**
     * 处理Bef变更集（批量）
     * @param entities
     */
    handleChangeDetails(entityType: Type<Entity>, entityList: EntityList<Entity> | EntityCollection<Entity>, changeDetails: ChangeDetail[]): void;
    /**
     * 处理Bef变更集（单条）
     */
    handleChangeDetail(entityType: Type<Entity>, entity: Entity, changeDetail: ChangeDetail): void;
    /**
     * 根据id获取实体，屏蔽EntityCollection和EntityList之间的差异
     */
    private getEntityById;
}
export { BefChangeHandler };
