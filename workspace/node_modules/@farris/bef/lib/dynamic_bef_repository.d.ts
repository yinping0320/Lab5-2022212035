import { Injector } from '@angular/core';
import { Entity } from '@farris/devkit';
import { Observable } from 'rxjs';
import { BefRepository } from './bef_repository';
declare class DynamicBefRepository<T extends Entity> extends BefRepository<T> {
    name: string;
    entityType: any;
    serverUri: string;
    apiUri: string;
    batchAppend(defaultValues: any[]): Observable<any>;
    constructor(injector: Injector, name: string, entityType: any, serverUri: string, apiUri: string);
}
export { DynamicBefRepository };
