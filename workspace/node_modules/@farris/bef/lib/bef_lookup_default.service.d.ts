import { Observable } from 'rxjs';
import { ILookupHttpService, RemoteParams, LookupGridResult } from '@farris/ui-lookup';
import { Repository } from '@farris/devkit';
import { FrameworkSessionService } from './framework_session_service';
/**
 * 帮助Rest取数服务
 */
export declare class BefLookupDefaultService implements ILookupHttpService {
    private sessionService;
    private befRepository;
    constructor(repository: Repository<any>, sessionService: FrameworkSessionService);
    createSession(url: string): Observable<{
        status: boolean;
        payload: string;
    }>;
    getData(url: string, params?: RemoteParams): Observable<LookupGridResult>;
}
