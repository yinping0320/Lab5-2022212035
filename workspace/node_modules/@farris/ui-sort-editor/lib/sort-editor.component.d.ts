import { OnInit, EventEmitter, Injector, ElementRef } from '@angular/core';
import { MessagerService } from '@farris/ui-messager';
import { SortCondition, SortType } from '@farris/ui-common/types';
import { DomSanitizer } from '@angular/platform-browser';
import { LocaleService } from '@farris/ui-locale';
import { PerfectScrollbarComponent } from '@farris/ui-perfect-scrollbar';
export declare class SortEditorComponent implements OnInit {
    private messagerService;
    private sanitizer;
    private injector;
    sortTypes: {
        label: string;
        value: SortType;
    }[];
    columnSizeData: number[];
    cls: string;
    fieldEditor: {
        type: string;
        options: any;
    };
    conditions: SortCondition[];
    columns: {
        label: string;
        value: string;
    }[];
    height: number;
    psRef: PerfectScrollbarComponent;
    tablebodybox: ElementRef;
    sortsChanged: EventEmitter<{}>;
    private newCondition;
    currentCondition: {
        index: number;
        condition: SortCondition;
    };
    localeService: LocaleService;
    constructor(messagerService: MessagerService, sanitizer: DomSanitizer, injector: Injector);
    ngOnInit(): void;
    private itemIntoView;
    selected($event: any): void;
    isSelected($event: number): boolean;
    insertCondition(index: number): void;
    removeCondition(index: number): void;
    onAdd(): void;
    onClear(): void;
    onMoveTop(): void;
    onMovePrev(): void;
    onMoveNext(): void;
    onMoveBottom(): void;
    canUse(): boolean;
    canMoveUp(): boolean;
    canMoveDown(): boolean;
    private changeConditionList;
}
