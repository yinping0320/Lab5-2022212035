import { ElementRef, SimpleChanges, OnChanges } from '@angular/core';
/**
 * 在表单dom中展示错误信息
 * 需要将formControl中的errors对象传入
 *
 * eg: 在姓名输入框下方展示验证信息
 *  将viewModel.form.name.errors对象传入
 *  <label class="k-form-field">
        <span>姓名</span>
        <input class="k-textbox" formControlName="name" />
        <validationMessage [errors]="viewModel.form.name.errors"></validationMessage>
    </label>
 */
declare class ValidationMessageComponent implements OnChanges {
    private ele;
    errors: any;
    type: string;
    errorMessage: string;
    showErrorMessage: boolean;
    constructor(ele: ElementRef);
    ngOnChanges(changes: SimpleChanges): void;
    private getErrorMessage;
}
export { ValidationMessageComponent };
