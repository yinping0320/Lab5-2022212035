import { OnInit, EventEmitter, Injector } from '@angular/core';
import { ControlValueAccessor } from '@angular/forms';
import { IdService } from '@farris/ui-common';
import { Radio } from './radio';
export declare class FarrisRadioGroupComponent implements ControlValueAccessor, OnInit {
    private idSer;
    private inject;
    tabIndex: number;
    data: Radio[];
    type: string;
    name: string;
    horizontal: boolean;
    disabled: boolean;
    value: any;
    textField: string;
    valueField: string;
    /** 数据中标识禁用的字段名称，默认为 disabled */
    disabledField: string;
    /** 列表中是否显示已禁用的项，默认 true */
    showDisabledItem: boolean;
    changeValue: EventEmitter<any>;
    private commonUtils;
    constructor(idSer: IdService, inject: Injector);
    ngOnInit(): void;
    clickHandler(item: any): void;
    _getText(item: any): any;
    _getValue(item: any): any;
    writeValue(value: any): void;
    registerOnChange(fn: () => void): void;
    registerOnTouched(fn: () => void): void;
    private controlChange;
    private controlTouch;
}
