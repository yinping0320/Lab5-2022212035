import { Injector, OnInit, ElementRef, AfterViewInit, OnChanges, SimpleChanges } from '@angular/core';
import { NgControl } from '@angular/forms';
export declare class TextareaAutoSizeDirective implements OnInit, AfterViewInit, OnChanges {
    private el;
    private ngControl;
    private injector;
    enable: boolean;
    maxHeight: number;
    private previousValue;
    private isUserResized;
    private textarea;
    private document;
    private documentElement;
    private minHeight;
    constructor(el: ElementRef, ngControl: NgControl, injector: Injector);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngAfterViewInit(): void;
    private init;
    onInput(): void;
    onValueChange(): void;
    private overflowOffset;
    private sizeToFit;
}
