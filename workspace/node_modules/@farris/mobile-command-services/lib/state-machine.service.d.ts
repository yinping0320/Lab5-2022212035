import { ViewModelContext } from '@farris/mobile-devkit';
/**
 * 状态机服务
 */
declare class StateMachineService {
    private viewModelContext;
    constructor(viewModelContext: ViewModelContext);
    /**
     * 执行状态迁移动作
     */
    executeAction(action: string): import("rxjs").Observable<never>;
}
export { StateMachineService };
