import { Observable } from 'rxjs';
import { Entity, ViewModelContext } from '@farris/mobile-devkit';
import { BaseDataService } from './base-data.service';
export declare class LoadDataService extends BaseDataService {
    constructor(viewModelContext: ViewModelContext);
    /**
     * 加载列表数据
     */
    loadForList(filters: any[], sorts: any[], size?: number, index?: number): Observable<Entity[]>;
    /**
     * 加载卡片数据
     */
    loadForCard(id: string): Observable<Entity>;
    /**
   * 加载进入子表后，未作任何操作之前的数据
   * @param path 子表路径
   */
    loadDataForChildCard(path: string): void;
}
