import { Observable } from 'rxjs';
import { ViewModelContext } from '@farris/mobile-devkit';
import { BaseDataService } from './base-data.service';
/**
 * 删除数据服务类
 */
export declare class RemoveDataService extends BaseDataService {
    constructor(viewModelContext: ViewModelContext);
    /**
     * 删除数据
     */
    removeById(id: string): Observable<any>;
    /**
     * 确认删除
     * @param id
     */
    private cancelChanges;
    /**
     * 批量删除数据
     */
    removeByIds(ids: string[]): Observable<boolean>;
    /**
     * 删除后代节点数据
     */
    removeByPathAndId(path: string, id: string): Observable<boolean>;
    removeByPathAndIdChanges(path: string, id: string): Observable<boolean>;
    /**
     * 批量删除后代节点数据
     */
    removeByPathAndIds(path: string, ids: string[]): Observable<boolean>;
    /**
     * 业务附件批量删除数据
     */
    removeByBusinessIds(url: string, ids: string[]): Observable<boolean>;
    /**
     * 业务附件删除后代节点数据
     */
    removeByBusinessPathAndId(url: string, path: string, id: string): Observable<boolean>;
    /**
     * 子表清除编辑与新增标记
     * @returns
     */
    ClearStatusAfterchildCardSave(): Observable<any>;
}
