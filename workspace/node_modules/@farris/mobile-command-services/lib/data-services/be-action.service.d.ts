import { Observable } from 'rxjs';
import { ViewModelContext, HttpMethod, HttpHeaders, HttpParams } from '@farris/mobile-devkit';
import { ResponseInfo } from '@farris/mobile-bef';
import { BaseDataService } from './base-data.service';
/**
 * BE动作调用服务
 */
declare class BeActionService extends BaseDataService {
    protected viewModelContext: ViewModelContext;
    /**
     * 构造函数
     */
    constructor(viewModelContext: ViewModelContext);
    /**
     * 执行自定义动作
     */
    invokeAction(actionUrl: string, httpMethod: HttpMethod, httpHeaders?: HttpHeaders, params?: HttpParams, body?: any): Observable<ResponseInfo>;
    /**
     * 调用自定义动作
     */
    private innerInvokeAction;
}
export { BeActionService };
