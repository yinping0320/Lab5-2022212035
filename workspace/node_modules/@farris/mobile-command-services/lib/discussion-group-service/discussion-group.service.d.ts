import { Observable } from 'rxjs';
import { HttpClient, ViewModelContext } from '@farris/mobile-devkit';
import { LoadingService } from '../ui-services/index';
export declare class DiscussionGroupService {
    private httpSvc;
    private viewModelContext;
    private loadingService;
    constructor(httpSvc: HttpClient, viewModelContext: ViewModelContext, loadingService: LoadingService);
    private readonly params;
    /**
     *
     * @param id 表单id
     * @param summary 消息描述
     * @param configId 消息配置id
     * @param text 评论内容
     * @param visibility 是否所有人可见(移动暂未支持)
     * @param parentId 是否是回复信息
     * @returns
     */
    addComment(id?: string, summary?: string, configId?: string, text?: string, visibility?: string, parentId?: string): Observable<any>;
    private buildAddCommentParam;
    /**
     *
     * @param id 表单id
     * @param configId 消息配置id
     * @param pageIndex
     * @param pageSize
     * @returns
     */
    queryComments(id: string, configId: string, pageIndex?: number | null, pageSize?: number | null): Observable<any>;
    private buildQueryCommentsUrl;
    /**
     * 回复赋值
     */
    setReplyUser(): void;
    queryFrequentAtUsers(pageIndex?: number | null, pageSize?: number | null): Observable<any>;
    queryAtUsers(user?: string, pageIndex?: number | null, pageSize?: number | null): Observable<any>;
}
