import { ViewModelContext } from '@farris/mobile-devkit';
import { JsBridgeService } from './js-bridge-service/index';
/**
 * 路由服务
 */
declare class RouterService {
    private jsBridgeService;
    /**
     * ViewModel上下文
     */
    private viewModelContext;
    /**
     * VueRouter实例
     */
    private router;
    /**
     * 构造函数
     */
    constructor(viewModelContext: ViewModelContext, router: any, jsBridgeService: JsBridgeService);
    /**
     * 路由跳转
     * @param path   路由路径
     * @param params 路由参数
     */
    navigate(path: string, queryParams?: any, backParams?: any, backPath?: string): boolean;
    private removeParams;
    private getsessionStorageSaveHistoryKey;
    private sessionStorageSaveHistory;
    /**
     * 带维度的路由跳转
     */
    navigateForRtc(path: string, queryParams?: any, backParams?: any, backPath?: string): void;
    /**
     * 纠正维度信息
     * @summary
     * 1、非运行时定制表单，widthDims=false, 维度为空；
     * 2、运行时定制表单：维度值不存在时，则纠正为public；
     */
    private correctRtcOptions;
    /**
     * 获取运行时定制表单地址
     */
    private getExtendPath;
    /**
     * 拼接url路径
     * @param path 路径
     * @param params 参数
     * @returns
     */
    private splicePath;
    private ParamsFromStringToObject;
    /**
   * 路由跳转
   * @param path   路由路径
   * @param params 路由参数
   */
    navigateReplace(path: string, queryParams?: any): void;
    /**
     * 后退
     */
    goBack(params: any): void;
    /**
     * 映射路由状态
     */
    mappingToUIState(): void;
}
/**
 * Token
 * @todo：临时方案，直接注入VueRouter实例。
 */
declare const ROUTER_INSTANCE_TOKEN = "@farris/mobile-command-services/ROUTER_INSTANCE_TOKEN";
export { RouterService, ROUTER_INSTANCE_TOKEN };
