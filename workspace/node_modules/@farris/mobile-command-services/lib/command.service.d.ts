import { Observable } from 'rxjs';
import { ViewModelContext } from '@farris/mobile-devkit';
/**
 * 命令服务
 */
declare class CommandService {
    private viewModelContext;
    /**
     * 构造函数
     */
    constructor(viewModelContext: ViewModelContext);
    /**
     * 执行命令
     * @param commandName 命令名称
     * @param viewModelId 视图模型id
     */
    execute(commandName: string, viewModelId?: string): Observable<any>;
}
export { CommandService };
