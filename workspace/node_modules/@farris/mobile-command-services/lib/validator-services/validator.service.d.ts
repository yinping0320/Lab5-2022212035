import { ViewModelContext } from '@farris/mobile-devkit';
import { Observable } from 'rxjs';
/**
 * 数据服务基类
 */
declare class ValidatorServices {
    private viewModelContext;
    constructor(viewModelContext: ViewModelContext);
    /**
     * 获取某个视图模型上得所有验证结果
     * @param viewModelId 视图模型id
     */
    validateFields(viewModelId?: string): any[];
    /**
     * 处理某个视图模型上得所有验证结果
     * @param viewModelId 视图模型id
     */
    handleValidateFields(viewModelId?: string): Observable<any>;
    /**
     * 获取某个字段的验证结果
     * @param name 字段名称
     * @param viewModelId 视图模型id
     */
    getFieldError(name: string, viewModelId?: string): any;
    /**
     * 重置一组校验状态 不传参数清除所有
     * @param fields name的数组
     * @param viewModelId 视图模型id
     */
    resetFieldsValidate(fields?: string[], viewModelId?: string): boolean;
}
export { ValidatorServices };
