import { ViewModelContext } from '@farris/mobile-devkit';
declare class EntityManipulationService {
    private viewModelContext;
    private readonly bindingData;
    constructor(viewModelContext: ViewModelContext);
    /**
     * 设置path对应的字段值
     */
    setPropValue(path: string[], value: any): any;
}
export { EntityManipulationService };
