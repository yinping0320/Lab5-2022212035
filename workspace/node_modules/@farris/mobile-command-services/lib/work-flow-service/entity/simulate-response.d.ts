import { ProcessDefinition } from "./execution-response";
import { FlowNodeInfo } from "./start-process-payload";
/**
 * 模拟提交审批、执行审批返回的实体结构
 */
export declare class SimulateResponse {
    needInteraction: boolean;
    selectTargetAble: boolean;
    simulationPaths: SimulationPath[];
    procDefs: ProcessDefinition[];
}
/**
 * 路径定义
 */
export declare class SimulationPath {
    id: string;
    name: string;
    default: boolean;
    nodeInfoList: FlowNodeInfo[];
}
