import { LocaleService } from '@farris/ui-locale';
import { ComponentFactoryResolver, Injector, ComponentRef, ApplicationRef, Renderer2, ElementRef, ChangeDetectorRef, OnDestroy } from '@angular/core';
import { Subscription } from 'rxjs';
import { DatagridComponent } from '@farris/ui-datagrid';
import { FilterRowPanelComponent } from './filter-row-panel.component';
import { DatagridFilterRowService } from '../datagrid-filter-row.service';
import { FilterRowEditorComponent } from './filter-row-editor.component';
export declare class FilterTextboxComponent extends FilterRowEditorComponent implements OnDestroy {
    private cfr;
    private injector;
    private app;
    dfrs: DatagridFilterRowService;
    private render;
    private cd;
    private localSer;
    column: any;
    disabled: boolean;
    delay: number;
    filterStr: string;
    readonly inputType: "text" | "number";
    gridInstance: DatagridComponent;
    filterPanelRef: ComponentRef<FilterRowPanelComponent>;
    filterPanelSubscription: Subscription;
    input: ElementRef;
    private ro;
    private zone;
    private el;
    constructor(cfr: ComponentFactoryResolver, injector: Injector, app: ApplicationRef, dfrs: DatagridFilterRowService, render: Renderer2, cd: ChangeDetectorRef, localSer: LocaleService);
    ngOnDestroy(): void;
    show(event: MouseEvent): void;
    close(): void;
    private initOperators;
    private updateFilterPanelPosition;
    onTextValueChange($event: any): void;
    clearValue(): void;
    initFilter(condition: any): void;
}
