import { OnInit, ElementRef, ChangeDetectorRef, Injector } from '@angular/core';
import { FilterRowEditorComponent } from './filter-row-editor.component';
import { DatagridFilterRowService } from '../datagrid-filter-row.service';
export declare class FilterCheckboxComponent extends FilterRowEditorComponent implements OnInit {
    dfrs: DatagridFilterRowService;
    private cd;
    private inject;
    i: number;
    checked: boolean;
    chk: ElementRef;
    constructor(dfrs: DatagridFilterRowService, cd: ChangeDetectorRef, inject: Injector);
    ngOnInit(): void;
    checkedChange($event: MouseEvent): void;
    clearValue(): void;
    initFilter(filterCondition: any): void;
}
