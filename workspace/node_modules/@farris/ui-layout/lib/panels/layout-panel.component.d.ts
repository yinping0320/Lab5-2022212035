import { OnInit, ElementRef, Renderer2, EventEmitter, SimpleChanges, OnChanges, NgZone } from '@angular/core';
import { AngularResizableDirective } from '@farris/ui-draggable';
import { LayoutComponent } from './../layout.component';
export declare class LayoutPanelComponent implements OnInit, OnChanges {
    el: ElementRef;
    private render2;
    private layout;
    private ngZone;
    region: string;
    height: number;
    width: number;
    minHeight: number;
    minWidth: number;
    maxHeight: number;
    maxWidth: number;
    title: string;
    iconCls: string;
    overflow: string;
    private _resizable;
    rzHandler: string;
    private bsColClass;
    split: boolean;
    showBorder: boolean;
    resizing: EventEmitter<{}>;
    resized: EventEmitter<{}>;
    panel: ElementRef;
    ngResizable: AngularResizableDirective;
    private setStyles;
    private getrzHandler;
    isCenterPanel(): boolean;
    constructor(el: ElementRef, render2: Renderer2, layout: LayoutComponent, ngZone: NgZone);
    ngOnChanges(changes: SimpleChanges): void;
    private getBsClass;
    private hasBsClass;
    private initSize;
    private initClass;
    ngOnInit(): void;
    onResizeStart($event: any): void;
    private resetFlex;
    resize(size: {
        width?: number;
        height?: number;
    }): void;
    onReizing($event: any): void;
    private updatePanelSize;
    onResized($event: any): void;
}
export declare class LayoutPanelTitleComponent {
    title: string;
    iconCls: string;
}
