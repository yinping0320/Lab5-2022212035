import { OnInit, ChangeDetectorRef, Injector, ElementRef, EventEmitter } from '@angular/core';
import { LocaleService } from '@farris/ui-locale';
export declare class BatchEditDialogComponent implements OnInit {
    private injector;
    private cdr;
    private localeService;
    header: ElementRef;
    inputs: any[];
    /**
     * grid列
     * @description 数据格式
     * ```json
     *  { id: "name", label: "姓名", dataType:"boolean" },
     * ```
     */
    fields: any[];
    /**
     * grid列对应的编辑器
     * @description 数据格式
     * ```json
     * {
     *   field: "sex",
     *   editor: {
     *      controlType: "textbox"
     *   }
     * }
     * ```
     */
    eidtors: any[];
    /**
     * 弹窗实例
     */
    dialogRef: any;
    title: string;
    rows: number;
    confirmed: EventEmitter<{}>;
    canceled: EventEmitter<{}>;
    constructor(injector: Injector, cdr: ChangeDetectorRef, localeService: LocaleService);
    ngOnInit(): void;
    cancel(event: any): void;
    confirm(event: any): void;
    /**
     * 新增字段编辑行
     */
    appendRow(): void;
    /**
     * 删除字段编辑行
     * @param event event
     * @param row row
     */
    removeRow(event: any, row: number): void;
    /**
     * 编辑字段发生变化
     * @param event event
     * @param row row
     */
    onFieldChanged(event: any, row: number): void;
    /**
     * 帮助选择数据后的回调
     * @param event event
     */
    onSelectRows(event: any): void;
    /**
     * 计算当前行可以选择的字段
     * @param appendField 要添加的字段
     */
    private computeFields;
    /**
     * 更新所有行可以选择的字段
     */
    private updateFields;
}
