import { ElementRef, Renderer2, NgZone, OnDestroy, AfterViewInit, OnChanges, SimpleChanges } from '@angular/core';
export declare class FAreaResponseDirective implements AfterViewInit, OnDestroy, OnChanges {
    private el;
    ngzone: NgZone;
    private render;
    private ro;
    private breakPoints;
    private clsNames;
    /**
     * 是否处理宽度不固定
     */
    autoResponse: boolean;
    /**
     * 只处理固定宽度；
     */
    enableResponse: boolean;
    constructor(el: ElementRef, ngzone: NgZone, render: Renderer2);
    ngAfterViewInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    private bindEvent;
    private supportResponse;
    private widthInterval;
    ngOnDestroy(): void;
}
