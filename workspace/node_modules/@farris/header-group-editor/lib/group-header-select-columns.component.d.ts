import { HeaderGroupEditorComponent } from './header-group-editor.component';
import { OnInit, ChangeDetectorRef } from '@angular/core';
export declare class SelectColumnsComponent implements OnInit {
    private cd;
    fields: any[];
    checkeds: any[];
    uncheckeds: any[];
    childs: any[];
    groupEditorRef: HeaderGroupEditorComponent;
    private checkedPropertyName;
    private hxl;
    constructor(cd: ChangeDetectorRef);
    ngOnInit(): void;
    private initCheckedProperty;
    checkedCols(f: any): void;
    isChecked(f: any): boolean;
    /** 在其他分组中的列，禁用选择
     * 仅当前分组中的列及候选列中的项可以选择
     */
    isDisabled(f: any): boolean;
}
