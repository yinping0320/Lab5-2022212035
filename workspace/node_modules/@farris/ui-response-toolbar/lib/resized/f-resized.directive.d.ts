import { EventEmitter, ElementRef, OnInit, AfterViewInit } from '@angular/core';
import { ResizedEvent } from './f-resized.events';
export declare class FResizedDirective implements OnInit, AfterViewInit {
    private readonly element;
    readonly resizedEvent: EventEmitter<ResizedEvent>;
    private oldWidth;
    private oldHeight;
    private resizeSubject;
    private resizeObservable;
    onResize(event: any): void;
    constructor(element: ElementRef);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    private doWithResize;
}
