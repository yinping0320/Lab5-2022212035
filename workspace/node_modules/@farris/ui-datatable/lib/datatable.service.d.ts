import { Subject } from 'rxjs';
export interface SelectRowArgs {
    rowIndex: number;
    rowData: any;
}
export declare class DataTableService {
    loadSuccess: Subject<{}>;
    /**
     * 选中行事件
     */
    selectedRow: Subject<SelectRowArgs>;
    unSelectedRow: Subject<SelectRowArgs>;
    selectedAll: Subject<{}>;
    dblClickRow: Subject<{}>;
    updateCheckAllStatus: Subject<{}>;
    constructor();
}
