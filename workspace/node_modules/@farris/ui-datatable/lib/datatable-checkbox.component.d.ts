import { OnInit, EventEmitter } from '@angular/core';
export declare class DTCheckboxComponent implements OnInit {
    checked: boolean;
    id: string;
    disabled: boolean;
    checkedChange: EventEmitter<{}>;
    constructor();
    ngOnInit(): void;
    handleClick(event: any): void;
}
