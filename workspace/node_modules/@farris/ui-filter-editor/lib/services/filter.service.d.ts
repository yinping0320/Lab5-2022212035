import { Subject } from 'rxjs';
import { FilterCondition } from '@farris/ui-common/types';
import { CommonUtils } from '@farris/ui-common';
import { LocaleService } from '@farris/ui-locale';
export declare class FilterService {
    conditionsChanged: Subject<FilterCondition[]>;
    commonUtils: CommonUtils;
    constructor(utils: CommonUtils);
    buildSqlWhere(c: any): string;
    isValid(conditions: FilterCondition[], localeService?: LocaleService): true | {
        bracket: {
            result: boolean;
            msg: any;
        };
        relation: {
            result: boolean;
            msg: any;
        };
        field: {
            result: boolean;
            msg: any;
        };
    };
}
