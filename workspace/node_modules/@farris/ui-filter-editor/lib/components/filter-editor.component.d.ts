import { OnInit, EventEmitter, Injector, ChangeDetectorRef } from '@angular/core';
import { DomSanitizer } from '@angular/platform-browser';
import { FilterService } from './../services/filter.service';
import { FilterCondition } from '@farris/ui-common/types';
import { LocaleService } from '@farris/ui-locale';
export declare class FilterEditorComponent implements OnInit {
    private sanitizer;
    private filterService;
    private injector;
    tabIndex: number;
    columns: any[];
    conditions: FilterCondition[];
    height: number;
    showCode: boolean;
    showSql: boolean;
    enableExpress: boolean;
    enableFrontExpress: boolean;
    fieldEditor: {
        type: string;
        options: {};
    };
    insertType: 'above' | 'below';
    /** 当值类型为 value 时是否显示按钮，默认不显示 */
    useCustomValue: boolean;
    showExpress: EventEmitter<{}>;
    cls: string;
    localeService: LocaleService;
    cd: ChangeDetectorRef;
    constructor(sanitizer: DomSanitizer, filterService: FilterService, injector: Injector);
    ngOnInit(): void;
    onTabChange(index: number): void;
    onShowExpress($event: any): void;
    validation(): true | {
        bracket: {
            result: boolean;
            msg: any;
        };
        relation: {
            result: boolean;
            msg: any;
        };
        field: {
            result: boolean;
            msg: any;
        };
    };
}
