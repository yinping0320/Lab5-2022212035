import { LocaleService } from '@farris/ui-locale';
import { OnInit, ElementRef, Renderer2, OnChanges, SimpleChanges, EventEmitter } from '@angular/core';
import { MessagerService } from '@farris/ui-messager';
import { FilterCondition, FilterRelation } from '@farris/ui-common/types';
import { FilterService } from './../services/filter.service';
export declare class FilterGridComponent implements OnInit, OnChanges {
    private el;
    private render;
    private filterService;
    private messagerService;
    LeftBracketData: string[];
    RighttBracketData: string[];
    relationData: {
        label: string;
        value: number;
    }[];
    operators: {
        label: string;
        value: number;
    }[];
    columnSizeData: number[];
    groupIcon: string;
    conditions: FilterCondition[];
    columns: {
        label: string;
        value: string;
    }[];
    enableExpress: boolean;
    enableFrontExpress: boolean;
    height: number;
    fieldEditor: any;
    /** 当值类型为 value 时是否显示按钮，默认不显示 */
    useCustomValue: boolean;
    insertType: 'above' | 'below';
    defulatRelation: FilterRelation;
    showExpress: EventEmitter<{}>;
    scrollContainer: HTMLElement;
    tablebodybox: ElementRef;
    currentCondition: {
        index: number;
        condition: FilterCondition;
    };
    private newCondition;
    localeService: LocaleService;
    constructor(el: ElementRef, render: Renderer2, filterService: FilterService, messagerService: MessagerService);
    ngOnChanges(changes: SimpleChanges): void;
    ngOnInit(): void;
    private initRelationData;
    private itemIntoView;
    insertCondition(index: number, $event?: MouseEvent): void;
    removeCondition(index: number): void;
    onAddFilter(): void;
    onClear(): void;
    onMoveTop(): void;
    onMovePrev(): void;
    onMoveNext(): void;
    onMoveBottom(): void;
    private changeConditionList;
    selected($event: any): void;
    isSelected($event: number): boolean;
    showExpressDialog($event: any, condition: any, value: any): void;
    onComboSelectChange($event: any, condition: any): void;
    validation(): true | {
        bracket: {
            result: boolean;
            msg: any;
        };
        relation: {
            result: boolean;
            msg: any;
        };
        field: {
            result: boolean;
            msg: any;
        };
    };
    trackByValue: (index: number, row: any) => any;
}
