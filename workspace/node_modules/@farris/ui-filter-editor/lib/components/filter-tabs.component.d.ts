import { OnInit, EventEmitter } from '@angular/core';
import { FilterCondition } from '@farris/ui-common/types';
export declare class FilterTabsComponent implements OnInit {
    columns: any[];
    conditions: FilterCondition[];
    height: number;
    showCode: boolean;
    showSql: boolean;
    fieldEditor: {
        type: string;
    };
    selectTabChange: EventEmitter<{}>;
    enableExpress: boolean;
    enableFrontExpress: boolean;
    showExpress: EventEmitter<{}>;
    insertType: 'above' | 'below';
    /** 当值类型为 value 时是否显示按钮，默认不显示 */
    useCustomValue: boolean;
    currentTabIndex: number;
    constructor();
    ngOnInit(): void;
    showTab(index: number): void;
    showExpressDialog($event: any): void;
}
