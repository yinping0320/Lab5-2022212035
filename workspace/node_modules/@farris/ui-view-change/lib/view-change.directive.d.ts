import { ElementRef, OnInit, OnDestroy, Renderer2 } from '@angular/core';
import { ViewChangeService } from './view-change.service';
export declare class FarrisViewItemDirective implements OnInit, OnDestroy {
    private elementRef;
    private viewChangeService;
    private render;
    /**使用方式
     * <div [fViewItem]="'type11'" [fViewGroupId]="'group2'">
         视图的内容区域
       </div>
     */
    viewItemType: string;
    fViewGroupId: string;
    private viewChangeManageSer;
    private guid;
    private hasInit;
    /**
     * 只要执行过初始方法，就调用。
     * 保证只执行一次
     */
    private cancelSignal;
    constructor(elementRef: ElementRef, viewChangeService: ViewChangeService, render: Renderer2);
    ngOnInit(): void;
    /**
     * 标记这个方法
     * @param guid
     * @returns
     */
    viewChangeOnInit(newGuid: string): void;
    ngOnDestroy(): void;
}
