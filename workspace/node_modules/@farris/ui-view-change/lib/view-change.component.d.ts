import { OnInit, EventEmitter, ElementRef, Renderer2, AfterViewInit, Injector, OnDestroy } from '@angular/core';
import { ViewChangeService } from './view-change.service';
export declare class ViewChangeComponent implements OnInit, AfterViewInit, OnDestroy {
    private _renderer;
    private viewChangeService;
    private inject;
    toolbarData: any;
    viewType: string;
    defaultType: string;
    viewGroupId: string;
    activeTypeItem: any;
    toolTypeChange: EventEmitter<{}>;
    typelist: ElementRef;
    viewchangewrapper: ElementRef;
    private guid;
    private ngZone;
    private ro;
    private viewChangeManageSer;
    constructor(_renderer: Renderer2, viewChangeService: ViewChangeService, inject: Injector);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    ngOnDestroy(): void;
    listItemClick(item: any): void;
    dropdownMouseEnter(): void;
    dropdownMouseLeave(): void;
    /**
     * 解决卡片和列表两个菜单同时打开，同步数据时候，当前的标签显示不对的问题
     */
    private observerElement;
}
