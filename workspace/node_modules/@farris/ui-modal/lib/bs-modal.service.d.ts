import { ComponentRef, TemplateRef, EventEmitter, RendererFactory2, NgZone } from '@angular/core';
import { ModalBackdropComponent } from './modal-backdrop.component';
import { ModalContainerComponent } from './modal-container.component';
import { ModalOptions } from './modal-options.class';
import { BsModalRef } from './bs-modal-ref.service';
import { ComponentLoaderFactory, ComponentLoader } from '@farris/ui-modal/component-loader';
export declare class BsModalService {
    private clf;
    ngZone: NgZone;
    version: string;
    config: ModalOptions;
    onShow: EventEmitter<any>;
    onShown: EventEmitter<any>;
    onHide: EventEmitter<any>;
    onHidden: EventEmitter<any>;
    protected isBodyOverflowing: boolean;
    protected originalBodyPadding: number;
    protected scrollbarWidth: number;
    protected backdropRef: ComponentRef<ModalBackdropComponent>;
    private _backdropLoader;
    private modalsCount;
    private lastDismissReason;
    private loaders;
    private _renderer;
    private _documentMouseDownHandler;
    private commonUtils;
    private modellessCount;
    constructor(rendererFactory: RendererFactory2, clf: ComponentLoaderFactory, ngZone: NgZone);
    private clearDocumentEvents;
    /** Shows a modal */
    show(content: string | TemplateRef<any> | any, config?: ModalOptions): BsModalRef;
    private isModeless;
    hide(level: number): void;
    _showBackdrop(): void;
    _hideBackdrop(): void;
    private updateZindex;
    _showModal(content: any): BsModalRef;
    /**
     * 增加参数，解决，在当前模态窗口内先弹出非模态再弹出Dialog时，点击外层
     * @param e
     * @param curModalisModeless
     * @returns
     */
    _onMouseDown(e: any, curModalisModeless?: any): boolean;
    _hideModal(level: number): void;
    getModalsCount(): number;
    getCurrentModalContainer(): ComponentLoader<ModalContainerComponent>;
    setDismissReason(reason: string): void;
    removeBackdrop(): void;
    /** AFTER PR MERGE MODAL.COMPONENT WILL BE USING THIS CODE */
    /** Scroll bar tricks */
    /** @internal */
    checkScrollbar(): void;
    setScrollbar(): void;
    private resetScrollbar;
    private getScrollbarWidth;
    private _createLoaders;
    private removeLoaders;
    private copyEvent;
    private checkDialogSize;
    closeUpdateModelessCountByModal(curIsModeless: any): void;
}
