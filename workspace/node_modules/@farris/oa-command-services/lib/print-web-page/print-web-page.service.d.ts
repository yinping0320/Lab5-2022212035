import { MessagerService } from '@farris/ui-messager';
import { templateType } from "./entity";
export declare class PrintWebPageService {
    private msgSer;
    private currentTemplate;
    constructor(msgSer: MessagerService);
    printWebPage(tempType?: templateType): void;
}
