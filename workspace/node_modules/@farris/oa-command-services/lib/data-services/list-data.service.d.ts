import { MessagerService } from '@farris/ui-messager';
import { NotifyService } from '@farris/ui-notify';
import { Observable } from "rxjs";
export declare class OAListDataService {
    private msgService;
    private notifySer;
    constructor(msgService: MessagerService, notifySer: NotifyService);
    filterAndRemoveRows(ids: string[], ifSave?: boolean | string): Observable<any>;
}
