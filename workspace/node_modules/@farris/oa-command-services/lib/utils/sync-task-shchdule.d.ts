import { Subject } from "rxjs";
export declare enum ScheduleResultType {
    Finally = "finally",
    Progress = "progress",
    Error = "error"
}
export interface ScheduleResult {
    type: ScheduleResultType;
    recentResult: any;
    progressIndex: Number;
    resultStack: Array<any>;
}
export declare class SyncTaskSchdule {
    constructor();
    private actIndex;
    private resultStack;
    private status;
    private taskResult;
    private taskStack;
    propTask(fun: Function, params: any[], context: any, key: string): Subject<ScheduleResult>;
    private actTask;
}
