import { Observable } from 'rxjs';
import { ValidationRule } from './types';
/**
 * 验证器工厂
 */
declare class ValidatorFactory {
    /**
     * 创建适配器
     */
    static create(validRules: ValidationRule | ValidationRule[]): any;
    private static initValidRuleFn;
    /**
     * 支持异步自定义函数
     * 遍历生成的校验方法对当前值进行校验，当发现错误就返回校验结果
     * 遍历完成没有错误则返回校验通过结果
     * @param validatorFn 校验方法
     * @param value 当前值
     */
    static executeValidator(validatorFn: any, value: any, context?: any, options?: any): Observable<any>;
    /**
     * 不支持异步自定义函数
     * 遍历生成的校验方法对当前值进行校验，当发现错误就返回校验结果
     * 遍历完成没有错误则返回校验通过结果
     * @param validatorFn 校验方法
     * @param value 当前值
     */
    static noSupportAsynExecuteValidator(validatorFn: any, value: any, context?: any, options?: any): any;
}
export { ValidatorFactory };
