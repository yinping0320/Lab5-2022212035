export declare const ENTITY_META = "EntityMeta";
/**
 * 实体描述
 */
export interface EntityMetadata {
    /**
     * 原始编号,对应schema中的code
     */
    originalCode: string;
    /**
     * 后端转换完之后的编号,对应schema中的label
     */
    nodeCode: string;
}
/**
 * 实体装饰器
 */
export interface EntityMetaDecorator {
    (obj?: EntityMetadata): any;
    new (obj?: EntityMetadata): any;
}
/**
 * 实体装饰器
 */
export declare function EntityMeta(options: EntityMetadata): any;
