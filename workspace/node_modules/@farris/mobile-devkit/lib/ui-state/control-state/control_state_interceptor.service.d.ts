import { Injector } from "../../core";
import { InterceptContext } from "./control_state_interceptor";
import { Observable } from "rxjs";
/**
 * 状态拦截器服务
 */
export declare class ControlStateInterceptorService {
    private injector;
    private controlInterceptors;
    private result;
    private processContext;
    constructor(injector: Injector);
    init(): Observable<any>;
    intercept(interceptContext: InterceptContext): void;
    private getProcessContext;
}
