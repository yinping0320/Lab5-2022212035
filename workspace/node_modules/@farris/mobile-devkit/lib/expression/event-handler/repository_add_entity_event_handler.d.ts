import { Expression } from "../expression";
import { EventHandler } from "./event_handler";
export declare class RepositoryAddEntityEventHandler extends EventHandler {
    /**
     * 获取相关表达式
     * @param event event
     * @description 不支持主表直接依赖子表属性的情况，只能聚合子表的字段
     */
    filter(event: Expression.EventArgs): any;
    /**
     * 发布事件
     * @param event event
     */
    dispatch(event: Expression.EventArgs): void;
    /**
     * 获取子表事件行
     * @param paths
     * @param event
     * @returns
     */
    getCurrentRowByEvent(paths: string[], event: Expression.EventArgs): null | {
        [prop: string]: any;
    };
}
