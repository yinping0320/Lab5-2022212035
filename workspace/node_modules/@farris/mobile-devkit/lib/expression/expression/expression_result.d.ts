import { ViewModelContext } from "../../view-model/index";
/**
 * 表达式计算结果
 */
export declare class ExpressionResult {
    private viewModelContext;
    constructor(viewModelContext: ViewModelContext);
    /**
     * 更新表达式的值
     * @param expressionId 表达式id
     * @param result
     */
    set(expressionId: string, result: any): void;
}
