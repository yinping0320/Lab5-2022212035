import { Observable } from "rxjs";
import { ViewModelContext } from '../../view-model/index';
import { Expression } from "./types";
/**
 * 事件监听器主要有以下几种类型的事件：
 * 1、值变化
 * 2、行切换
 *    行切换使用场景为需要为数据计算依赖当前行时
 * 3、数据加载
 */
export declare class ExpressionEventEmitter {
    private viewModelContext;
    private onEvent;
    private events;
    private listeners;
    constructor(viewModelContext: ViewModelContext);
    attach(): Observable<Array<Expression.EventArgs>>;
}
