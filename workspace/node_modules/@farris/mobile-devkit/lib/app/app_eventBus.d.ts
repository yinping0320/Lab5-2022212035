declare class AppEventBus {
    /**
  * eventBus
  */
    private eventBus;
    private subscriptionsMap;
    /**
     * 构造函数
     */
    constructor();
    triggerEvent(event: any): void;
    subscribe(componentId: string, func: Function, funcError?: Function): void;
    unsubscribe(componentId: string): void;
    /**
     * 清除注册得指定key名监听器
     * @param keyStr 监听器key 默认值为listview上注册得
     * @returns
     */
    unsubscribeAllListview(keyStr?: string): void;
}
export { AppEventBus };
