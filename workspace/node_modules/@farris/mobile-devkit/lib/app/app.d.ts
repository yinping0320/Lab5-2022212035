import { AppContext } from './app_context';
import { ViewModel, ViewModelOptions } from '../view-model/index';
declare class App {
    context: AppContext;
    /**
     * 构造函数
     */
    constructor(options: any);
    /**
     * 启用ViewModel
     */
    createViewModel(options: ViewModelOptions): ViewModel;
}
export { App };
