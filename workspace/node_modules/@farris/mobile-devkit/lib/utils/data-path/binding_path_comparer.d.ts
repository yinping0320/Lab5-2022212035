/**
 * BindingPath比较器
 */
declare class BindingPathComparer {
    /**
     * 是否相等
     */
    static isEqual(srcPath: string | string[], dstPath: string | string[]): boolean;
    /**
     * 是否是父路径
     */
    static isParent(childPath: string | string[], parentPath: string | string[]): boolean;
    /**
     * 是否是祖先路径
     */
    static isAncestor(descendantPath: string | string[], ancestorPath: string | string[]): boolean;
}
export { BindingPathComparer };
