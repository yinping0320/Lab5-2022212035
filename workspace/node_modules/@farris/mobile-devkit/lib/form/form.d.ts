import { ViewModelContext } from '../view-model/index';
import { Subject } from 'rxjs';
/**
 * Form抽象类
 */
declare abstract class Form {
    /**
     * 表单控件配置
     */
    private formControlConfigs;
    /**
     * ViewModel上下文
     */
    private viewModelContext;
    private validateformControls;
    private validateformControlPathMap;
    changes: Subject<any>;
    /**
     * 构造函数
     */
    constructor(viewModelContext: ViewModelContext);
    /**
     * 初始化
     */
    init(): void;
    /**
     * 使用不包含绝对路径的/或者/子表/的路径信息获取对应的formControlConfig的name属性与bindingPath并且返回该路径的formControl
     * @param path 路径信息
     * @returns
     */
    getFormValueByBindPath(path: any): {
        form_name: {};
        name: string;
        bindingPath: string;
    };
    /**
     *
     * @returns 获取当前表单上的存在校验字段的集合
     */
    getValidateformControls(): string[];
    setValidateformControls(name: any): void;
    getValidateformControlPathMap(): Map<string, string>;
    setValidateformControlPathMap(name: any, value: any): void;
    /**
     * 使用formControlConfig的bindingPath路径信息与对应formControlConfig的name信息储存起来作为验证信息在form上的记录
     * @param path formControlConfig的bindingPath路径信息
     * @param name 对应formContro的name信息
     */
    addValidate(path: any, name: any): void;
    /**
     * 全部校验
     *  formControlConfigs 上所有的formControl的存在方法调用一遍 将错误信息集中返回
     */
    validateFields(): any[];
    /**
     * 不支持自定义异步全部校验
     *  formControlConfigs 上所有的formControl的存在方法调用一遍 将错误信息集中返回
     */
    noSupportAsynValidateFields(): any[];
    /**
     * 获取某一个得校验错误信息
     * @param name 属性名称
     */
    getFieldError(name: string): {};
    /**
   * 根据form元数据中的path获取某一个得校验错误信息
   * @param path 属性名称数组
   */
    getFieldErrorByPath(path: string[]): any;
    /**
     * 清除一组字段验证状态
     * @param fields 字段的数组
     */
    resetFieldsValidate(fields?: string[]): boolean;
    /**
     * 创建FormControls
     */
    private createFormControls;
    /**
     * 收集元数据
     */
    private collectMetadatas;
    getEntityValueChangingListeners(): {
        [property: string]: string;
    };
    getEntityValueChangedListeners(): {
        [property: string]: string;
    };
}
export { Form };
