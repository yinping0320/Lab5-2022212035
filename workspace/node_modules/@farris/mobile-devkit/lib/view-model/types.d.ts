import { StaticProvider } from '../core/index';
import { ViewModel } from './view_model';
declare class ViewModelOptions {
    id: string;
    /**
     * 应用providers
     */
    providers: StaticProvider[];
    /**
     * 父ViewModel
     */
    parent?: ViewModel;
}
export { ViewModelOptions };
