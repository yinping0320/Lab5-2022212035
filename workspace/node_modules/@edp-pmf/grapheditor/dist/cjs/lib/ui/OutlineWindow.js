"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.OutlineWindow = void 0;
var ActionKeys_1 = require("../action/ActionKeys");
var mxgraph_ts_1 = require("@edp-pmf/mxgraph-ts");
var PmfEvents_1 = require("../util/PmfEvents");
/**
 * mxgraph弹窗类
 */
var OutlineWindow = /** @class */ (function () {
    function OutlineWindow(editorUi, x, y, w, h) {
        var _this = this;
        var graph = editorUi.graph;
        var div = document.createElement('div');
        div.style.position = 'absolute';
        div.style.width = '100%';
        div.style.height = '100%';
        div.style.border = '1px solid whiteSmoke';
        div.style.overflow = 'hidden';
        this.window = new mxgraph_ts_1.mxWindow(mxgraph_ts_1.mxResources.get('outline'), div, x, y, w, h, true, true);
        this.window.destroyOnClose = false;
        this.window.setMaximizable(false);
        this.window.setResizable(true);
        this.window.setClosable(true);
        this.window.setVisible(true);
        this.window.setLocation = function (x, y) {
            var iw = window.innerWidth || document.documentElement.clientWidth || document.body.clientWidth;
            var ih = window.innerHeight || document.documentElement.clientHeight || document.body.clientHeight;
            x = Math.max(0, Math.min(x, iw - _this.window.table.clientWidth));
            y = Math.max(0, Math.min(y, ih - _this.window.table.clientHeight - 48));
            if (_this.window.getX() != x || _this.window.getY() != y) {
                mxgraph_ts_1.mxWindow.prototype.setLocation.apply(_this.window, [x, y]);
            }
        };
        mxgraph_ts_1.mxEvent.addListener(window, 'resize', function () {
            var x = _this.window.getX();
            var y = _this.window.getY();
            _this.window.setLocation(x, y);
        });
        var outline = new mxgraph_ts_1.mxOutline(editorUi.graph);
        outline.border = 20;
        mxgraph_ts_1.mxEvent.addListener(window, 'resize', function () {
            outline.update();
        });
        editorUi.addListener('pageFormatChanged', function () {
            outline.update();
        });
        editorUi.addListener(PmfEvents_1.PmfEvents.ResizeGraphView, function () {
            outline.update();
        });
        this.window.addListener(mxgraph_ts_1.mxEvent.RESIZE, function () {
            outline.update(false);
            outline.outline.sizeDidChange();
        });
        this.window.addListener(mxgraph_ts_1.mxEvent.SHOW, function () {
            outline.suspended = false;
            outline.outline.refresh();
            outline.update();
        });
        this.window.addListener(mxgraph_ts_1.mxEvent.HIDE, function () {
            outline.suspended = true;
        });
        this.window.addListener(mxgraph_ts_1.mxEvent.NORMALIZE, function () {
            outline.suspended = false;
            outline.update();
        });
        this.window.addListener(mxgraph_ts_1.mxEvent.MINIMIZE, function () {
            outline.suspended = true;
        });
        var outlineCreateGraph = outline.createGraph;
        outline.createGraph = function (container) {
            var g = outlineCreateGraph.apply(outline, [container]);
            g.gridEnabled = false;
            g.pageScale = graph.pageScale;
            g.pageFormat = graph.pageFormat;
            g.background = graph.background;
            g.pageVisible = graph.pageVisible;
            var current = mxgraph_ts_1.mxUtils.getCurrentStyle(graph.container);
            div.style.backgroundColor = current.backgroundColor;
            return g;
        };
        var update = function () {
            outline.outline.pageScale = graph.pageScale;
            outline.outline.pageFormat = graph.pageFormat;
            outline.outline.pageVisible = graph.pageVisible;
            outline.outline.background = graph.background;
            var current = mxgraph_ts_1.mxUtils.getCurrentStyle(graph.container);
            div.style.backgroundColor = current.backgroundColor;
            if (graph.view.backgroundPageShape != null && outline.outline.view.backgroundPageShape != null) {
                outline.outline.view.backgroundPageShape.fill = graph.view.backgroundPageShape.fill;
            }
            outline.outline.refresh();
        };
        outline.init(div);
        editorUi.addListener('resetGraphView', update);
        editorUi.addListener('pageFormatChanged', update);
        editorUi.addListener('backgroundColorChanged', update);
        editorUi.addListener('backgroundImageChanged', update);
        editorUi.addListener('pageViewChanged', function () {
            update();
            outline.update(true);
        });
        if (outline.outline.dialect == mxgraph_ts_1.mxConstants.DIALECT_SVG) {
            var zoomInAction_1 = editorUi.actions.getAction(ActionKeys_1.ActionKeys.pmf_zoomIn);
            var zoomOutAction_1 = editorUi.actions.getAction(ActionKeys_1.ActionKeys.pmf_zoomOut);
            mxgraph_ts_1.mxEvent.addMouseWheelListener(function (evt, up) {
                var outlineWheel = false;
                var source = mxgraph_ts_1.mxEvent.getSource(evt);
                while (source != null) {
                    if (source == outline.outline.view.canvas.ownerSVGElement) {
                        outlineWheel = true;
                        break;
                    }
                    source = source.parentNode;
                }
                if (outlineWheel) {
                    if (up) {
                        zoomInAction_1.execute();
                    }
                    else {
                        zoomOutAction_1.execute();
                    }
                    mxgraph_ts_1.mxEvent.consume(evt);
                }
            });
        }
    }
    return OutlineWindow;
}());
exports.OutlineWindow = OutlineWindow;
//# sourceMappingURL=OutlineWindow.js.map