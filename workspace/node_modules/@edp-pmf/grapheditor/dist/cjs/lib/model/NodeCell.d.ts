import { BPMNShape, FlowNodeState, FlowNode } from "@edp-pmf/bpmn-model";
import { Cell } from "./Cell";
import { EdgeCell } from "./EdgeCell";
/**
 * 节点元素
 */
export declare abstract class NodeCell extends Cell {
    edge: boolean;
    vertex: boolean;
    flowElement: FlowNode;
    diagramElement: BPMNShape;
    edges: EdgeCell[];
    /**
     * 不同状态下的节点图片
     */
    abstract imgUrls: {
        /**
         * 默认
         */
        [FlowNodeState.Default]: string;
        [state: string]: string;
    };
    unbindBpmnModel(): any;
    createFlowElement(flowElt?: FlowNode): FlowNode;
    createDiagramElement(diagElt?: BPMNShape): BPMNShape;
}
