import { BpmnModel } from "../BpmnModel";
import { ExtensionElement } from "../ExtensionElement";
export declare abstract class Participant extends ExtensionElement {
    id: string;
    name: string;
    constructor(model: BpmnModel);
    ConvertToJsonObject(): any;
    LoadFromJsonObject(obj: {}): void;
    clone(): Participant;
}
