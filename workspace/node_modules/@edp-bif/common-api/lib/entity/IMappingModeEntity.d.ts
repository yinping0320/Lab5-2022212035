import { IJsonConvertable } from "../ref";
import { MappingMode } from './MappingMode';
/**
 * 映射方式实体接口。
 */
export interface IMappingModeEntity extends IJsonConvertable {
    /**
     * 映射方式。
     */
    mappingMode: MappingMode;
    /**
     * 映射方式内容。
     */
    content: string;
    /**
     * 映射规则内容。
     */
    mappingRuleContent: string;
    /**
     * 自定义映射构件内容。
     */
    componentContent: string;
    /**
     * 视图对象映射动作内容。
     */
    voActionContent: string;
}
