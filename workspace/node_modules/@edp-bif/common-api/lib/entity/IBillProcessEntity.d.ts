import { IEntity } from '../ref';
import { ISlimBillProcessEntity } from '../slim-entity/ISlimBillProcessEntity';
/**
 * 单据流实体基接口。
 */
export interface IBillProcessEntity extends IEntity, ISlimBillProcessEntity {
    /**
     * 单据流名称。
     */
    name: string;
    /**
     * 单据流描述。
     */
    description: string;
    /**
     * 持久化版本。
     */
    persistenceVersion: string;
    /**
     * 版本号。
     */
    version: number;
    /**
     * 调用类型。
     */
    callType: string;
    /**
     * 上游单据应用编号。
     */
    upBillAppCode: string;
    /**
     * 上游单据服务单元编号。
     */
    upBillSuCode: string;
    /**
     * 上游单据业务对象标识。
     */
    upBillBoId: string;
    /**
     * 上游单据业务实体元数据标识。
     */
    upBillBeMetadataId: string;
    /**
     * 下游单据应用编号。
     */
    downBillAppCode: string;
    /**
     * 下游单据服务单元编号。
     */
    downBillSuCode: string;
    /**
     * 下游单据业务对象标识。
     */
    downBillBoId: string;
    /**
     * 下游单据业务实体元数据标识。
     */
    downBillBeMetadataId: string;
    /**
     * 业务动作标识。
     */
    bizActionId: string;
    /**
     * 基础流程标识。
     */
    originId: string;
    /**
     * 根流程标识。
     */
    rootId: string;
    /**
     * 标识路径。
     */
    idPath: string;
    /**
     * 版本标志。
     */
    versionTag: string;
    /**
     * 上游单据业务实体元数据根标识。
     */
    upBillBeMetadataRootId: string;
    /**
     * 上游单据业务实体元数据父标识。
     */
    upBillBeMetadataParentId: string;
    /**
     * 上游单据业务实体元数据标识路径。
     */
    upBillBeMetadataIdPath: string;
    /**
     * 上游单据业务实体根标识。
     */
    upBillBeRootId: string;
    /**
     * 上游单据业务实体父标识。
     */
    upBillBeParentId: string;
    /**
     * 上游单据业务实体标识路径。
     */
    upBillBeIdPath: string;
    /**
     * 上游单据视图对象元数据根标识。
     */
    upBillVoMetadataRootId: string;
    /**
     * 上游单据视图对象元数据父标识。
     */
    upBillVoMetadataParentId: string;
    /**
     * 上游单据视图对象元数据标识路径。
     */
    upBillVoMetadataIdPath: string;
    /**
     * 上游单据视图对象根标识。
     */
    upBillVoRootId: string;
    /**
     * 上游单据视图对象父标识。
     */
    upBillVoParentId: string;
    /**
     * 上游单据视图对象标识路径。
     */
    upBillVoIdPath: string;
    /**
     * 下游单据业务实体元数据根标识。
     */
    downBillBeMetadataRootId: string;
    /**
     * 下游单据业务实体元数据父标识。
     */
    downBillBeMetadataParentId: string;
    /**
     * 下游单据业务实体元数据标识路径。
     */
    downBillBeMetadataIdPath: string;
    /**
     * 下游单据业务实体根标识。
     */
    downBillBeRootId: string;
    /**
     * 下游单据业务实体父标识。
     */
    downBillBeParentId: string;
    /**
     * 下游单据业务实体标识路径。
     */
    downBillBeIdPath: string;
    /**
     * 下游单据视图对象元数据根标识。
     */
    downBillVoMetadataRootId: string;
    /**
     * 下游单据视图对象元数据父标识。
     */
    downBillVoMetadataParentId: string;
    /**
     * 下游单据视图对象元数据标识路径。
     */
    downBillVoMetadataIdPath: string;
    /**
     * 下游单据视图对象根标识。
     */
    downBillVoRootId: string;
    /**
     * 下游单据视图对象父标识。
     */
    downBillVoParentId: string;
    /**
     * 下游单据视图对象标识路径。
     */
    downBillVoIdPath: string;
    colI18nValues: any;
}
