import { IJsonConvertable } from '../ref';
import { SaveMode } from './SaveMode';
/**
 * 保存模式实体接口。
 */
export interface ISaveModeEntity extends IJsonConvertable {
    /**
     * 保存模式。
     */
    saveMode: SaveMode;
    /**
     * 保存方式内容。
     */
    content: string;
    /**
     * 自定义保存构件内容。
     */
    componentContent: string;
    /**
     * 视图对象保存动作内容。
     */
    voActionContent: string;
    /**
     * 推送任务中心。
     */
    taskCenterContent: string;
}
