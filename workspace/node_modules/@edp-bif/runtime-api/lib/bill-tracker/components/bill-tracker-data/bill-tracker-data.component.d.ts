import { OnInit, ComponentFactoryResolver, Injector, AfterViewInit, ViewContainerRef, NgModuleFactoryLoader, ChangeDetectorRef, ElementRef } from '@angular/core';
import { BillTrackerDataService } from '../../services/bill-tracker-data.service';
import { GetMainEntityDataArgs } from '../../args/GetMainEntityDataArgs';
import { LoadingService } from '@farris/ui-loading';
import { MessagerService } from '@farris/ui-messager';
import { EntityDataResult } from '../../args/EntityDataResult';
import { BillInfo } from '../../../entity/tracking/BillInfo';
import { NotifyService } from '@farris/ui-notify';
import { BsModalService } from '@farris/ui-modal';
import { BillTrackerDataInfoService } from '../../services/bill-tracker-data-info.service';
import { BillTrackerLocalePipe } from '../../pipe/bill-tracker-locale.pipe';
import { BillDataInfo } from '../../../entity/tracking/BillDataInfo';
export declare class BillTrackerDataComponent implements OnInit, AfterViewInit {
    notify: NotifyService;
    messager: MessagerService;
    loading: LoadingService;
    modal: BsModalService;
    dataService: BillTrackerDataService;
    inj: Injector;
    cfr: ComponentFactoryResolver;
    cdf: ChangeDetectorRef;
    loader: NgModuleFactoryLoader;
    dataRegion: ViewContainerRef;
    dataInfoService: BillTrackerDataInfoService;
    localePipe: BillTrackerLocalePipe;
    localeId: string;
    vcr: ViewContainerRef;
    er: ElementRef<HTMLDivElement>;
    constructor(notify: NotifyService, messager: MessagerService, loading: LoadingService, modal: BsModalService, dataService: BillTrackerDataService, inj: Injector, cfr: ComponentFactoryResolver, cdf: ChangeDetectorRef, loader: NgModuleFactoryLoader, localeId: string, vcr: ViewContainerRef, er: ElementRef);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    clearDataRegion(): void;
    showDataGrid(args: GetMainEntityDataArgs, billInfo?: BillInfo, billDataInfo?: BillDataInfo, res?: EntityDataResult): void;
    /**
     * 打开通用表单
     * @param args
     * @param billInfo
     * @param res
     */
    private showGeneralForm;
    /**
     * 打开自定义表单
     * @param args
     * @param billInfo
     */
    private showCustomForm;
    /**
   * 创建通用表组件
   * @param res
   * @param billInfo
   */
    createDataComp(res: EntityDataResult, billInfo: BillInfo, billDataInfo: BillDataInfo): void;
    /**
   * 创建通用表组件
   * @param res
   * @param billInfo
   */
    createNewDataComp(res: EntityDataResult, billInfo?: BillInfo, billDataInfo?: BillDataInfo): void;
}
