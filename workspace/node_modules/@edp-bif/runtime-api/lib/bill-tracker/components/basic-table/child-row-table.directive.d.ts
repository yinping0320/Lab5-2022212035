import { ViewContainerRef, ComponentFactoryResolver, EventEmitter, ChangeDetectorRef } from '@angular/core';
import { BillDataRow, BillDataTable, BillSchemaTable } from '@edp-aif/common-api';
import { BillInfo } from '../../../entity/tracking/BillInfo';
export declare class ChildRowTableDirective {
    private cfr;
    vcRef: ViewContainerRef;
    cdr: ChangeDetectorRef;
    billInfo: BillInfo;
    colspan: number;
    parentRow: BillDataRow;
    parentSchemaTable: BillSchemaTable;
    parentDataTable: BillDataTable;
    private _expanded;
    expanded: boolean;
    hasChildren: EventEmitter<boolean>;
    constructor(cfr: ComponentFactoryResolver, vcRef: ViewContainerRef, cdr: ChangeDetectorRef);
    showChildTable(): void;
    clearChildTable(): void;
}
