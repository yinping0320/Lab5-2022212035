import { mxCell } from "../../ref/mxgraph";
import { BillDataInfo } from '../../entity/tracking/BillDataInfo';
import { DownBillInfoResult } from '../args/DownBillInfoResult';
import { UpBillInfoResult } from '../args/UpBillInfoResult';
import { CellData } from '../../entity/tracking/cell-data/CellData';
import { Graph } from "./Graph";
import { CellOverlay } from "./CellOverlay";
export declare class BillCell extends mxCell {
    readonly THUMB_SCALE: number;
    entry: boolean;
    data: CellData;
    billDataInfo: BillDataInfo;
    billInfo: DownBillInfoResult | UpBillInfoResult;
    downCellOverlay: CellOverlay;
    upCellOverlay: CellOverlay;
    constructor(billDataInfo: BillDataInfo, billInfo: DownBillInfoResult | UpBillInfoResult, entry: boolean, geometry?: any, style?: any);
    getCaption(): string;
    buildHtmlTooltip(): string;
    /**
     * 样式类在graph组件中定义
     * @param graph
     * @returns
     */
    buildHtmlLabel(graph: Graph): HTMLDivElement;
    /**
     * 通过操纵dom元素方式创建html
     * @param graph
     * @returns
     */
    private buildHtmlLabelByDOM;
    /**
     * 通过模板字符串方式创建html
     * @param graph
     * @returns
     */
    private buildHtmlLabelByTemplateText;
}
