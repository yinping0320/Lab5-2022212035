export declare class WindowVariableService {
    constructor();
    /**
     * 从当前window遍历到框架顶级window
     * 并获取一个非空的自定义变量
     */
    getVariableFromWindow(key: string): any;
    /**
     * 从当前window遍历到框架顶级window
     * 并获取一个非空且含有元素的自定义数组变量
     */
    getArrayFromWindow(key: string): any[];
    /**
     * 获取框架的顶级window
     * 进行跨域封装
     */
    getTopWindow(): Window;
}
