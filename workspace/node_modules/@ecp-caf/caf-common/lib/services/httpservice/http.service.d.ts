import { HttpClient } from '@angular/common/http';
import { SessionService } from '../sessionservice/session.service';
import { Observable } from 'rxjs/Observable';
export declare class HttpService {
    private http;
    private sessionService;
    constructor(http: HttpClient, sessionService: SessionService);
    get(url: string): Observable<ArrayBuffer>;
    post(url: string, body: any): Observable<ArrayBuffer>;
    put(url: string, body: any): Observable<ArrayBuffer>;
    delete(url: string, body: any): Observable<ArrayBuffer>;
    patch(url: string, body: any): Observable<ArrayBuffer>;
    /** 默认用当前sessionid */
    setHeader(options: any, bizContextId?: string): any;
    request(method: string, url: string, options: any): Observable<ArrayBuffer>;
    request$(method: string, url: string, options: any): Observable<any>;
}
