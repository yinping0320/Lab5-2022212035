import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Observable } from 'rxjs';
import { EntityObject } from '../../define/structure/entity-object';
export declare class CommonStructureRTService {
    private http;
    private commom_structure_url;
    constructor(http: HttpClient);
    /**
     * 获取CommonStructure
     * @param type 元数据类型
     * @param id 元数据Id
     * @param headers 请求header，应包含sessionId等必需参数
     */
    get(id: string, headers: HttpHeaders): Observable<EntityObject>;
}
