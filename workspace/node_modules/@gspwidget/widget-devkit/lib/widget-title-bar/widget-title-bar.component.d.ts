import { ElementRef, EventEmitter, OnInit, TemplateRef } from '@angular/core';
import { DatalessJumpConfig, JumpTypes } from '../preset/preset.jump';
import { WidgetDevkitService } from '../widget-devkit.service';
export declare class WidgetTitleBarComponent implements OnInit {
    private devkitSvc;
    icon: string;
    /** 标题 */
    showTitle: boolean;
    title: string;
    /** 数字 */
    showNumber: boolean;
    number: number;
    /** 设置按钮 */
    showSetting: boolean;
    settingTitle: string;
    clickSetting: EventEmitter<any>;
    /** 刷新按钮 */
    showRefresh: boolean;
    clickRefresh: EventEmitter<any>;
    /** 是否启用跳转，开启后元部件配置中可配置全局联查 */
    jumpConfig: DatalessJumpConfig;
    /** @deprecated 应改用 enableJump。全局联查配置 */
    jumpProperty: any;
    /** @deprecated 应改用 enableJump。为了跳转时能取到数据的第一行，用来拼接跳转路由参数。 */
    data: any[];
    /** 传入模板 */
    leftTemplate: TemplateRef<ElementRef>;
    rightTemplate: TemplateRef<ElementRef>;
    JumpTypes: typeof JumpTypes;
    constructor(devkitSvc: WidgetDevkitService);
    ngOnInit(): void;
    handleClickJump(): void;
}
