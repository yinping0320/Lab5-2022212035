import { PropertyOption } from '../../types';
/**
 * 元部件属性注册表，形如
 * { constructorName: { propertyName: propertyOption }
 */
export declare const WidgetPropertyMap: Map<string, PropertyOption[]>;
/**
 * TODO: 加入检查，编译时报错（name不应该写；）
 * @param option 可选，默认为 { type: PropertyTypes.String }
 */
export declare function Property(option?: PropertyOption): (widgetPrototype: any, propName: any) => void;
