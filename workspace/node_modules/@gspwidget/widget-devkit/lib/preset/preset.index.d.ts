/**
 * 存放所有从预制元部件中抽取出来的公共值、方法。
 * 好处是在此库中统一维护、管理，不会被打到每个元部件中。
 */
export * from './preset.jump';
export * from './preset.echarts';
/**
 * dataItem 这个名字会影响 exp 中的写法，不可随意更改。
 */
export declare function resolveTemplate(html: string, dataItem: any): string;
