import { BsModalService, ModalOptions } from 'ngx-bootstrap/modal';
import { DataJumpConfig, DatalessJumpConfig } from './preset/preset.jump';
/**
 * @deprecated
 * 为了 widget-devkit 的兼容而暂时保留。不准再用了。
 * 元部件应该直接用 @gspwidget/util。
 */
export declare const DASHBOARD_API = "DashboardApi";
export declare class WidgetDevkitService {
    private modalService;
    constructor(modalService: BsModalService);
    openModal(content: any, config?: ModalOptions): import("ngx-bootstrap/modal/bs-modal-ref.service").BsModalRef;
    openMenu(funcId: string, queryParamsArray?: string[][]): void;
    openApp(appId: string): void;
    /**
     * options 参考 https://ng.ant.design/components/message/zh
     */
    openInfoMsg(content: string, options?: any): void;
    /**
     * options 参考 https://ng.ant.design/components/message/zh
     */
    openSuccessMsg(content: string, options?: any): void;
    /**
     * options 参考 https://ng.ant.design/components/message/zh
     */
    openWarningMsg(content: string, options?: any): void;
    /**
     * options 参考 https://ng.ant.design/components/message/zh
     */
    openErrorMsg(content: string, options?: any): void;
    getData(dataOption: any): any;
    getUserInfo(reload?: boolean): any;
    getUserAvatar(reload?: boolean): any;
    /**
     * @deprecated 应用另外两个 jump 方法
     * 联查跳转，直接把配置属性的值传进来就行。配成“无”就不跳转。
     * @param jumpProperty 跳转的配置属性的值
     * @param dataItem 当前跳转对应的数据行。如果是全局跳转，就传入第一行？
     */
    jump(jumpProperty: any, dataItem?: any): void;
    jumpWithData(dataJumpConfig: DataJumpConfig, dataItem: any): void;
    jumpWithoutData(datalessJumpConfig: DatalessJumpConfig): void;
}
