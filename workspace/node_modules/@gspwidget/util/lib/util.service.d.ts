import { HttpClient } from '@angular/common/http';
export declare class UtilService {
    private http;
    constructor(http: HttpClient);
    uploadFile(url: string, content: {
        [key: string]: File | any;
    }): import("rxjs").Observable<string>;
}
