/**
 * 此常量代表“部件系统”和“Dashboard”依赖的文件的位置。
 * dashboard 本身不需要此值，空串开头即可在运行时取到部署目录，不要用它。
 * 对于可能被用在 dashboard 之外的代码，比如几个公共库等，
 * 必须用它找到绝对位置，以当代码运行在其他项目、部署在其他地方时能获取到部件运行时依赖。
 */
/** 通过 ls 中 LS_LOAD_LOCAL_ASSET 标记判断是否加载 localhost/assets。仅用于部件系统开发人员，只能手动修改 ls 中的标记值。 */
export declare const LS_LOAD_LOCAL_ASSET = "LS_LOAD_LOCAL_ASSET";
export declare const IGIX_HOME_PATH: string;
export declare const BASE_URL = "/api/runtime/epp/v1.0/";
export declare function addBaseUrl(node: any): void;
/** 数据源相关的 base url, 为了防止跟其他的不同. 若相同, 可删掉 */
export declare const DATA_PREFIX = "data";
export declare const FILTERS_WIDGET_NAME = "filters";
export declare const FILTERS_WIDGET_FILTERS_PROP_NAME = "filters";
export declare const FILTERS_WIDGET_PROP_NAME = "change";
export declare const DESIGNER_CONFIG: {
    PAGE_DESIGNER_ID: string;
    PAGE_RUNTIME_ID: string;
};
export declare const dimensionTypeConfig: {
    text: {
        list: string[];
        color: string;
        iconName: string;
    };
    number: {
        list: string[];
        color: string;
        iconName: string;
    };
    date: {
        list: string[];
        color: string;
        iconName: string;
    };
    byte: {
        list: string[];
        color: string;
        iconName: string;
    };
    boolean: {
        list: string[];
        color: string;
        iconName: string;
    };
    unknown: {
        list: string[];
        color: string;
        iconName: string;
    };
};
