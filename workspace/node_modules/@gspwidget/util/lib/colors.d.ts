/**
 * 部件系统默认色系。
 * 直接用的缺陷是无法拿到 home.config.json 中定制的色系值，
 * 尽量用 colors
 */
export declare class PlThemeColors {
    static pure: string[];
    static gradient: string[];
    static SetColors(pure: string[], gradient: string[]): void;
    static SetPure(pure: string[]): void;
    static SetGradient(gradient: string[]): void;
}
