/**
 * 字段转换器
 * 为了解决前端 - 服务端某些字段不一致，在读取、保存端动态转换
 */
/**
 * 接受原对象，转换出新对象
 * @param originEntity 待转换原对象
 * @param entityType 对象类型，应为 fieldMapping 的属性名
 * @param inverse 是否反向转换（默认为后端 -> 前端）
 */
export declare function mapEntityFeilds<T>(originEntity: any, entityType: any, inverse?: boolean): T;
/**
 * 返回 rxjs 的 map 操作符调用，用法：
 * ```js
 * this.http.get(url).pipe(mapFeilds<PageInfo>('pageInfo'))
 * ```
 * 可将 http 请求得到的对象 pipe 转换为前端适用字段；可接受数组或对象，可自动区分。
 *
 * @param entityType 对象类型，应为 fieldMapping 的属性名
 * @param inverse 是否反向转换（默认为后端 -> 前端）
 */
