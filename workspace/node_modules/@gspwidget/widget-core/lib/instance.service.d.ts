import { WidgetComponent } from './widget/widget.component';
/**
 * instance 指运行时，在某处运行的一个部件。
 * 此服务被动收集、输出实例id与实例引用信息，
 * 主动调用者为 widgetComponent 或 portletComponent
 */
export declare class InstanceService {
    /** 部件实例引用信息 */
    instances: {
        [insId: string]: {
            widgetComp?: WidgetComponent;
        };
    };
    constructor();
    getInsByPInsId(insId: string): {
        widgetComp?: WidgetComponent;
    };
    addInsInfo(insId: string, widgetComp: WidgetComponent, type: 'widgetComp' | 'portletComp'): void;
}
