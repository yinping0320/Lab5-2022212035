import { EventEmitter, OnChanges, OnInit, PipeTransform, SimpleChanges } from '@angular/core';
import { FilterSchema, FSEnumItem } from '@gspwidget/widget-devkit';
import { TranslateService } from '@ngx-translate/core';
export declare class FilterComponent implements OnInit, OnChanges {
    translate: TranslateService;
    /** 用于获得 enum 列的值 */
    /** filter 里绝不应该需要完整数据，否则参与查询的筛选还有啥意义？ */
    fs: FilterSchema;
    singleValueChange: EventEmitter<{}>;
    multiValueChange: EventEmitter<{}>;
    value: any;
    constructor(translate: TranslateService);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    getEnumItems(): FSEnumItem[];
    /**
     * filter 不关心外界如何看待、处理多个值（多个值对应一个还是多个 dp），
     * 仅把多个值的数组发出去
     */
    handleChange(value: any): void;
    handleChangeMulti(values: any[]): void;
}
export declare class ColValuesPipe implements PipeTransform {
    transform(data: any[], col: string): any;
}
