import { OnInit, OnChanges, SimpleChanges } from '@angular/core';
import { TrackType } from '../config/track-type.map';
import { AifDocumentsTrackService } from '../service/aif-documents-track.service';
/**
 * 单据追踪主组件
 */
export declare class AifDocumentsTrackManagerComponent implements OnInit, OnChanges {
    private trackService;
    /**子组件显示控制 */
    isHidden: boolean;
    isShowable: boolean;
    isResultEmpty: boolean;
    documentId: string;
    beTypeCode: string;
    selectedTrackType: TrackType;
    trackTypes: any[];
    /**传递给子组件的所选择的追踪方式 */
    selectedTrackTypeToChildren: string;
    /**响应结果转化为组件内传输格式 */
    beTypeCodes: any[];
    documents: {
        [beTypeCode: string]: any[];
    };
    beTypeNames: {
        [beTypeCode: string]: string;
    };
    constructor(trackService: AifDocumentsTrackService);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    search(): void;
    /**
     * 为外部提供的方便递归调用的查询接口
     * @param documentId
     * @param beType
     * @param trackType
     */
    searchFromOut(documentId: string, beType: string, trackType: TrackType): void;
    /**
     * 从查询结果中选择单据继续追踪时，父组件接收到子组件传来的
     * 对象，并自动调用查询方法
     * @param $event
     */
    trackGoingOn($event: any): void;
    private isNullOrUndefined;
}
