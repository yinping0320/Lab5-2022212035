import { Injector, ComponentFactoryResolver } from "@angular/core";
import { HttpService } from "@ecp-caf/caf-common";
import { Observable } from "rxjs";
import { TrackType } from "../config/track-type.map";
import { BsModalService, BsModalRef } from '@farris/ui-modal';
import { AifDocumentsTrackManagerModuleConfig } from '../../entity/aif-documents-track-manager-module-config';
/**
 * 供内部和外部共同使用的单据追踪服务类，可输入参数自动查询并显示单据追踪模态框，也可以仅获取单据追踪查询结果
 */
export declare class AifDocumentsTrackService {
    private http;
    private modalService;
    private injector;
    private cfr;
    private serverIP;
    private baseUrl;
    constructor(http: HttpService, modalService: BsModalService, injector: Injector, cfr: ComponentFactoryResolver, config: AifDocumentsTrackManagerModuleConfig);
    /**
     * 根据参数追踪并显示追踪模态框，注意：调用该方法时应同时引用TrackType枚举类
     * @param documentId 单据ID
     * @param beType 单据类型编号
     * @param trackType 追踪类型，仅可用“UpSearch”（上查）或“DownSearch”（下查)
     */
    showResultDialog(documentId: string, beType: string, trackType: TrackType): BsModalRef;
    /**
     * 根据参数追踪并将查询结果返回，注意：调用该方法时应同时引用TrackType枚举类
     * @param documentId 单据ID
     * @param beType 单据类型编号
     * @param trackType 追踪类型，仅可用“UpSearch”（上查）或“DownSearch”（下查),
     */
    getTrackedDocumentData(documentId: string, beType: string, trackType: TrackType): Observable<any>;
    private getTargetData;
    private getSourceData;
}
