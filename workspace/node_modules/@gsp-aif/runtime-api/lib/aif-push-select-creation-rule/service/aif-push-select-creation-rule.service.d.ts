import { Injector, ComponentFactoryResolver } from "@angular/core";
import { Observable } from "rxjs";
import { BsModalService, ModalOptions } from '@farris/ui-modal';
import { AifCreationRuleEntity } from '@gsp-aif/common-api';
import { GetCreationRuleArgs } from '../../entity/get-creation-rule-args';
/**
 * 供外部调用的推式选择生单规则服务类接口
 */
export declare class AifPushSelectCreationRuleService {
    private injector;
    private cfr;
    private modalService;
    /**
     * 内置模态框配置
     */
    private readonly innerModalOptions;
    constructor(injector: Injector, cfr: ComponentFactoryResolver, modalService: BsModalService);
    /**
     * 显示选择生单规则模态框，点击确定按钮时返回选中的生单规则
     * @param args 查询生单规则所需参数
     * @param modalOptions 模态框配置,可为空
     */
    showCrRuleSelectModal(args: GetCreationRuleArgs, modalOptions?: ModalOptions): Observable<AifCreationRuleEntity>;
    private isNullOrUndefined;
}
