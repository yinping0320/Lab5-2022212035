import { IEntity } from './interface-entity';
/**
 * 元数据实体类接口
 */
export interface IMetadataEntity extends IEntity {
    /**元数据ID */
    MetadataId: string;
    /**应用（APP）编号 */
    AppCode: string;
    /**服务单元（SU）编号 */
    SuCode: string;
    /**业务对象（BO）Id */
    BoId: string;
}
