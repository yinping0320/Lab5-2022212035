import { IP2PEntity } from './interface-p2p-entity';
import { IMetadataEntity } from './interface-metadata-entity';
/**
 * 点到点元数据实体类接口。点到点元数据包括生单规则、回写规则、生单流程、回写流程。
 */
export interface IP2PMetadataEntity extends IP2PEntity, IMetadataEntity {
}
