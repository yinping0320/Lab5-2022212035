import { EntityBase } from './abstract-entity-base';
import { IMetadataEntity } from './interface-metadata-entity';
/**
 * 元数据实体抽象类
 */
export declare abstract class MetadataEntityBase extends EntityBase implements IMetadataEntity {
    MetadataId: string;
    AppCode: string;
    SuCode: string;
    BoId: string;
}
