import { OnInit, TemplateRef } from '@angular/core';
import { DataColumn, DatagridComponent } from '@farris/ui-datagrid';
import { LoadingService } from '@farris/ui-loading';
import { NotifyService } from '@farris/ui-notify';
import { Observable, Subject } from 'rxjs';
import { CommonComment } from '../../entity/comment';
import { CommonCommentFilter } from '../../entity/commonCommentFilter';
import { I18nService } from '../../service/i18n/i18n.service';
import { TaskService } from '../../service/task.service';
export declare class CommentManageComponent implements OnInit {
    private i18nService;
    private taskService;
    private notify;
    private loadingService;
    commentList: CommonComment[];
    originCommentList: CommonComment[];
    commentColumns: DataColumn[];
    confirmSubject: Subject<{}>;
    cancelSubject: Subject<{}>;
    filter: CommonCommentFilter;
    dataGrid: DatagridComponent;
    commentTypeCell: TemplateRef<any>;
    commentOperateCell: TemplateRef<any>;
    buttons: TemplateRef<any>;
    constructor(i18nService: I18nService, taskService: TaskService, notify: NotifyService, loadingService: LoadingService);
    ngOnInit(): void;
    /**
   * 点击常用意见，获取用户常用意见列表
   */
    loadComments(): void;
    /**
     * 新增一条常用意见
     */
    addCommonComment(): void;
    /**
     * 删除一条意见 data.rowData
     */
    deleteComment(data: any): void;
    /**
     * 常用意见点击保存
     */
    saveCommonComment(): void;
    /**
     * 点击取消（此处已返回给farris）
     */
    clickCancel(): void;
    /**
     * 系统预置的不允许编辑删除
     * @param data data.rowData为实体数据
     * @returns
     */
    beforeEdit: (data: any) => Observable<boolean>;
    getConfirmSUbject(): Observable<any>;
    getCancelSubject(): Observable<any>;
}
