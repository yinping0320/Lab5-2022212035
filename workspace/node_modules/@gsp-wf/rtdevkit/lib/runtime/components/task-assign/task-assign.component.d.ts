import { OnInit, EventEmitter } from '@angular/core';
import { AssigneeInfo } from '../../../entity';
import { TaskService } from '../../services/task.service';
import { UserService } from '../../services/user.service';
import { UserHelperComponent } from '../user-helper/user-helper.component';
import { NotifyService } from '@farris/ui-notify';
import { I18nService } from '../../services/i18n.service';
export declare class TaskAssignComponent extends UserHelperComponent implements OnInit {
    taskSvc: TaskService;
    protected userSvc: UserService;
    protected notify: NotifyService;
    protected i18nService: I18nService;
    assigneeInfo: AssigneeInfo[];
    completed: EventEmitter<boolean>;
    private allUserData;
    private assignees;
    private assigneesPa;
    private allowGlobalAssign;
    constructor(taskSvc: TaskService, userSvc: UserService, notify: NotifyService, i18nService: I18nService);
    ngOnInit(): void;
    execute(): void;
    getAssignedUsers(): void;
    handleOk(): void;
    search(event: any): void;
    protected showModal(show: boolean): void;
    onPageSizeChange(pageSize: any): void;
    onPageChange(event: any): void;
}
