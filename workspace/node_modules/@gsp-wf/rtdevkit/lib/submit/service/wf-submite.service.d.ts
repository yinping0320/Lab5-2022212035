import { ComponentFactoryResolver, Injector } from '@angular/core';
import { BsModalService } from '@farris/ui-modal';
import { ExcutionResponse } from '../../entity/excution-response';
import { InteractionResponse } from '../../entity/interaction-response';
import { I18nService } from '../../runtime/services/i18n.service';
/**
 * 提交审批客户端交互服务
 */
export declare class WFSubmiteService {
    private injector;
    private resolver;
    private modalService;
    private i18nService;
    constructor(injector: Injector, resolver: ComponentFactoryResolver, modalService: BsModalService, i18nService: I18nService);
    /**
     * 客户端交互处理服务端返回结果
     * @param excutionRe 提交审批服务器端返回结果
     * @param handler 处理结果回调页面
     */
    excute(excutionRe: ExcutionResponse, handler: (result: InteractionResponse) => void): void;
    /**
     * 处理选择流程结果
     */
    private selectProcess;
    /**
     * 处理指定下一步人员结果
     */
    private assignee;
}
