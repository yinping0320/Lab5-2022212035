import { OnInit, TemplateRef, Injector, ChangeDetectorRef } from '@angular/core';
import { DatagridComponent } from '@farris/ui-datagrid';
import { Subject } from 'rxjs/Subject';
import { MessagerService } from '@farris/ui-messager';
import { WfLocalePipe } from '../../../../../i18n/pipe/wf-process-editor-locale.pipe';
export declare class EnumListComponent implements OnInit {
    private messageService;
    private injector;
    private cdr;
    private localeId;
    private resources;
    private localePipe;
    cls: string;
    columns: {
        field: string;
        width: number;
        title: any;
    }[];
    dg: DatagridComponent;
    btnRef: TemplateRef<any>;
    confirm: Subject<any>;
    closeModalEmitter: Subject<any>;
    /**
     * 显示值
     */
    data: any[];
    selectedIds: any[];
    constructor(messageService: MessagerService, injector: Injector, cdr: ChangeDetectorRef, localeId: string, resources: any, localePipe: WfLocalePipe);
    ngOnInit(): void;
    /**
     * 加载显示数据
     */
    load(enumValues: any): void;
    /**
     * 确认
     */
    onConfirm(): void;
    /**
     * 取消
     */
    cancel(): void;
}
