import { OnInit, NgZone, ElementRef, ChangeDetectorRef } from '@angular/core';
import { DialogComponent } from "@farris/ui-dialog";
import { DatagridComponent } from "@farris/ui-datagrid";
import { ExpressionService } from "@gsp-svc/expression";
import { ProcessDesignerUIState } from "../../../../app/process-designer.uistate";
import { ActivityExecutorParticipant } from "./entities/activity-executor.participant";
import { WfLocalePipe } from '../../../../i18n/pipe/wf-process-editor-locale.pipe';
export declare class ActivityExecutorPerformerComponent implements OnInit {
    private ngZone;
    designerUIState: ProcessDesignerUIState;
    private expr;
    private cdr;
    private localeId;
    private resources;
    private localePipe;
    cls: string;
    showClearButton: boolean;
    activityExecutors: ActivityExecutorParticipant[];
    condition: string;
    cols: {
        title: any;
        field: string;
        width: number;
    }[];
    inputGroup: ElementRef;
    allActivityDialog: DialogComponent;
    allActivityTable: DatagridComponent;
    constructor(ngZone: NgZone, designerUIState: ProcessDesignerUIState, expr: ExpressionService, cdr: ChangeDetectorRef, localeId: string, resources: any, localePipe: WfLocalePipe);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    onMouseEnter(event: any): void;
    onMouseLeave(event: any): void;
    private toggleClearIcon;
    clear(data: any): void;
    selectUsers(data: any): void;
    editConditon(): void;
    activitiesSelect(): void;
    getPerformers(): ActivityExecutorParticipant[];
}
