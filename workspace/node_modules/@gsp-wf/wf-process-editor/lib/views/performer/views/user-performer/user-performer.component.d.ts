import { OnInit, Injector, NgZone, AfterViewInit, ElementRef, ChangeDetectorRef } from '@angular/core';
import { ProcessDesignerUIState } from '../../../../app/process-designer.uistate';
import { ExpressionService } from '@gsp-svc/expression';
import { UserPerformerEntity } from '../../entities/user-performer.entity';
import { PerformerConfigService } from '../../performer-config.service';
import { HttpService } from '@ecp-caf/caf-common';
import { MessagerService } from '@farris/ui-messager';
import { PersonnelSelectorComponent } from '@farris/ui-personnel-selector';
import { WfLocalePipe } from '../../../../i18n/pipe/wf-process-editor-locale.pipe';
export declare class UserPerformerComponent implements OnInit, AfterViewInit {
    private designerUIState;
    private expr;
    private injector;
    private lookupConfig;
    private ngZone;
    private cdr;
    private localeId;
    private resources;
    private localePipe;
    private http?;
    private message?;
    cls: string;
    userHelp: PersonnelSelectorComponent;
    inputGroup: ElementRef;
    showClearButton: boolean;
    bindingData: {
        id?: any;
        name?: any;
    };
    farrisUserHelpShowTabIds: string;
    activeTabId: string;
    selectedUserIds: string;
    userTypeCollection: {
        text: any;
        value: string;
    }[];
    userType: string;
    variableOrgTypes: {
        name: any;
        value: string;
    }[];
    curOrgType: string;
    hasAdminOrg: boolean;
    userTypeButtonNeedShow: boolean;
    multiSelect: boolean;
    condition: string;
    users: UserPerformerEntity[];
    constructor(designerUIState: ProcessDesignerUIState, expr: ExpressionService, injector: Injector, lookupConfig: PerformerConfigService, ngZone: NgZone, cdr: ChangeDetectorRef, localeId: string, resources: any, localePipe: WfLocalePipe, http?: HttpService, message?: MessagerService);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    onMouseEnter(event: any): void;
    onMouseLeave(event: any): void;
    private toggleClearIcon;
    clear(data: any): void;
    selectUsers(data: any): void;
    editConditon(): void;
    usersSelect(event: any): void;
    showExprHelp(): void;
    getPerformers(): UserPerformerEntity[];
    private variableParticipantName;
    orgTypeChange(data: any): void;
}
