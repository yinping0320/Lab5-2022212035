import { OnInit, EventEmitter, TemplateRef, Injector, ChangeDetectorRef } from '@angular/core';
import { Observable } from 'rxjs';
import { LookupGridComponent } from '@farris/ui-lookup';
import { HttpService } from '@ecp-caf/caf-common';
import { MessagerService } from '@farris/ui-messager';
import { WfLocalePipe } from '../../i18n/pipe/wf-process-editor-locale.pipe';
export declare class SaveAsEditComponent implements OnInit {
    private injector;
    private http;
    private message;
    private cdr;
    private localeId;
    private resources;
    private localePipe;
    btnRef: TemplateRef<any>;
    procName: string;
    procKey: string;
    lookUp: LookupGridComponent;
    submit: EventEmitter<any>;
    cancel: EventEmitter<any>;
    accessLevel: string;
    organization: any;
    organizationName: any;
    isAdministratorConfig: boolean;
    enableAdminOrgAut: boolean;
    administrator: any;
    dataUri: string;
    loadType: string;
    disabled: boolean;
    items: any[];
    accessLevelCollection: {
        value: string;
        text: any;
    }[];
    columns: {
        title: any;
        field: string;
        width: string;
        searchField: string;
    }[];
    constructor(injector: Injector, http: HttpService, message: MessagerService, cdr: ChangeDetectorRef, localeId: string, resources: any, localePipe: WfLocalePipe);
    ngOnInit(): void;
    saveAsOK(): void;
    saveAsCancel(): void;
    validator(): boolean;
    search(data: any): void;
    dictPicking: (data: any) => Observable<boolean>;
    dictPicked: (data: any) => Observable<boolean>;
    selectedDataHandler(data: any): void;
    private orgTreeData;
    private treeIterator;
    private findChild;
    /** 按照parent码构造树 */
    private makeTreeByParent;
    /** 找到子节点 */
    private findChildrenByParent;
    private getSysOrgs;
}
