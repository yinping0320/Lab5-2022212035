import { BpmnModel, ExtensionCompletionConditionRuler } from '@edp-pmf/bpmn-model';
import { OnInit, EventEmitter, TemplateRef, Injector, ComponentFactoryResolver, ChangeDetectorRef } from '@angular/core';
import { WfLocalePipe } from '../../i18n/pipe/wf-process-editor-locale.pipe';
import { DatagridComponent } from '@farris/ui-datagrid';
import { BsModalService } from '@farris/ui-modal';
import { TypeConverter } from '@farris/ide-property-panel';
import { MessagerService } from '@farris/ui-messager';
import { ExpressionService } from '@gsp-svc/expression';
import { ProcessDesignerUIState } from '../../app/process-designer.uistate';
import { MultiInstanceLoopCharacteristics } from './multi-instance-loop-characteristics.entity';
import { HttpService } from '@ecp-caf/caf-common';
export declare class MultiInstanceLoopCharacteristicsComponent implements OnInit {
    private resolver;
    private injector;
    private uistate;
    private expr;
    private modalService;
    private message;
    private http;
    private cdr;
    private localeId;
    private resources;
    private localePipe;
    cls: string;
    groupIcon2: string;
    closeModal: EventEmitter<any>;
    submitModal: EventEmitter<any>;
    value: any;
    editorParams: any;
    modalFooter: TemplateRef<any>;
    cell: TemplateRef<any>;
    grid: DatagridComponent;
    preEnable: boolean;
    preSequential: boolean;
    loopCharacteristics: MultiInstanceLoopCharacteristics;
    loopCharacteristicsTemp: MultiInstanceLoopCharacteristics;
    conditionRulesIsSet: string;
    promptAct: boolean;
    bpmnModel: BpmnModel;
    modalConfig: {
        title: any;
        width: number;
        height: number;
        showButtons: boolean;
    };
    constructor(resolver: ComponentFactoryResolver, injector: Injector, uistate: ProcessDesignerUIState, expr: ExpressionService, modalService: BsModalService, message: MessagerService, http: HttpService, cdr: ChangeDetectorRef, localeId: string, resources: any, localePipe: WfLocalePipe);
    ngOnInit(): void;
    loopCardinalityExpr(data: any): void;
    showloopCardinalityHelp(data: string): void;
    showExpr(): void;
    isEnable(data: any): void;
    validate(data: any): void;
    setCompletionConditionRules(completionConditionRules: ExtensionCompletionConditionRuler[]): void;
    clickOK(): void;
    clickCancel(): void;
}
export declare class MultiInstanceLoopCharacteristicsConverter implements TypeConverter {
    private localePipe;
    constructor(localePipe: WfLocalePipe);
    convertTo(data: any): string;
}
