import { ChangeDetectorRef, EventEmitter, OnInit, TemplateRef } from '@angular/core';
import { AutoHandle, ExpireOperation } from "../time-limit-control.entity";
import { MessagerService } from '@farris/ui-messager';
import { ProcessDesignerUIState } from '../../../app/process-designer.uistate';
import { WfLocalePipe } from '../../../i18n/pipe/wf-process-editor-locale.pipe';
export declare class AutoHandleOperationComponent implements OnInit {
    private message;
    uiState: ProcessDesignerUIState;
    private cdr;
    private localeId;
    private resources;
    private localePipe;
    footer: TemplateRef<any>;
    value: AutoHandle;
    placeholder: string;
    operation: AutoHandle;
    submitEmitter: EventEmitter<ExpireOperation>;
    closeEmitter: EventEmitter<any>;
    autoHandleType: any[];
    constructor(message: MessagerService, uiState: ProcessDesignerUIState, cdr: ChangeDetectorRef, localeId: string, resources: any, localePipe: WfLocalePipe);
    ngOnInit(): void;
    clickOK(): void;
    clickCancel(): void;
    private validator;
}
