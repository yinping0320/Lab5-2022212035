import { OnInit, EventEmitter, TemplateRef, Injector, ChangeDetectorRef } from '@angular/core';
import { TypeConverter } from '@farris/ide-property-panel';
import { AnonymousSet } from './anonymous-set.entity';
import { WfLocalePipe } from '../../i18n/pipe/wf-process-editor-locale.pipe';
export declare class AnonymousSetComponent implements OnInit {
    private injector;
    private cdr;
    private localeId;
    private resources;
    private localePipe;
    cls: string;
    closeModal: EventEmitter<any>;
    submitModal: EventEmitter<any>;
    value: AnonymousSet;
    modalFooter: TemplateRef<any>;
    anonymousSet: AnonymousSet;
    modalConfig: {
        title: any;
        width: number;
        height: number;
        showButtons: boolean;
    };
    constructor(injector: Injector, cdr: ChangeDetectorRef, localeId: string, resources: any, localePipe: WfLocalePipe);
    ngOnInit(): void;
    clickOK(): void;
    clickCancel(): void;
}
export declare class AnonymousSetConverter implements TypeConverter {
    private localePipe;
    constructor(localePipe: WfLocalePipe);
    convertTo(data: any): string;
}
