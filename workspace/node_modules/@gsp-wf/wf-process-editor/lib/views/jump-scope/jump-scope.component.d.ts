import { OnInit, EventEmitter, TemplateRef, ChangeDetectorRef } from '@angular/core';
import { TypeConverter } from '@farris/ide-property-panel';
import { ProcessDesignerUIState } from '../../app/process-designer.uistate';
import { DialogComponent } from '@farris/ui-dialog';
import { MessagerService } from '@farris/ui-messager';
import { DatagridComponent } from '@farris/ui-datagrid';
import { LookupGridComponent, PickedResult } from '@farris/ui-lookup';
import { Observable } from 'rxjs';
import { WfLocalePipe } from '../../i18n/pipe/wf-process-editor-locale.pipe';
export declare class JumpScopeComponent implements OnInit {
    uistate: ProcessDesignerUIState;
    private message;
    private cdr;
    private localeId;
    private resources;
    private localePipe;
    cls: string;
    closeModal: EventEmitter<any>;
    submitModal: EventEmitter<any>;
    value: any;
    modalFooter: TemplateRef<any>;
    modalConfig: {
        title: any;
        width: number;
        height: number;
        showButtons: boolean;
    };
    allActisTable: LookupGridComponent;
    allActisDialog: DialogComponent;
    actTable: DatagridComponent;
    lookup: LookupGridComponent;
    userActis: LookupGridComponent;
    cols: {
        title: any;
        field: string;
        width: number;
        searchField: string;
    }[];
    actis: any[];
    fullActis: any[];
    defaultacty: string;
    categoryListItems: {
        value: string;
        name: any;
    }[];
    category: string;
    defaultType: string;
    defaultTypeListItems: {
        value: string;
        name: any;
    }[];
    defaultActivity: string;
    pageInfo: {
        pageIndex: number;
        pageList: number[];
        pageSize: number;
    };
    outConnectNode: true;
    total: number;
    constructor(uistate: ProcessDesignerUIState, message: MessagerService, cdr: ChangeDetectorRef, localeId: string, resources: any, localePipe: WfLocalePipe);
    ngOnInit(): void;
    clickOK(): void;
    clickCancel(): void;
    private validator;
    searchDefault(arg: any): void;
    loadDefault(search?: {
        field: string;
        value: string;
    }): void;
    searchSelect(arg: any): void;
    loadSelect(search?: {
        field: string;
        value: string;
    }): void;
    actisSelect: (rowData: any) => Observable<PickedResult>;
    defaultActivitySelect: (rowData: any) => Observable<PickedResult>;
    deleteRow(): void;
}
export declare class JumpScopeConverter implements TypeConverter {
    private localePipe;
    constructor(localePipe: WfLocalePipe);
    convertTo(): string;
}
