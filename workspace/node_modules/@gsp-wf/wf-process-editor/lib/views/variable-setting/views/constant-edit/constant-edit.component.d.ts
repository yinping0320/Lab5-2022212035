import { OnInit, TemplateRef, EventEmitter, ChangeDetectorRef } from '@angular/core';
import { WfLocalePipe } from '../../../../i18n/pipe/wf-process-editor-locale.pipe';
export declare class ConstantEditComponent implements OnInit {
    private cdr;
    private localeId;
    private resources;
    private localePipe;
    footer: TemplateRef<any>;
    closeEmitter: EventEmitter<any>;
    submitEmitter: EventEmitter<string>;
    value: string;
    constructor(cdr: ChangeDetectorRef, localeId: string, resources: any, localePipe: WfLocalePipe);
    ngOnInit(): void;
    clickOK(): void;
    clickCancel(): void;
}
