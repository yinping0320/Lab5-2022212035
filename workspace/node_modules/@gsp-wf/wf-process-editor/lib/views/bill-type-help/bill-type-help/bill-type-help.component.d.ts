import { EventEmitter, OnInit } from '@angular/core';
import { ActivatedRoute, Router } from '@angular/router';
import { LoadingService } from '@farris/ui-loading';
import { MessagerService } from '@farris/ui-messager';
import { BsModalService } from '@farris/ui-modal';
import { NotifyService } from '@farris/ui-notify';
import { DocuTypeHelperEntity } from '../entity/DocuTypeHelperEntity';
import { BillTypeHelpService } from '../bill-type-help.service';
import { NodeSelectedParam, TreeNode, TreeTableComponent } from '@farris/ui-treetable';
import { AifLocalePipe } from '../../aif-ui-common/pipe/aif-ui-common-locale.pipe';
export declare class BillTypeHelpComponent implements OnInit {
    private entityService;
    private messager;
    private notify;
    private modal;
    private loading;
    private localePipe;
    private router;
    private activatedRoute;
    onselected: EventEmitter<DocuTypeHelperEntity>;
    tt: TreeTableComponent;
    ttData: TreeNode[];
    ttCols: any[];
    ttIdField: string;
    ttSearchFields: {
        label: string;
        value: string;
    }[];
    ttSearchObj: {
        field: string;
        value: string;
    };
    ttAllColumnsTitle: string;
    docuTypeList: DocuTypeHelperEntity[];
    docuTypeId: string;
    constructor(entityService: BillTypeHelpService, messager: MessagerService, notify: NotifyService, modal: BsModalService, loading: LoadingService, localePipe: AifLocalePipe, router: Router, activatedRoute: ActivatedRoute);
    ngOnInit(): void;
    getSelectedDocuType(): DocuTypeHelperEntity;
    /**
     * 左侧树表搜索事件
     */
    ttSearch(obj: {
        field: string;
        value: string;
    }): void;
    loadItems(): void;
    ttNodeSelected(nsp: NodeSelectedParam): void;
    buildTreeData(entities: DocuTypeHelperEntity[]): TreeNode[];
    private configLookupColumns;
}
