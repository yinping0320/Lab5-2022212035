import { ChangeDetectorRef, EventEmitter, OnInit, TemplateRef } from '@angular/core';
import { DatagridComponent } from '@farris/ui-datagrid';
import { MessagerService } from '@farris/ui-messager';
import { ProcessDesignerUIState } from '../../app/process-designer.uistate';
import { WfLocalePipe } from '../../i18n/pipe/wf-process-editor-locale.pipe';
export declare class ActivityListComponent implements OnInit {
    private designerUIState;
    private message;
    private cdr;
    private localeId;
    private resources;
    private localePipe;
    closeEmitter: EventEmitter<any>;
    submitEmitter: EventEmitter<any>;
    buttonRef: TemplateRef<any>;
    grid: DatagridComponent;
    multiSelect: boolean;
    showCheckbox: boolean;
    data: any[];
    columns: {
        title: any;
        field: string;
        width: number;
    }[];
    constructor(designerUIState: ProcessDesignerUIState, message: MessagerService, cdr: ChangeDetectorRef, localeId: string, resources: any, localePipe: WfLocalePipe);
    ngOnInit(): void;
    clickOK(): void;
    clickCancel(): void;
}
