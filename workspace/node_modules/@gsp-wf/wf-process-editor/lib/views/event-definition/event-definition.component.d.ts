import { OnInit, EventEmitter, TemplateRef, Injector, ChangeDetectorRef } from '@angular/core';
import { DatagridComponent } from '@farris/ui-datagrid';
import { MessagerService } from '@farris/ui-messager';
import { ProcessDeUtil } from '../../domain/process-de-util';
import { EventDefinition } from './event-definition.entity';
import { TypeConverter } from '@farris/ide-property-panel';
import { WfLocalePipe } from '../../i18n/pipe/wf-process-editor-locale.pipe';
export declare class EventDefinitionComponent implements OnInit {
    private message;
    private util;
    private injector;
    private cdr;
    private localeId;
    private resources;
    private localePipe;
    cls: string;
    closeModal: EventEmitter<any>;
    submitModal: EventEmitter<any>;
    value: any[];
    modalFooter: TemplateRef<any>;
    grid: DatagridComponent;
    modalConfig: {
        title: any;
        width: number;
        height: number;
        showButtons: boolean;
    };
    triggerActionCollection: any[];
    columns: any[];
    data: EventDefinition[];
    constructor(message: MessagerService, util: ProcessDeUtil, injector: Injector, cdr: ChangeDetectorRef, localeId: string, resources: any, localePipe: WfLocalePipe);
    ngOnInit(): void;
    add(): void;
    delete(): void;
    clickOK(): void;
    clickCancel(): void;
    private validator;
}
export declare class EventDefinitionConverter implements TypeConverter {
    private localePipe;
    constructor(localePipe: WfLocalePipe);
    convertTo(data: any[]): string;
}
