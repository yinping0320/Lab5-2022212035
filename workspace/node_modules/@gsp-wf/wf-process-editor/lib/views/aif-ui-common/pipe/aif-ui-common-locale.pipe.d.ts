import { PipeTransform, InjectionToken } from '@angular/core';
export declare const BIF_LANG_RESOURCES: InjectionToken<{}>;
export declare class AifLocalePipe implements PipeTransform {
    private readonly defaultLang;
    private langCode;
    private lang;
    private resources;
    constructor(localeId: string, resources: any);
    transform(name: string): any;
    instant(name: string): any;
}
