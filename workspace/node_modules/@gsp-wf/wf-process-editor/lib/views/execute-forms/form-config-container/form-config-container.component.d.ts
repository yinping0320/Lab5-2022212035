import { OnInit, ViewContainerRef, Injector, ComponentFactoryResolver, TemplateRef, SimpleChanges, OnChanges } from '@angular/core';
import { HttpService } from '@ecp-caf/caf-common';
import { Observable } from 'rxjs';
export declare class FormConfigContainerComponent implements OnInit, OnChanges {
    private injector;
    private cfr;
    private http;
    private componentRef;
    container: ViewContainerRef;
    modalFooter: TemplateRef<any>;
    formFormatId: string;
    formatConfigId: string;
    showMethodTab: boolean;
    constructor(injector: Injector, cfr: ComponentFactoryResolver, http: HttpService);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    submit(): Observable<any>;
    private showComponentRef;
    private createWFComponentRef;
    private getFormFormatType;
}
