import { ChangeDetectorRef, EventEmitter, OnInit, TemplateRef } from '@angular/core';
import { DatagridComponent } from '@farris/ui-datagrid';
import { WfLocalePipe } from '../../../i18n/pipe/wf-process-editor-locale.pipe';
export declare class VariableGridComponent implements OnInit {
    private cdr;
    private localeId;
    private resources;
    private localePipe;
    grid: DatagridComponent;
    buttonRef: TemplateRef<any>;
    cancel: EventEmitter<any>;
    submit: EventEmitter<any>;
    data: any[];
    columns: {
        field: string;
        width: number;
        title: any;
    }[];
    constructor(cdr: ChangeDetectorRef, localeId: string, resources: any, localePipe: WfLocalePipe);
    ngOnInit(): void;
    clickOK(): void;
    clickCancel(): void;
}
