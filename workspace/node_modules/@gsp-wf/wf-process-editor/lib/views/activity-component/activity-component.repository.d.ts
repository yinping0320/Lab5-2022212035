import { ActivityComponentUIState } from './activity-component.uistate';
import { HttpService } from '@ecp-caf/caf-common';
import { BizComponentEntity } from '../../domain/entities/biz-component.entity';
import { GspComponent } from '@gsp-cmp/common-component';
import { ProcessDesignerUIState } from '../../app/process-designer.uistate';
import { Subject } from 'rxjs';
import { ProcessDeUtil } from '../../domain/process-de-util';
export declare class ActivityComponentRepository {
    private uistate;
    private util;
    private http;
    private designerState;
    private defaultProcessComponentIds;
    subject: Subject<BizComponentEntity>;
    constructor(uistate: ActivityComponentUIState, util: ProcessDeUtil, http: HttpService, designerState: ProcessDesignerUIState);
    loadComponents(flowFormId: string, bizActId: string): void;
    removeComponent(id: string): void;
    addComponent(cpt: GspComponent): void;
    private bindParas;
    private assignParameterValue;
    swapArray(arr: Array<any>, i1: number, i2: number): Array<any>;
    getBizCmp(hander: any): void;
}
