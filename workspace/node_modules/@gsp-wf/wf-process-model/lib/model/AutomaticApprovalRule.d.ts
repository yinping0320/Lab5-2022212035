import { BpmnModel, ExtensionElement } from "@edp-pmf/bpmn-model";
import { Condition } from "./extension/condition-expression/Condition";
export declare class AutomaticApprovalRule extends ExtensionElement {
    static Clr_Type_ID: string;
    readonly ClrTypeID: string;
    /**
     * 自动审批规则标识
     * DUPLICATE_PARTICIPANT 重复审批人
     * CUSTOM_CONDITION 自定义条件
     * TIMEOUT_OPERATION 超时自动审批
     */
    Id: string;
    /**
     * 自动审批的结论项
     */
    conclusionValue: string;
    /**
     * 自动审批意见（备注）
     */
    comment: string;
    /**
     * 自动审批操作人显示文本
     */
    operatorText: string;
    /**
     * 自动审批规则条件
     */
    condition: Condition;
    /**
     * 自动审批操作人记录为当前活动参与者
     */
    participantAsOperator: boolean;
    constructor(model: BpmnModel);
    protected ResetBeforeLoadFrom(): void;
    ConvertToJsonObject(): any;
    LoadFromJsonObject(obj: any): void;
    NewInstance(): AutomaticApprovalRule;
    clone(): AutomaticApprovalRule;
}
