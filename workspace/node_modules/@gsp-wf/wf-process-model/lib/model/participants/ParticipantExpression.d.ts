import { BpmnModel, ExtensionElement } from "@edp-pmf/bpmn-model";
import { ActionScope } from "../ActionScope";
import { Condition } from "../extension";
export declare abstract class ParticipantExpression extends ExtensionElement {
    id: string;
    name: string;
    actionScope: ActionScope;
    condition: Condition;
    order: number;
    constructor(model: BpmnModel);
    ConvertToJsonObject(): any;
    LoadFromJsonObject(obj: any): void;
}
