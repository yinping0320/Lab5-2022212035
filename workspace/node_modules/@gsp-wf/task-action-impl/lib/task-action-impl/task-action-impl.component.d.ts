import { OnInit, ViewContainerRef, Injector, ComponentFactoryResolver, ComponentRef, EventEmitter, ElementRef, NgZone } from '@angular/core';
import { TaskAction, TaskEntity, ActionBaseImplComponent, SubmitResult } from '@gsp-wf/task-impl-api';
import { Observable } from 'rxjs';
import { LoadingService } from '@farris/ui-loading';
import { MessagerService } from '@farris/ui-messager';
import { TaskActionImplService } from '../task-action-impl.service';
export declare class TaskActionImplComponent implements OnInit {
    private injector;
    private cfr;
    private loadingService;
    private msgService;
    private taskService;
    container: ViewContainerRef;
    actions: TaskAction[];
    submitName: string;
    showButtons: boolean;
    batch: boolean;
    task: TaskEntity;
    _iframe: ElementRef<HTMLIFrameElement>;
    iframe: ElementRef<HTMLIFrameElement>;
    origin: string;
    submited: EventEmitter<SubmitResult>;
    actionCode: EventEmitter<any>;
    selectAction: TaskAction;
    actionCodeMap: Map<any, any>;
    componentRef: ComponentRef<ActionBaseImplComponent>;
    index: number;
    submitDisable: boolean;
    ifFocused: boolean;
    focus: string;
    ifShowFocus: boolean;
    dialogHeight: number;
    dialogWidth: number;
    ngZone: NgZone;
    constructor(injector: Injector, cfr: ComponentFactoryResolver, loadingService: LoadingService, msgService: MessagerService, taskService: TaskActionImplService);
    ngOnInit(): void;
    /**
     * 选中动作按钮
     * @param action
     * @param index
     * @returns
     */
    actionSelect(action: TaskAction, index?: number): Observable<any>;
    /**
     * 动态加载审批组件
     * @param cmpFactory
     */
    private loadComponentFactory;
    /**
     * 点击提交
     */
    submit(): void;
    /**
     * 是否提交前确认
     */
    private showConfirmDialog;
    private executeSubmit;
    private confirmSubmit;
    handleSubmit(): Observable<SubmitResult>;
    batchHandleSubmit(tasks: TaskEntity[]): Observable<SubmitResult>;
    private executeEvents;
    /**
     * 点击添加/取消关注
     */
    focusTask(): void;
    /**
     * 国际化
     * @param code
     * @returns
     */
    i18nService(code: string): any;
}
