import { Injector, ComponentFactoryResolver } from '@angular/core';
import { MessagerService } from '@farris/ui-messager';
import { BsModalService } from '@farris/ui-modal';
import { LogFilter } from './entity/LogParams';
export declare class WfApprovalLogsService {
    private msgService;
    private injector;
    private resolver;
    private modalService;
    dialog: any;
    constructor(msgService: MessagerService, injector: Injector, resolver: ComponentFactoryResolver, modalService: BsModalService);
    /**
     * 根据流程实例id打开审批记录弹框
     * @param procInstId 流程实例id
     */
    showByProcInstId(procInstId: string): void;
    /**
     * 根据表单数据id打开审批记录弹框
     * @param dataId 表单数据id
     * @param params 其他参数 params.style 为datagrid时，弹框使用大的（520,760,960）
     */
    showByDataId(dataId: string): void;
    /**
     * 查看审批记录
     * @param parameters
     */
    showApprovalLog(parameters: LogFilter): void;
    /**
     * 获取多语
     * @param name
     * @returns
     */
    private getI18nValue;
    private addEventListener;
    private beforeClose;
}
