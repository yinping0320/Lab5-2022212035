import { OnInit, ElementRef } from '@angular/core';
import { SubmitResult, TaskEntity, TaskAction } from '../entity';
import { Observable } from 'rxjs';
export declare class ActionBaseImplComponent implements OnInit {
    /**
     * 是否为批量操作
     */
    isBatch: boolean;
    /**
     * 提交按钮名称，支持重写
     */
    submitName: string;
    /**
     * 任务列表上弹框的高度
     */
    dialogHeight: number;
    /**
     * 任务列表上弹框的宽度
     */
    dialogWidth: number;
    /**
     * 动作执行来源（任务列表、任务办理界面）
     */
    origin: string;
    /**
     * 单据
     */
    iframe: ElementRef<HTMLIFrameElement>;
    constructor();
    ngOnInit(): void;
    /**
     * 任务赋值
     */
    setTask(t: TaskEntity): void;
    /**
     * 动作赋值
     */
    setAction(a: TaskAction): void;
    /**
     * 执行操作
     */
    submit(): Observable<SubmitResult>;
    /**
     * 批量执行操作
     */
    batchSubmit(tasks: TaskEntity[]): Observable<SubmitResult>;
}
