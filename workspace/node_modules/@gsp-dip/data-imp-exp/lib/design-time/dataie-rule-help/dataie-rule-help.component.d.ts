import { ComponentFactoryResolver, Injector, OnInit } from '@angular/core';
import { Observable } from "rxjs";
import { BsModalService } from "@farris/ui-modal";
export declare class DataieRuleHelpComponent implements OnInit {
    private injector;
    private modalService;
    private resolver;
    constructor(injector: Injector, modalService: BsModalService, resolver: ComponentFactoryResolver);
    ngOnInit(): void;
    /**
     * 打开导入导出规则id选择帮助
     * @param ruleType 规则类型，导入为0，导出为1
     * @param voId
     * @param beId
     */
    showRuleIdHelp(ruleType: number, voId: string, beId: string): Observable<any>;
}
