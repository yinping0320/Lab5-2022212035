import { EventEmitter, OnInit, TemplateRef } from '@angular/core';
import { DatagridComponent } from "@farris/ui-datagrid";
import { NotifyService } from "@farris/ui-notify";
import { LoadingService } from "@farris/ui-loading";
import { HttpClient } from "@angular/common/http";
export declare class RuleIdHelpComponent implements OnInit {
    private notify;
    private http;
    private loadingService;
    modalFooter: TemplateRef<any>;
    RuleIdHelpGrid: DatagridComponent;
    RuleIdHelpColumn: any;
    RuleIdHelpData: any;
    ruleId: string;
    closeModal: EventEmitter<any>;
    afterHelpConfirm: EventEmitter<any>;
    ruleType: number;
    beId: string;
    voId: string;
    fileTypes: {
        label: string;
        value: string;
    }[];
    constructor(notify: NotifyService, http: HttpClient, loadingService: LoadingService);
    ngOnInit(): void;
    okHandler(): void;
    cancelHandler(): void;
}
