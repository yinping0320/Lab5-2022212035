import { ComponentFactoryResolver, Injector } from '@angular/core';
import { BsModalService } from '@farris/ui-modal';
import { Repository } from '@farris/devkit';
import { FileOperateService } from './file-operate.service';
import { NotifyService } from '@farris/ui-notify';
import { HttpClient } from '@angular/common/http';
import { CommonParams } from "../Entity/CommonParams";
export declare class DataImportService {
    private modalService;
    private resolver;
    private operateService;
    private notify;
    private http;
    private injector;
    private origin;
    private url;
    DataIeProgressParams: {
        "ImportProgressValue": number;
    };
    complete: any;
    errorlevel: any;
    master_table_noid: string;
    bind_form_error: string;
    validate_result_show: string;
    constructor(modalService: BsModalService, resolver: ComponentFactoryResolver, operateService: FileOperateService, notify: NotifyService, http: HttpClient, injector: Injector);
    ImportSubTable(commonParams: CommonParams): boolean;
    ImportSubTable4HandCraft(commonParams: CommonParams): boolean;
    ImportByRuleId(commonParams: CommonParams): void;
    Import(commonParams: CommonParams): void;
    private ShowValidatedResult;
    private getBefRestService;
    private CreateValidatedComponent;
    JudgeIllegalChar(str: string): boolean;
    DowmloadTemplate(ruleId: string, repository: Repository<any>, importOption: any, suffix: any): void;
    DowmloadTemplate4HandCraft(ruleId: string, importOption: any, suffix: any): void;
}
