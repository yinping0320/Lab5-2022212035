import { ComponentFactoryResolver, Injector } from '@angular/core';
import { DataExportService } from './data-export.service';
import { NotifyService } from '@farris/ui-notify';
import { Subject, Observable } from 'rxjs';
import { BsModalService } from '@farris/ui-modal';
import { HttpClient } from '@angular/common/http';
import { CustomDataIEService } from './custom-data-ie.service';
import { LoadingService } from "@farris/ui-loading";
export declare class DataieHandcraftService {
    private resolver;
    private modalService;
    private injector;
    private dataExportService;
    private notify;
    private customIEService;
    private http;
    private loadingService;
    data_import: string;
    data_export: string;
    data_export_customfile: string;
    export_success: string;
    export_fail: string;
    i18nResource: any;
    constructor(resolver: ComponentFactoryResolver, modalService: BsModalService, injector: Injector, dataExportService: DataExportService, notify: NotifyService, customIEService: CustomDataIEService, http: HttpClient, loadingService: LoadingService);
    /**
     * 通用导出方法
     * @param type
     * @param ruleID
     * @param option
     */
    CommonDataExport(type: any, ruleID: string, option: string): Observable<any>;
    ExportByRule(ruleID: string, option: any): Subject<any>;
    ExportCurrentChildData(ruleID: string, option: string): Observable<any>;
    private ExportCommonEntry;
    private ExportByRuleImpl;
    /**
     * 通用导入方法
     * @param type
     * @param ruleID
     * @param option
     */
    CommonDataImport(type: any, ruleID: string, option: string): Observable<any>;
    ImportByRule(ruleID: string, option: any): Observable<any>;
    ImportSublistData(ruleID: string, option: any): Observable<any>;
    private ImportCommonEntry;
    private ImportByRuleImpl;
    private ParseExportJson;
    private ParseImportJson;
    StartLoading(): void;
    CloseLoading(): void;
}
