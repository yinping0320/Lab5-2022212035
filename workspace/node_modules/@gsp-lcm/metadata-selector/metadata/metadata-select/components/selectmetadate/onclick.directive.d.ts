import { OnDestroy, OnInit, EventEmitter, Renderer2, ElementRef } from '@angular/core';
export declare class OnceClickDirective implements OnInit, OnDestroy {
    private renderer;
    private element;
    clickCall: EventEmitter<MouseEvent>;
    duration: number;
    private $sub;
    private subscription;
    constructor(renderer: Renderer2, // Angular 2.x导入Renderer
    element: ElementRef);
    ngOnInit(): void;
    clickEvent(event: MouseEvent): void;
    ngOnDestroy(): void;
}
