import { OnInit, EventEmitter } from '@angular/core';
import { ComboListComponent } from '@farris/ui-combo-list';
import { BusinessObjectEntity, BusinessObjectDtService } from '@gsp-lcm/bo-dt-service';
import { BoCreatorUtils } from '../../utils/bo-create-utils';
import { NotifyService } from '@farris/ui-notify';
import { ValidatorResult } from '../../common/validation-result';
export declare class BoCreatorModalComponent implements OnInit {
    private boCreatorUtils;
    private boDtService;
    private notifyService;
    parentId: string;
    _parentId: string;
    boConfirmEmitter: EventEmitter<any>;
    boGroupSelector: ComboListComponent;
    /**
     * 当前BO节点
     */
    currentBo: BusinessObjectEntity;
    /**
     * 关键应用集合
     */
    items: BusinessObjectEntity[];
    /**
     * 选择应用节点下的所有服务单元集合
     */
    suList: BusinessObjectEntity[];
    /**
     * 当前选择的SU下的所有BO
     */
    bosInSU: BusinessObjectEntity[];
    /**
     * 所有业务对象节点集合
     */
    data: any[];
    /**
     * 所属微服务
     */
    suCode: string;
    parentBo: BusinessObjectEntity;
    labelLength: string;
    inputLength: string;
    constructor(boCreatorUtils: BoCreatorUtils, boDtService: BusinessObjectDtService, notifyService: NotifyService);
    ngOnInit(): void;
    private initData;
    create(): void;
    getCurrentBo(): BusinessObjectEntity;
    boInfoValidator(): ValidatorResult;
    emptyValidator(): ValidatorResult;
    namingConventionValidator(): ValidatorResult;
    repeatValidator(): ValidatorResult;
    boGroupSelectChanged($event: any): void;
    private getParentBo;
    clearHandler(): void;
}
