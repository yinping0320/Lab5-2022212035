import { Observable } from 'rxjs/Observable';
import { HttpClient } from '@angular/common/http';
export declare class DboHelpDevService {
    private rest;
    private databaseurlDev;
    constructor(rest: HttpClient);
    sessionId: string;
    /**
     * 数据库对象选择帮助界面（设计时）
     */
    getAllDatabaseObject(relativePath: string): Observable<any>;
    /**
     * 数据库对象详细信息选择帮助界面
     */
    getCurrentDatabaseObject(relativePath: string, databaseobjectid: string): Observable<any>;
}
