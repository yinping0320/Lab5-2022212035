import { OnChanges, EventEmitter, SimpleChanges, ComponentFactoryResolver, Injector } from '@angular/core';
import { MetadataRTService } from '../../metadatart.service';
import { BsModalService } from '@farris/ui-modal';
export declare class SelectMetadataRTComponent implements OnChanges {
    private metadataService;
    private modalService;
    private resolver;
    private injector;
    typeList: string;
    boId: string;
    bizTypeId: string;
    classifier: string;
    closeModalEmitter: EventEmitter<any>;
    selectedMetadata: EventEmitter<any>;
    confirm: string;
    cancel: string;
    private notifyService;
    constructor(metadataService: MetadataRTService, modalService: BsModalService, resolver: ComponentFactoryResolver, injector: Injector);
    ngOnChanges(changes: SimpleChanges): void;
    LoadMetadata(): void;
    hideModal(): void;
    onSelectedChange(classifier: string): void;
}
