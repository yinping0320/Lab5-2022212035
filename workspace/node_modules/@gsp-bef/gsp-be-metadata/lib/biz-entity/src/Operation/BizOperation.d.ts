import { BEOperationType } from '../Enum/BEOperationType';
/**
* BE操作基类.
* BE操作，是基于服务端业务逻辑构件的，每个操作都是其中的一个方法。
* 但BE对用户隐藏了该层关系，即让用户感觉不到后台构件的存在。
* 应该确保BE操作的Code、Name与Method的Code、Name的一致。
* @author wangjiegj<wangjiegj@inspur.com>
 */
/**
* BE操作基类.
* BE操作，是基于服务端业务逻辑构件的，每个操作都是其中的一个方法。
* 但BE对用户隐藏了该层关系，即让用户感觉不到后台构件的存在。
* 应该确保BE操作的Code、Name与Method的Code、Name的一致。
*/
export declare class BizOperation {
    constructor();
    /**
     * 唯一标志
     */
    ID: string;
    /**
     * 编号
     */
    Code: string;
    /**
     * 名称
     */
    Name: string;
    /**
     * 描述
     */
    Description: string;
    /**
     * 方法Id
     * 2018.4.27修改，单构件对应单方法，方法Id可不记录
     */
    /**
     * 对应构件实体Id
     */
    ComponentId: string;
    /**
     * 对应构件实体包名
     */
    ComponentPkgName: string;
    /**
     * 对应构件实体名
     */
    ComponentName: string;
    /**
     * 是否可见
     */
    IsVisible: boolean;
    /**
     * 操作类型
     */
    OpType: BEOperationType;
    /**
     * 所属的Node节点
     */
    /**
     * 所属扩展模型元数据ID
     */
    BelongModelID: string;
    /**
     * 是否引用
     */
    IsRef: boolean;
    /**
     * 是否生成构件
     */
    IsGenerateComponent: boolean;
}
