import { OnInit, ElementRef, OnChanges, AfterViewChecked } from '@angular/core';
import { FileDescription } from '../entities/filedescription';
import { ZoomService } from '../../service/zoom.service';
import { WindowService } from '../../service/window.service';
import { NavigateService } from '../../service/navigate.service';
export declare class GspDocumentComponent implements OnInit, OnChanges, AfterViewChecked, OnChanges {
    protected _elementRef: ElementRef<HTMLElement>;
    private _zoomService;
    private _windowService;
    private _navigateService;
    mode: boolean;
    preloadPageCount: number;
    file: FileDescription;
    selectedPage: number;
    showActiveSlide: boolean;
    wait: boolean;
    zoom: number;
    docWidth: any;
    docHeight: any;
    viewportWidth: any;
    viewportHeight: any;
    scale: any;
    lastScale: any;
    container: any;
    doc: any;
    x: number;
    lastX: number;
    y: number;
    lastY: number;
    pinchCenter: any;
    pinchCenterOffset: any;
    curWidth: number;
    curHeight: number;
    isDesktop: boolean;
    refreshView: boolean;
    constructor(_elementRef: ElementRef<HTMLElement>, _zoomService: ZoomService, _windowService: WindowService, _navigateService: NavigateService);
    ngOnInit(): void;
    ngOnChanges(): void;
    ngAfterViewInit(): void;
    ifExcel(): boolean;
    ifPresentation(): boolean;
    getDimensionWithUnit(value: number, pageNumber: number): any;
    ifChromeOrFirefox(): boolean;
    ifFirefox(): boolean;
    ifEdge(): boolean;
    ngAfterViewChecked(): void;
    isVisible(pageNumber: any): boolean;
}
