import { HttpService } from '@ecp-caf/caf-common';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { DocViewRequest } from '../doc-viewer/entity/docviewrequest';
import { FileCredentials } from './file.service';
import { ConfigService } from './config.service';
export declare class DocViewerService {
    private http;
    private _http;
    private _config;
    constructor(http: HttpService, _http: HttpClient, _config: ConfigService);
    private setHeader;
    loadFileInfo(body: DocViewRequest): Observable<any>;
    loadPage(credentials: FileCredentials, page: number): Observable<Object>;
    rotate(credentials: FileCredentials, angle: number, page: number): Observable<any>;
}
