import { HttpClient } from "@angular/common/http";
import { Observable } from "rxjs";
import { DocViewerConfig } from '../doc-viewer/entity/docviewerconfig';
import { ConfigService } from './config.service';
export declare class ViewerConfigService {
    private _http;
    private _config;
    private _viewerConfig;
    private _updatedConfig;
    constructor(_http: HttpClient, _config: ConfigService);
    readonly updatedConfig: Observable<DocViewerConfig>;
    load(): Promise<void>;
}
