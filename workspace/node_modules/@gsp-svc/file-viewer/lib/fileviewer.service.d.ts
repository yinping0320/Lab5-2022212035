import { Injector, ComponentFactoryResolver } from '@angular/core';
import { BsModalService, BsModalRef } from '@farris/ui-modal';
import { FileListService } from './service/file-list.service';
import { MessagerService } from '@farris/ui-messager';
import { CloudprintService } from "@gsp-svc/cloudprint";
import { PdfViewOptions } from './pdf-viewer/entitires/pdfviewoptions';
import { AppService } from '@gsp-sys/rtf-common';
export declare class FileViewerService {
    private modalService;
    private componentFactoryResolver;
    private injector;
    private fileListService;
    private msgService;
    private cloudprintService;
    private appService;
    constructor(modalService: BsModalService, componentFactoryResolver: ComponentFactoryResolver, injector: Injector, fileListService: FileListService, msgService: MessagerService, cloudprintService: CloudprintService, appService: AppService);
    dlg: BsModalRef;
    windowWidth: number;
    windowHeigth: number;
    viewerFormFile(formId: string, rootId: string, viewOptions?: PdfViewOptions): Promise<{}>;
    viewerFileList(metadataIdList: string[], rootId: string, viewOptions?: PdfViewOptions): Promise<{}>;
    viewerFormFileWithIndex(formId: string, rootId: string, metadataId: string, viewOptions?: PdfViewOptions): Promise<{}>;
    viewerFileListWithIndex(metadataIdList: string[], rootId: string, metadataId: string, viewOptions?: PdfViewOptions): Promise<{}>;
    mergeAndViewPdfFiles(metadataIdList: string[], rootId: string): void;
    viewerFormFileInpage(formId: string, rootId: string, viewOptions?: PdfViewOptions): void;
    viewerFileListInPage(metadataIdList: string[], rootId: string, viewOptions?: PdfViewOptions): void;
    viewerFormFileWithIndexInPage(formId: string, rootId: string, metadataId: string, viewOptions?: PdfViewOptions): void;
    viewerFileListWithIndexInPage(metadataIdList: string[], rootId: string, metadataId: string, viewOptions?: PdfViewOptions): void;
}
