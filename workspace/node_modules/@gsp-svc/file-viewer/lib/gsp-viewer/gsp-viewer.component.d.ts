import { OnInit, OnChanges, SimpleChanges } from '@angular/core';
import { StorageType } from '../file-list-mobile/entity/storagetype';
import { FileListService } from '../service/file-list.service';
import { LoadingService } from '@farris/ui-loading';
import { MessagerService } from '@farris/ui-messager';
import { ImageViewerComponent } from '../image-viewer/image-viewer/image-viewer.component';
import { DocViewerComponent } from '../doc-viewer/doc-viewer/doc-viewer.component';
import { OfdViewerComponent } from '../ofd-viewer/ofd-viewer/ofd-viewer.component';
import { PdfViewerComponent } from '../pdf-viewer/pdf-viewer/pdf-viewer.component';
import { PdfViewOptions } from '../pdf-viewer/entitires/pdfviewoptions';
export declare class GspViewerComponent implements OnInit, OnChanges {
    private listService;
    private msgService;
    private loadService;
    filePath: string;
    metadataId: string;
    rootId: string;
    fileName: string;
    fileType: StorageType;
    viewOptions: PdfViewOptions;
    serviceUnit: string;
    params: string;
    file: any;
    isImage: boolean;
    isOfd: boolean;
    isPdf: boolean;
    imageUrl: string;
    docPath: string;
    isDocShow: boolean;
    isOfdShow: boolean;
    isPdfShow: boolean;
    imageViewer: ImageViewerComponent;
    docViewer: DocViewerComponent;
    ofdViewer: OfdViewerComponent;
    pdfViewer: PdfViewerComponent;
    constructor(listService: FileListService, msgService: MessagerService, loadService: LoadingService);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    viewFile(filePath: string, metadataId: string, rootId: string, fileType: StorageType, fileName: string, suName: string, params: string): void;
    fileChange(): void;
    imageChange(url: any): void;
    docChange(filePath: string): void;
    pdfChange(filePath: string): void;
    ofdChange(filePath: string): void;
}
