/**
 * 注入实体信息
 * @property name 注入对象在schema中的名称
 * @property schemaName 注入对象所在schema
 * @property injectedType 注入对象类型
 */
export declare class FilterInjectedEntity {
    name: string;
    schemaName: string;
    injectedType: FilterInjectedType;
}
export declare enum FilterInjectedType {
    EntitySet = 0,
    EntityType = 1
}
