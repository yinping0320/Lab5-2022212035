import { PipeTransform } from '@angular/core';
export declare class FilterConditionLocalePipe implements PipeTransform {
    private readonly defaultLang;
    private langCode;
    private lang;
    constructor(localeId: string);
    transform(name: string): any;
}
