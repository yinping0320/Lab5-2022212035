import { OnInit, EventEmitter } from '@angular/core';
import { Schema } from '../schema';
import { TreeNode } from '@farris/ui-treetable';
import { CurrentNode } from '../currentNode';
export declare class ExpressionSchemaGridComponent implements OnInit {
    schema: Map<string, Schema>;
    selectschemachange: EventEmitter<{}>;
    currentNode: CurrentNode;
    cols: {
        field: string;
        title: string;
    }[];
    treedata: TreeNode[];
    constructor();
    ngOnInit(): void;
    selectRowChange($event: any): void;
    convertSchema(): void;
}
