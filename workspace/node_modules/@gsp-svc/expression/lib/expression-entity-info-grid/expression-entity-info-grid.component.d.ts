import { OnInit, EventEmitter } from '@angular/core';
import { Schema } from '../schema';
import { CurrentNode } from '../currentNode';
import { NewSchema } from '../i18n/NewSchema';
export declare class ExpressionEntityInfoGridComponent implements OnInit {
    private localeId;
    private localePipe;
    schema: Map<string, Schema>;
    newSchemaMap: Map<string, NewSchema>;
    choseEntityTableName: CurrentNode;
    selectSchemaRow: EventEmitter<{}>;
    dbclickInsert: EventEmitter<{}>;
    selectString: string;
    items: any[];
    columns: any[];
    constructor();
    ngOnInit(): void;
    ngOnChanges(): void;
    ondblclickRow($event: any): void;
    selectRowChange($event: any): void;
}
