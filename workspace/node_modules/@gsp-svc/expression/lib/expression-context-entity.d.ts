import { VarEntity } from './varentity';
import { FuncEntity } from './funcentity';
import { Schema } from './schema';
import { InjectedEntity } from './injected-entity';
import { SmartHelpEntity } from './smartHelpEntity';
import { NewSchema } from './i18n/NewSchema';
/**
 * 内部上下文实体
 * @property sessionEntities 预置的系统变量列表
 * @property contextEntities 预置的上下文变量列表
 * @property funcEntities 预置的函数列表
 * @property expr 预置的表达式文本
 * @property schemaMap 预置schema键值对
 * @property newSchemaMap 预置newSchema键值对
 * @property injectedEntities 注入实体对象
 * @property smartHelpEntities 注入帮助
 */
export declare class ExpressionContextEntity {
    sessionEntities: VarEntity[];
    contextEntities: VarEntity[];
    funcEntities: FuncEntity[];
    expr: string;
    sexpr: string;
    schemaMap: Map<string, Schema>;
    newSchemaMap: Map<string, NewSchema>;
    injectedEntities: InjectedEntity[];
    smartHelpEntities: SmartHelpEntity[];
}
