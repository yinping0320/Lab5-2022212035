import { OnInit, OnChanges, EventEmitter } from '@angular/core';
import { Schema, Property } from '../schema';
import { CurrentNode } from '../currentNode';
export declare class ExpressionSchemaInfoGridComponent implements OnInit, OnChanges {
    schema: Map<string, Schema>;
    choseSchemaTableName: CurrentNode;
    selectSchemaRow: EventEmitter<{}>;
    dbclickInsert: EventEmitter<{}>;
    selectString: string;
    items: Property[];
    columns: {
        field: string;
        title: string;
        filter: boolean;
    }[];
    constructor();
    ngOnInit(): void;
    ngOnChanges(): void;
    ondblclickRow($event: any): void;
    selectRowChange($event: any): void;
}
