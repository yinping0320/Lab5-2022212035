import { HttpClient } from '@angular/common/http';
import { OnInit, EventEmitter } from '@angular/core';
export declare class ExpressionVarGridComponent implements OnInit {
    private localeId;
    private localePipe;
    constructor();
    h1: HttpClient;
    selectVarRow: EventEmitter<{}>;
    columns: {
        field: string;
        title: string;
    }[];
    items: any[];
    ngOnInit(): void;
    selectRowChange($event: any): void;
}
