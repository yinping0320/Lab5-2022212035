import { Injector, ApplicationRef } from '@angular/core';
import { ComponentFactoryResolver } from '@angular/core';
import { PrintEntity } from './entitys/printEntity';
import { NotifyService } from '@farris/ui-notify';
import { OutputOptions } from './entitys/outputoptions/outputOptions';
import { AppService } from '@gsp-sys/rtf-common';
import { PrintFormatService } from './services/printFormatService';
import { BsModalService } from '@farris/ui-modal';
import { LocalLangPipe } from './local.pipe';
import { AttachmentOptions } from './entitys/datasourceoptions/attachmentoptions';
import { HttpClient } from '@angular/common/http';
/**
 * 附件打印的服务
 */
export declare class CloudPrintAttachmentService {
    private formatSrv;
    private resolver;
    private injector;
    private applicationRef;
    private notifyService;
    private appService;
    private modalService;
    private httpClient;
    /**
     * 打印参数实体
     */
    printEntity: PrintEntity;
    /**
     * 设置PrintEntity参数
     * @description 如果外界new新的PrintEntity，则需要调用该方法同步service的entity
     *              外界没有new新的PrintEntity，则不需要调用该方法
     */
    setPrintEntity: any;
    localeid: string;
    localpipe: LocalLangPipe;
    constructor(formatSrv: PrintFormatService, resolver: ComponentFactoryResolver, injector: Injector, applicationRef: ApplicationRef, notifyService: NotifyService, appService: AppService, modalService: BsModalService, httpClient: HttpClient);
    printAttachment(attachmentoptions: AttachmentOptions, options: OutputOptions, su?: any, param?: any): Promise<void>;
    private printAttachmentJob;
}
