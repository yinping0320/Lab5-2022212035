export declare class Utils {
    /**
     * 判断是否PC端 or 移动端
     * true:PC
     * false:Mobile
     */
    static isPC(): boolean;
    static uuid(): string;
    static renderPdfByByte(rendedFormat: any, ele: any): void;
    private static gen;
}
