/**
 * 云打印函数工具类
 */
export declare class PrintFunctionUtils {
    /**
     * 将数字转为汉字
     * @param number 数值
     */
    static FormatToCurrencyNumber(number: number): string;
    /**
     * 将数字转换为汉字
     * @param number 数值
     */
    static FormatToCurrencyNumberCN(number: number): string;
    static ConvertCurrency(money: any): string;
    /**
     * 将数值格式化为中文大写货币金额，形如【壹拾万柒仟元零伍角叁分】
     */
    static FormatToCurrencyCN(money: number): string;
    static FormatToCurrency(money: number): string;
    private static insert_item;
    /**
    * 对日期进行格式化， 和C#大致一致 默认yyyy-MM-dd HH:mm:ss
    * 可不带参数 一个日期参数 或一个格式化参数
    * @param date 要格式化的日期
    * @param format 进行格式化的模式字符串
    *     支持的模式字母有：
    *     y:年,
    *     M:年中的月份(1-12),
    *     d:月份中的天(1-31),
    *     H:小时(0-23),
    *     h:小时(0-11),
    *     m:分(0-59),
    *     s:秒(0-59),
    *     f:毫秒(0-999),
    *     q:季度(1-4)
    * @return String
    * @author adswads@gmail.com
    */
    static dateFormat(date?: any, format?: string): string;
    /**
    * 返回字符串 为n个char构成
    * @param char 重复的字符
    * @param count 次数
    * @return String
    * @author adswads@gmail.com
    */
    private static charString;
    /**
     * 格式化数字
     * @param  number  [要格式化的数字]
     * @param  decimals         [保留几位小数]
     * @param  dec_point        [小数点符号]
     * @param  thousands_sep    [千分位符号]
     * @param  roundtag         [舍入参数，默认 "ceil" 向上取,"floor"向下取,"round" 四舍五入]
     * @return  格式化后的结果
     */
    private static number_format;
}
