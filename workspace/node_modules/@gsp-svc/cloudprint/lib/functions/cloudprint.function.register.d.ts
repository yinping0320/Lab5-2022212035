import { HttpClient } from '@angular/common/http';
export declare class CloudprintFunctionRegister {
    private http;
    private httpoptions;
    constructor(http: HttpClient);
    /**
     * 初始化自定义函数
     */
    initCustomFunction(configUrl: any): Promise<any>;
    /**
     * 添加js业务函数插件
     */
    private addJsFuncPlugins;
}
