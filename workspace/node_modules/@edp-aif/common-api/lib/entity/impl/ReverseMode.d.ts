export declare enum ReverseMode {
    /**
     * 覆盖。
     */
    COVERING = "COVERING",
    /**
     * 累加。
     */
    ACCUMULATING = "ACCUMULATING",
    /**
     * 扣减。
     */
    DEDUCTION = "DEDUCTION",
    /**
     * 表达式。
     */
    EXPRESSION = "EXPRESSION"
}
