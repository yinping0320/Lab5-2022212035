import { Injector, ComponentFactoryResolver, NgModuleFactoryLoader } from '@angular/core';
import { ModalOptions, BsModalService } from '@farris/ui-modal';
import { FormSettings, ISlimCreationRuleEntityEx } from '@edp-aif/common-api';
import { RpcCreationRuleExecutorResult } from '../../entity';
import { AifDataService } from './aif-data.service';
import { FrameContext, Entity } from '@farris/devkit';
import { Observable } from 'rxjs';
import { AbstractAifUiService } from './abstract-aif-ui-service';
export declare class AifTargetUiService extends AbstractAifUiService {
    protected aifDataService: AifDataService;
    protected frameContext: FrameContext;
    protected modalService: BsModalService;
    protected injector: Injector;
    protected cfr: ComponentFactoryResolver;
    protected loader: NgModuleFactoryLoader;
    localeId: string;
    private readonly aifUIStateCreationRuleResultKey;
    constructor(aifDataService: AifDataService, frameContext: FrameContext, modalService: BsModalService, injector: Injector, cfr: ComponentFactoryResolver, loader: NgModuleFactoryLoader, localeId: string);
    /**
     * 参照时打开生单规则选择器
     */
    openCreationRuleSelector4Refer(targetVoId: string, bizFlowchartId?: string, modalOptions?: ModalOptions, customSelectorModuleUrl?: string): Observable<ISlimCreationRuleEntityEx>;
    /**
     * 特定参照时打开生单规则选择器
     */
    openCreationRuleSelector4SpecificRefer(sourceVoId: string, targetVoId: string, bizFlowchartId?: string, modalOptions?: ModalOptions, customSelectorModuleUrl?: string): Observable<ISlimCreationRuleEntityEx>;
    /**
     * 打开来源单据选择器
     * @param formSettings
     * @param modalOptions
     */
    openSourceDataSelector4Refer(formSettings?: FormSettings, modalOptions?: ModalOptions): Observable<Entity[]>;
    /**
     * 获取传到根组件UIState上的生单规则执行器结果
     */
    getRpcCreationRuleExecutorResult(): RpcCreationRuleExecutorResult;
}
