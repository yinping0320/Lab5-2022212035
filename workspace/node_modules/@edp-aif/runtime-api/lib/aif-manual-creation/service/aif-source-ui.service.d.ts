import { Injector, ComponentFactoryResolver, NgModuleFactoryLoader } from "@angular/core";
import { Observable } from 'rxjs';
import { ISlimCreationRuleEntityEx, FormSettings } from '@edp-aif/common-api';
import { ModalOptions, BsModalService } from '@farris/ui-modal';
import { RpcCreationRuleExecutorResult } from '../../entity';
import { AifDataService } from './aif-data.service';
import { FrameContext } from '@farris/devkit';
import { NavigationService } from '@farris/command-services';
import { AbstractAifUiService } from './abstract-aif-ui-service';
export declare class AifSourceUiService extends AbstractAifUiService {
    protected aifDataService: AifDataService;
    protected navigationService: NavigationService;
    protected frameContext: FrameContext;
    protected modalService: BsModalService;
    protected injector: Injector;
    protected cfr: ComponentFactoryResolver;
    protected loader: NgModuleFactoryLoader;
    localeId: string;
    private readonly aifUIStateCreationRuleResultKey;
    constructor(aifDataService: AifDataService, navigationService: NavigationService, frameContext: FrameContext, modalService: BsModalService, injector: Injector, cfr: ComponentFactoryResolver, loader: NgModuleFactoryLoader, localeId: string);
    /**
     * 下推时打开生单规则选择器
     */
    openCreationRuleSelector4Push(sourceVoId: string, bizFlowchartId?: string, modalOptions?: ModalOptions, customSelectorModuleUrl?: string): Observable<ISlimCreationRuleEntityEx>;
    /**
     * 特定下推时打开生单规则选择器
     */
    openCreationRuleSelector4SpecificPush(sourceVoId: string, targetVoId: string, bizFlowchartId?: string, modalOptions?: ModalOptions, customSelectorModuleUrl?: string): Observable<ISlimCreationRuleEntityEx>;
    /**
     * 打开下游单据制单界面
     * @param formSettings
     * @param rpcCreationRuleExecutorResult
     * @param extParams
     */
    openTargetForm(formSettings: FormSettings, rpcCreationRuleExecutorResult: RpcCreationRuleExecutorResult, extParams?: {}): Observable<ISlimCreationRuleEntityEx>;
    /**
     *
     * @param action 目标App动作
     * @param pairs 其他实体参数键值对
     */
    private buildRouteParams;
}
