import { SchemaManagerService } from "@qdp/common";
import { Observable } from "rxjs";
export declare class SchemaUtils {
    schemaManager: SchemaManagerService;
    cacheService: any;
    constructor(_serviceCache: any);
    private init;
    /**
     * 获取格式方案信息
     * @param option 参数配置项
     * { schemaId: 方案ID, schemaValue: 格式方案（交叉表時快速設置時使用）, controlType: 查询类型：list  treelist  crosstab, queryId: 查询ID, queryRelativeUrl：eapi基础uri, groupType: 方案分组标识 }
     * @returns 方案列表
     */
    getSchemaInfo(option: any): Observable<any>;
    /**
     * 获取默认格式方案
     * @param option 参数配置项
     * { queryId：查询ID, queryRelativeUrl：eapi基础uri, groupType: 方案分组标识 }
     * @returns 默认格式方案
     */
    getDefaultSchemaInfo(option: any): Observable<any>;
}
