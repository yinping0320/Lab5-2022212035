import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
export declare type RestfulMethod = 'GET' | 'PUT' | 'POST' | 'DELETE';
export declare class RestfulService {
    private httpClient;
    /**
     * 构造函数
     * @param httpClient http客户端
     */
    constructor(httpClient: HttpClient);
    /**
     * 发送GET请求
     * @param uri 请求地址
     * @param params 请求参数
     */
    get(uri: string, params?: object, options?: any): Observable<any[]>;
    /**
     * 发送POST请求
     * @param uri 请求地址
     * @param params 请求参数
     */
    put(uri: string, body: any, params?: object, options?: any): Observable<any>;
    /**
     * 发送PUT请求
     * @param uri 请求地址
     * @param params 请求参数
     */
    post(uri: string, body: any, params?: object, options?: any): Observable<any>;
    /**
     * 发送DELETE请求
     * @param uri 请求地址
     * @param params 请求参数
     */
    delete(uri: string, params?: object, options?: any): Observable<any>;
    /**
     * 发送请求
     * @param uri 请求地址
     * @param params 请求参数
     */
    private request;
    /**
     * 构造Http请求参数
     * @param params 请求参数
     */
    private buildParams;
    private addBody;
}
