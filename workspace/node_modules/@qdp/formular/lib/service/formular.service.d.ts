import { ComponentFactoryResolver, Injector } from '@angular/core';
import { Observable } from 'rxjs';
import { MessagerService } from '@farris/ui-messager';
import { BsModalService, BsModalRef } from '@farris/ui-modal';
import { EntityDataService } from './entitydata.service';
import { LocalizeService } from '@qdp/localize';
export declare class FormularService {
    private modalService;
    private injector;
    private cfr;
    private msgService;
    private entityDataService;
    private url;
    private localizeService;
    dlg: BsModalRef;
    isFinish: any;
    constructor(modalService: BsModalService, injector: Injector, cfr: ComponentFactoryResolver, msgService: MessagerService, entityDataService: EntityDataService, url: any, localizeService: LocalizeService);
    showFormularDefine(currentSU: string, currentFormularId: any, formularType?: 'public'): Observable<any>;
}
