import { OnInit } from '@angular/core';
import { LookupGridComponent, PickingResult } from '@farris/ui-lookup';
import { RestfulService } from '../service/restful.service';
import { Observable } from 'rxjs';
export declare class HelpBeforeEventDirective implements OnInit {
    private lookup;
    private restfulService;
    helpData: any;
    localHost: any;
    dynamicInput: any;
    constructor(lookup: LookupGridComponent, restfulService: RestfulService);
    ngOnInit(): void;
    dictPicking: () => Observable<PickingResult>;
}
