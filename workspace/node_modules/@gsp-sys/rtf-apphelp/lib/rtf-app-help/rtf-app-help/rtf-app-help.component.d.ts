import { RtfApp } from './../entities/rtf-app';
import { OnInit, EventEmitter, Injector, ComponentFactoryResolver } from '@angular/core';
import { BsModalService } from '@farris/ui-modal';
import { RtfUIWebapiService } from '../../common/rtf-ui-webapi.service';
import { MessagerService } from '@farris/ui-messager';
export declare class RtfAppHelpComponent implements OnInit {
    private rtfCommon;
    private modalService;
    private farrisMsg;
    private injector;
    private cfr;
    /**
     * 所属的业务对象内码
     */
    bizObjectId: string;
    /**
     * 初始选中的权限对象内码
     */
    initSelectKeys: string[];
    languageCode: string;
    /**
     * 输出事件
     */
    afterHelpConfirm: EventEmitter<RtfApp[]>;
    private funcOpComp;
    private funcOpDialog;
    constructor(rtfCommon: RtfUIWebapiService, modalService: BsModalService, farrisMsg: MessagerService, injector: Injector, cfr: ComponentFactoryResolver);
    ngOnInit(): void;
    /**
     * 弹出Farris模态框帮助
     */
    showHelp(sessionId?: string): void;
}
