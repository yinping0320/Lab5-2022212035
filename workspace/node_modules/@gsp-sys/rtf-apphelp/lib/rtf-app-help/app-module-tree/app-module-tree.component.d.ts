import { OnInit, EventEmitter, OnChanges, SimpleChanges } from '@angular/core';
import { RtfApp } from '../entities/rtf-app';
export declare class AppModuleTreeComponent implements OnInit, OnChanges {
    /**
     * 菜单明细类
     */
    appTreeData: RtfApp[];
    /**业务对象 */
    bizObjectId: string;
    selectKeys: string[];
    currentid: string;
    /**功能菜单模块变化后后 */
    appTreeChanged: EventEmitter<RtfApp>;
    /**树控件展开的节点值 */
    expandedKeys: any[];
    constructor();
    /**
     * 变化情况
     * @param changes 变化
     */
    ngOnChanges(changes: SimpleChanges): void;
    ngOnInit(): void;
    /**选中行改变后触发事件 */
    selectChange(args: any): void;
}
