import { OnInit, EventEmitter, Injector, ComponentFactoryResolver } from '@angular/core';
import { SysFrontUtil } from '../../sys-common/sys-front.util';
import { BsModalService } from '@farris/ui-modal';
import { SysLocaleService } from '../../locale/sys-locale.service';
export declare class SysFuncOpHelpComponent implements OnInit {
    private modalService;
    private injector;
    private cfr;
    private sysUtil;
    private localService;
    /** 所属的业务对象内码 */
    bizObjectId: string;
    /** 是否启用新UI,启用后性能提升 */
    useNewUI: boolean;
    /**
     * 初始选中的权限对象内码
     */
    initSelectKeys: string[];
    /** 是否只显示分组 */
    onlyShowGroup: boolean;
    gridSelectMode: 'single' | 'multiple';
    /** 分组能否被选择 */
    isGroupCanSelected: boolean;
    /**
     * 输出事件
     */
    afterHelpConfirm: EventEmitter<object[]>;
    private funcOpComp;
    private funcOpDialog;
    i18nRes: any;
    constructor(modalService: BsModalService, injector: Injector, cfr: ComponentFactoryResolver, sysUtil: SysFrontUtil, localService: SysLocaleService);
    ngOnInit(): void;
    /**
     * 新UI
     */
    showNewHelp(sessionId: any): void;
    /**
     * 弹出Farris模态框帮助
     */
    showHelp(sessionId: string): void;
}
