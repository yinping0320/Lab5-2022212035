import { HttpHeaders } from '@angular/common/http';
import { SessionService } from '@ecp-caf/caf-common';
import { BizDataDTO } from '../entity/auresult.entity';
import { TreeTableComponent } from '@farris/ui-treetable';
export declare class RacCommonUtil {
    private sessionSv;
    constructor(sessionSv: SessionService);
    sessionId: string;
    ownerId: string;
    ownerCode: string;
    ownerName: string;
    /** 权限所有者类型 岗位1，用户2 */
    ownerType: number;
    /**  岗位类型 组织岗0，通用岗1，用户2 TODO 命令不支持传参数，暂时这么处理 */
    posType: number;
    packageId: string;
    year: any;
    /** 维度1的值 */
    assid1: string;
    /** 维度2的值 */
    assid2: string;
    funcId: string;
    AUDATACONTROL: string;
    /**
     * 构造Header
     */
    getHeader(): {
        headers: HttpHeaders;
    };
    /**
     * 获取上下文中的用户
     */
    getCurrentUserId(): string;
    /** Guid生成 */
    getGuid36(): string;
    /**
     * 权限分配结果路径
     */
    getResultPath(): any;
    /**
     * 权限对象路径
     */
    getAuthorizationPath(): any;
    /**
     * 权限字段路径
     */
    getAuthfieldPath(): any;
    /**
     * 重授权对象路径
     */
    getReAssignPath(): string;
    /**
     * 获取新增的内容
     * @param newarr: 最新的内容
     * @param oldarr：历史内容
     */
    getAddIds(newarr: string[], oldarr: string[]): string[];
    /**
     * 获取删除的内容
     * @param newarr: 最新的内容
     * @param oldarr：历史内容
     */
    getRemoveIds(newarr: string[], oldarr: string[]): string[];
    convertToBizDataDTO(arrs: string[]): BizDataDTO[];
    /**
     * 有效的字符串
     */
    isNullOrEmptyString(value: string): boolean;
    /**
     * 不为空
     */
    isBlank(value: any): boolean;
    /**
     * 字符串不为空
     */
    isNotNullOrEmptyString(value: string): boolean;
    /** 找到树形Parent分级方式的加载的第一层 */
    findLayer1ByParentId(datas: any[], key: string, parentKey: string): any[];
    /** 类型是否为导航 */
    isNav(type: string): boolean;
    expandIcon: string;
    collapseIcon: string;
    leafIcon: string;
    setTreeIcon(farrisTt: TreeTableComponent): void;
}
