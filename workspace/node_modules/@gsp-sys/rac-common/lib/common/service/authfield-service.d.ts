import { HttpErrorResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { RestfulService } from '@farris/devkit';
import { RacCommonUtil } from '../util/rac-util';
import { MessagerService } from '@farris/ui-messager';
/** 取数服务 */
export declare class AuthfieldService {
    private rest;
    private util;
    private msgService;
    constructor(rest: RestfulService, util: RacCommonUtil, msgService: MessagerService);
    /**根据权限字段的标识获取权限字段
     * @param authfield 权限字段标识
     */
    getbyId(authfield: string): Observable<any>;
    /**
     * 异常处理
     * @param auobj 权限对象实体
     */
    handlerError(error: HttpErrorResponse): void;
    private handleError;
}
