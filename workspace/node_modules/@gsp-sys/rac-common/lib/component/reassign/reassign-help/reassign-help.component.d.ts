import { OnInit, EventEmitter } from '@angular/core';
import { LookupComponent } from '@farris/ui-lookup';
import { ReAssignEntity } from '../../../common/entity';
export declare class ReassignHelpComponent implements OnInit {
    displayText: string;
    curKeyId: string;
    datas: ReAssignEntity[];
    curKeyIdList: string[];
    curObj: ReAssignEntity;
    selectedKeyOld: string;
    reassignLookup: LookupComponent;
    /** 导航事件 */
    selectedChanged: EventEmitter<ReAssignEntity>;
    constructor();
    ngOnInit(): void;
    afterKeySelected(args: any): void;
    /**
     * 选中行改变后触发事件
     */
    handleSelection(args: any): void;
}
