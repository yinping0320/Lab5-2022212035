import { OnInit, OnChanges, EventEmitter, TemplateRef, SimpleChanges } from '@angular/core';
import { ReAssignEntity } from '../../../common/entity/reassign.entity';
import { ReAssignService } from '../../../common/service/reassign.service';
export declare class ReassignGridComponent implements OnInit, OnChanges {
    private reAssignService;
    columns: any;
    datas: ReAssignEntity[];
    selectedKey: string;
    selectedKeyOld: string;
    selectedModel: ReAssignEntity;
    selectedChanged: EventEmitter<ReAssignEntity>;
    /** 帮助输出事件 */
    selectedAfterOk: EventEmitter<ReAssignEntity>;
    closeModalEmitter: EventEmitter<any>;
    btnOkAndCancleRef: TemplateRef<any>;
    constructor(reAssignService: ReAssignService);
    ngOnInit(): void;
    /** 获取允许重授权的权限 */
    initData(): void;
    /** 输入属性变化后 */
    ngOnChanges(changes: SimpleChanges): void;
    /**
     * 选中行改变后触发事件
     */
    handleSelection(args: any): void;
    iconClass({ id }: any): any;
    /** 帮助确定按钮 */
    confirm(): void;
    cancle(): void;
}
