import { OnInit, EventEmitter } from '@angular/core';
import { LookupComponent } from '@farris/ui-lookup';
import { AuthobjEntity } from '../../../common/entity/authobj.entity';
export declare class AuobjHelpComponent implements OnInit {
    /** 要加载的数据类型
     * all代表全部权限对象
     * reassign代表允许在用户上重新授权的权限对象
     */
    dataMode: 'all' | 'reassign';
    displayText: string;
    curKeyId: string;
    curKeyIdList: string[];
    curAuthObj: AuthobjEntity;
    selectedKeyOld: string;
    auobjLookup: LookupComponent;
    /** 导航事件 */
    selectedChanged: EventEmitter<AuthobjEntity>;
    ngOnInit(): void;
    constructor();
    afterKeySelected(args: any): void;
    /**
     * 选中行改变后触发事件
     */
    handleSelection(args: any): void;
}
