import { SysCommonUtilService } from '../service/sys-manager-util.service';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
/** 保密测评服务类 */
export declare class SysSecurityConfigService {
    private http;
    private sysUtil;
    constructor(http: HttpClient, sysUtil: SysCommonUtilService);
    /** 是否开启三员 */
    isOnSecurity(): Observable<boolean>;
    /**
     * 根据当前登录用户获取保密测评的功能配置范围
     * @param menuId 功能标识
     */
    getCurrentUserAppConfig(menuId: string): Observable<any>;
    /**
     * 根据指定用户类别获取界面显示信息-,目前只传递type="alluser"使用
     * @param menuId 功能标识
     */
    getAppConfigByUserType(menuId: string, type: string): Observable<any>;
}
