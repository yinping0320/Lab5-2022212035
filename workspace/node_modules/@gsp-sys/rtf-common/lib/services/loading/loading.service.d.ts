import { LoadingComponent } from './loading.component';
import { ApplicationRef, ComponentFactoryResolver, Injector } from '@angular/core';
import { LoadingConfig } from './loading.config';
export declare class LoadingService {
    private appRef;
    private cfr;
    private injecotr;
    config: LoadingConfig;
    private loadingCmpRef;
    constructor(appRef: ApplicationRef, cfr: ComponentFactoryResolver, injecotr: Injector);
    show(config?: LoadingConfig): LoadingComponent;
    close(): void;
    private clearDom;
    private removeDomCheck;
}
