import { OnInit, ElementRef, Renderer2, AfterViewInit, EventEmitter } from '@angular/core';
export declare class LoadingComponent implements OnInit, AfterViewInit {
    private render;
    private el;
    id: number;
    private _isActive;
    message: string;
    isActive: boolean;
    loadingContainerEl: ElementRef;
    loadingBackdrop: ElementRef;
    closed: EventEmitter<{}>;
    constructor(render: Renderer2, el: ElementRef);
    ngOnInit(): void;
    ngOnDestroy(): void;
    ngAfterViewInit(): void;
    close(): void;
    private setPosition;
    private animate;
    private addAnimationEndEvent;
}
