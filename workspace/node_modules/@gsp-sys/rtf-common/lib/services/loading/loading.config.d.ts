import { InjectionToken } from '@angular/core';
export interface LoadingConfig {
    container?: any;
    message?: string;
    position?: string;
    template?: string;
}
export declare const loaddingDefaultConfig: {
    container: string;
    message: string;
    posion: string;
};
export declare const LOADING_DEFAULT_CONFIG: InjectionToken<LoadingConfig>;
