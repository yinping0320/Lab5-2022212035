export declare class FrameworkVariableService {
    private frameworkVariable;
    private nameSpace;
    private frmVariableName;
    private windowService;
    constructor();
    getFrameworkVariable(): any;
    /**
     * 在框架顶级window注册变量
     */
    frameworkVariableReg(key: string, value: any): void;
    getFrmVariableByKey(key: string): any;
    /**
     * 在框架的顶级window上
     * 定义一个公共变量。
     * 用于存放菜单、应用缓存数据，eventbus都公共对象
     * 对适配层提供服务
     */
    private frmVariableInitial;
}
